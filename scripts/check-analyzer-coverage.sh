#!/usr/bin/env bash
# scripts/check-analyzer-coverage.sh
# Stub. The real implementation lands when the first library opts into
# `-fanalyzer` (per docs/rules/static-analysis.md). At that point this script
# walks src/oran-*/README.md and asserts that the documented analyzer-clean TU
# list matches the set of TUs the build feeds to `-fanalyzer`.
set -euo pipefail
echo "(stub) check-analyzer-coverage.sh — real implementation TBD; see docs/rules/static-analysis.md"
