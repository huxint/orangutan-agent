#!/usr/bin/env bash
# scripts/check-pch.sh
# Stub. Real implementation lands alongside the artifact this script enforces.
# Listed in docs/rules/critical-rules.md or related rule docs as the mechanical
# enforcement of a documented constraint. Until then this script is a no-op so
# CI does not falsely succeed.
set -euo pipefail
echo "(stub) check-pch.sh — real implementation TBD; see docs/rules/critical-rules.md and docs/rules/docs-in-sync.md"
