# Tech Debt Tracker

Track known debt that is real enough to preserve but not urgent enough to block the
current task.

Order rows by *date discovered* (newest at the top). Add a row in the same PR that
created the debt.

| Date discovered | Area | Debt | Why It Exists | Planned Follow-Up |
| --------------- | ---- | ---- | ------------- | ------------------ |
| 2026-05-21 | review/deep-2026-05-21 | Backlog of un-actioned findings from [`orangutan-deep-review.md`](../../orangutan-deep-review.md). Slice 31 closed the four rank-0 items (§9.1 Channel race + executor, §9.2 Bus exception, §9.5 doc drift); slice 32 closed the atomic-write P0 item by routing `file.edit` and `file.write` (truncate mode) through a new `oran-io::WriteTextOptions::atomic` opt-in (§4.1.1, `file_edit.cpp:122-150`); slice 33 closed the cancellation-polling P0 item by threading `cancellation_state` into `file.search` `walk_and_scan` + `read_text_capped` (§4.1.3, `file_search.cpp:366-373`); the remainder is grouped here for follow-up slices. **P0 (remaining):** content-size caps on `file.write`/`file.edit` (§4.1.6 / §4.1.7); transparent hashing on `Registry::entries_` to remove the per-dispatch `std::string` allocation (§4.2.1, `registry.cpp:151,160,184`); validate `input_schema_json` at `Registry::add` (§4.1.12). **P1 (pre-`oran-agent`):** `Bus::publish_blocking` + `EventTraits<E>::Decision` (already tracked above as the 2026-05-18 hook row; deep review §5.3 reinforces the priority); extract `tool::parse_input<T>` helper to delete ~120 LoC across the six built-ins (§4.1.8 / §5.2 Rule 1); first `code.*` family (`code.symbols` / `code.references`) via LSP (§5.2 Rule 5); MVP `oran-skill` loader (§5.4); first `bench-oran-agent/prompt_cache_hit_rate` (already tracked above as the 2026-05-17 prompt row). **P2 (design-influencing):** unified `file.modify` + `directory.scan` refactor (§5.2 Rule 4); bump `tool::Output` to `{text, data, attachments, cost, is_error}` (§5.2 Rule 3); parallel sink fan-out in `publish_advisory` (§4.8.2); export `io::run_blocking` as a public utility (§4.1.4 / §4.4.3); `DispatchContext::for_now()` factory (§4.2.6). **P3 (measure first):** `Pool` mutex contention bench (§4.7.1); `Runtime::Impl::run()` clarification or refactor (§4.5.1); `shared_ptr<const Payload>` for multi-sink publish (§4.2.3); vector backend trait + `sqlite-vec` adapter (§5.5 step 4). | The review surfaces ~20 follow-up items; landing them all in one slice would blow the ~600 LoC / ~6 file guideline by an order of magnitude. Each row has a clear file:line target in the review so the next agent can pick one off the top of the priority band and land it in isolation. | Land in priority order, one or two items per slice. Each slice (a) writes its own history, (b) updates the relevant production doc + this row to remove the closed bullets, (c) cites the review §number in the commit body so the link from finding → fix is traceable. Per [`rules/docs-in-sync.md`](../rules/docs-in-sync.md) the same commit closes both code and docs. |
| 2026-05-18 | hook | Hook bus dispatch is advisory-only; sinks observe but cannot veto. | Slice 22 ships the foundation + tool-dispatch wiring; the first blocking consumer (the operator-prompt sink for `permission_ask_rendered`, or a `tool_before` rewrite path) does not exist yet, so adding the veto branch would land unused. | Add a `publish_blocking(Event, Payload) -> Awaitable<Result<Decision>>` overload + the matching `EventTraits` specialisations once the first blocking sink lands. ~80 LoC implementation + tests; the advisory `publish_advisory` stays as the cheap path. |
| 2026-05-17 | tool/search | `file.search` does not yet ship ripgrep-class optimisations (mmap'd reads, extension-based binary skip, `.gitignore` parsing, multi-threaded directory walk). | The hand-rolled implementation is ~30 µs for a 4-file tree at slice 20 — adequate for pre-v1 agent workloads but 3-10× slower than a tuned scanner on repo-scale (10k+ files) inputs. Optimisation is premature without a real workload measurement. | Re-bench against a representative repo workload once `oran-agent` lands. The handler is in its own TU; the upgrade can replace the implementation without touching the dispatch surface. Optimisation order per [`rules/testing-and-bench.md`](../rules/testing-and-bench.md): algorithmic (Boyer-Moore-Horspool for long patterns) → cache-friendly (mmap) → memoisation (extension/`.gitignore` filters) → parallelism (per-subtree work-stealing on `runtime.cpu_executor()`). |
| 2026-05-17 | prompt | `scripts/check-prompt-preamble` static grep promised in [`rules/prompt-design.md`](../rules/prompt-design.md) "Enforcement" is not yet implemented. | The prompt builder does not exist yet; the grep would have no preamble to scan. | Author when the first stable preamble template lands in `oran-agent`; then list under "Mechanical Enforcement" in [`rules/docs-in-sync.md`](../rules/docs-in-sync.md). |
| 2026-05-17 | prompt | `bench/oran-agent/prompt_cache_hit_rate.cpp` regression scenario promised in [`rules/prompt-design.md`](../rules/prompt-design.md) "Enforcement" is not yet implemented. | The `oran-agent` library does not exist yet; the bench bucket and the fixture have no host. | Ship alongside `oran-agent` slice 1 (system preamble builder). Fixture shape lives in [`product-specs/0010-benchmark-harness.md`](../product-specs/0010-benchmark-harness.md) once written. |
| 2026-05-20 | build | `scripts/check-compile-budget.sh` exists and works (slice 28) but is not wired into `scripts/ci.sh`. | Two preconditions: (1) `ci.sh` does not yet run xmake — the trailing comment documents the gap; (2) `docs/rules/compile-budget.md`'s reference hardware (8-core / NVMe / native Linux) is not what every CI runner provides today. Wiring the gate without calibrating would either fail every PR on slower hardware or require `--no-strict` (which defeats the purpose). | When CI provisions xmake on the documented reference hardware, the next slice replaces the trailing "Once xmake is provisioned" comment block in `scripts/ci.sh` with real `xmake build` + `xmake test` + `scripts/check-compile-budget.sh` invocations. If hardware diverges, recalibrate `compile_budget.json` in the same commit. |
| 2026-05-14 | docs | Generated `docs/generated/config.schema.json` is not yet implemented. | The first config slice ships a typed loader and checked `config.example.json`; schema generation waits for broader config section models. | Generate from `oran-config` C++ types via build step. |
| 2026-05-14 | bench | A-vs-B scenarios listed in `bench/<lib>/README.md` are placeholders. | Bench code follows each library's MVP. | Land with the corresponding library's first PR. |
| 2026-05-14 | docs/web | Frontend stack choice (Preact vs. plain JS) not yet decided. | Defer until the first useful UI flow. | Decide in `docs/exec-plans/active/<date>-web-mvp.md`. |
