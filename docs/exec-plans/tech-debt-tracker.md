# Tech Debt Tracker

Track known debt that is real enough to preserve but not urgent enough to block the
current task.

Order rows by *date discovered* (newest at the top). Add a row in the same PR that
created the debt.

| Date discovered | Area | Debt | Why It Exists | Planned Follow-Up |
| --------------- | ---- | ---- | ------------- | ------------------ |
| 2026-05-26 | review/deep-2026-05-26 | Cleanup items lifted from the 2026-05-26 multi-agent deep review of slice 112. Slices 113 and 114 closed the high/medium and most low-severity findings: F2 (SessionState observation), F19 (executor validation), F1 + F18 (trace-write error preservation), F5 (`route_profile` under fallback via new `Response::route_profile_used`), F6 + F12 (bootstrap parse_args strictness — short-flag sniff + duplicate-flag rejection), F9 (OpenAI Responses `status="cancelled"` → terminal `StopReason::cancelled`), F10 (retry-backoff sleep error target context), F11 (OpenAI system-message dedup into `instructions`), F3 + F4 + F23 (oran-io singleflight RAII leader guard + cross-executor wake + redundant post removal), F8 (spec 0018 binary-handoff sync), F13 (signal_drain docstring), F14 (static_cast<void>), F15 (signal_name string_view), F16 + F20 (raw `new` → `make_unique` with passkey), F22 (BoundedCache invalidated reason), F24 (erase_if in approval broker), F25 (duplicate is_zero_id helper). Remaining: write a dedicated singleflight regression test that exercises cross-executor wake plus leader cancellation under a deterministic scheduler (the live fix is covered by unchanged `test-io` semantics, but a targeted test would catch future regressions sooner); audit any future cache stat consumers when the `evictions_*` semantics tighten further. | The bundled slices cover the actionable findings; what is left is a regression-test gap rather than a production defect. | Open a small follow-up slice when the singleflight test scaffold can drive deterministic interleavings (likely after the future `asio::strand` audit). Cite the 2026-05-26 deep review as provenance only. |
| 2026-05-21 | review/deep-2026-05-21-followup | Cleanup items lifted from the second 2026-05-21 deep review (Refactor-Agent Tool Review) that did **not** turn into a product spec. Slice 34 deleted the stale root deep-review artifact after its actionable items were absorbed into this tracker and specs 0011-0018; it also closed the `-Wmissing-field-initializers` warnings in `tests/tool/test_registry.cpp` by default-constructing captured hook rows and centralising the test-only empty-capability `ToolDef`; slice 151 closed the config strictness sweep for typed nested provider/route/hook sections while preserving reserved hook sink/binding placeholders; slice 152 closed the `FileWrite` / `FileEdit` default hook payload redaction item by delivering hash-and-byte-count input summaries to non-trusted sinks while preserving raw inputs for `trusted_local` sinks; slice 153 closed the atomic-write durability item by adding `WriteTextDurability` fsync modes and PID+random exclusively-created temp leaves. Remaining: (b) Wire `xmake build orangutan && xmake test` into `scripts/ci.sh` once the existing 2026-05-20 compile-budget row's reference-hardware precondition holds. | The second review (slice 33 state) re-confirmed the slice-30 backlog was partially superseded by slices 31/32/33 and identified six additional small fixes. Each is < 1 slice on its own; bundling would blow the ~600 LoC / ~6 file guideline and obscure the link from finding → fix. | Land one item per slice. Each slice writes its own history and updates this row to remove the closed bullet. Cite review §numbers as provenance only; current targets live in this tracker and the linked specs. Lifecycle is [`rules/deep-review.md`](../rules/deep-review.md). |
| 2026-05-21 | review/deep-2026-05-21 | Backlog absorbed from the now-deleted root deep-review artifact. Historical § tags are retained only as provenance; the current source of truth is this row plus specs 0011-0018. Slice 31 closed the four rank-0 items (§9.1 Channel race + executor, §9.2 Bus exception, §9.5 doc drift); slice 32 closed the atomic-write P0 item by routing `FileEdit` and `FileWrite` (truncate mode) through a new `oran-io::WriteTextOptions::atomic` opt-in (§4.1.1, `file_edit.cpp:122-150`); slice 33 closed the cancellation-polling P0 item by threading `cancellation_state` into `FileSearch` `walk_and_scan` + `read_text_capped` (§4.1.3, `file_search.cpp:366-373`); slice 34 closed the content-size cap P0 item by adding `max_bytes` to `FileWrite` / `FileEdit` (§4.1.6 / §4.1.7; default and hard ceiling 16 MiB); slice 35 closed the `Registry::add` JSON Schema sanity-check P0 item (§4.1.12) by validating `input_schema_json` before insertion; slice 36 closed the transparent-hashing P0 item (§4.2.1) by making `Registry::entries_` use heterogeneous lookup and removing the per-lookup `std::string` construction from `remove`, `find`, and `dispatch`; slice 60 closed the P2 `tool::Output` too-small item (§5.2 Rule 3) by promoting the public result envelope to `{text, data_json, attachments, usage, is_error}` and copying usage into `ToolAfterPayload` while leaving provider, audit, byte-cap, and built-in structured migrations under spec 0014; slice 72 closed the first `bench-agent` prompt-cache fixture by adding the SessionState no-promotion / after-promotion scenario; slice 115 closed the P1 `tool::parse_input<T>` helper item (§4.1.8 / §5.2 Rule 1) by extracting `tool::detail::parse_input_object` and `require_string_field` into `src/oran-tool/_impl/parse_input.hpp` and rewriting all seven built-ins to consume them; slice 154 closed the P2 `io::run_blocking` public utility item (§4.1.4 / §4.4.3) by exporting the existing cancel-aware blocking boundary through `<oran/io/blocking.hpp>`; slice 155 closed the P2 `DispatchContext::for_now()` factory item (§4.2.6) by moving current-clock context creation and prototype clone/refresh into the public `oran-tool` context surface; slice 156 closed the P2 parallel advisory fan-out item (§4.8.2) by starting subscribed `publish_advisory` sinks as sibling child coroutines while preserving subscription-ordered `PublishOutcome` rows; slice 157 closed the P3 `Pool` mutex contention bench item (§4.7.1) by adding an acquire-only reader-pool contention pair to `bench-storage`; slice 158 closed the P3 `shared_ptr<const Payload>` multi-sink publish item (§4.2.3) by switching sinks to shared immutable `PayloadPtr` delivery and reusing one raw plus one default/redacted payload snapshot per hook publish; slice 159 closed the P3 `Runtime::Impl::run()` clarification item (§4.5.1) by making the private runtime lifecycle one-shot and translating escaping `io_context.run()` handler exceptions into `Result` errors; slice 160 closed the vector backend trait half of the P3 memory follow-up by adding `memory::longterm::Backend`, `VectorBackend`, and validation helpers without wiring sqlite-vec; slice 161 closed the default FTS5 repository half by adding `memory::longterm::Fts5Backend` over `storage::Pool`; slice 162 closed the library-local runtime recall composition half by adding `memory::longterm::Runtime` and deterministic `memory::Framing` rendering from recall hits; slice 163 closed the bootstrap assembly ownership half by adding configured-route `memory.db` provisioning plus backend/runtime accessors; slice 164 closed the prompt-boundary recall rendering half by adding opt-in `AgentPromptRunnerOptions::longterm_recall` plumbing; slice 165 closed the configured-route config mapping half through `memory.longterm.recall.enabled` / `limit`; slice 166 closed the first richer recall query-policy half by mapping optional configured kind filters into `memory::longterm::Query::kinds`; slice 167 closed the first query-derivation selector by adding `memory.longterm.recall.query_strategy` with `prompt_text` and `last_user_message`; slice 168 closed the first read-only memory tool by wiring deferred `MemoryRecall` through the existing permissioned dispatch path; slice 169 closed the first write-side memory tool by wiring deferred `MemoryRemember` through that same dispatch path; slice 170 closed the delete-side long-term memory tool by wiring deferred `MemoryForget` through that same dispatch path; slice 171 added the first 10k-record FTS5 `longterm::Runtime::search` benchmark baseline; slice 172 closed the first hybrid/vector search composition contract by adding `memory::longterm::HybridRuntime` over the shipped lexical and vector backend traits; slice 173 added the first vector-vs-FTS5 corpus comparison bench (`bench/memory/scenarios/search_fts5_vs_vector.cpp`) over a brute-force cosine reference `VectorBackend`; slice 176 closed the real gated vector backend by adding `memory::longterm::SqliteVecBackend` behind `--vector_memory=y`; slice 177 closed the sqlite-vec corpus comparison numbers by adding gated sqlite-vec vector-only and FTS5+sqlite-vec hybrid rows to `bench-memory`; slice 178 closed the gated bootstrap embedding/vector owner and hybrid recall wiring by consuming `memory.longterm.hybrid_search` under `--vector_memory=y` while preserving the default-build guard. **P1 (pre-full-loop):** `Bus::publish_blocking` + `EventTraits<E>::Decision` (already tracked above as the 2026-05-18 hook row; deep review §5.3 reinforces the priority; first consumer is the `agent::ToolScheduler` per spec 0012 v1.1); first `code.*` family (`CodeSymbols` / `code.references`) via LSP (§5.2 Rule 5; index home is spec 0012 v1.1); MVP `oran-skill` loader (§5.4). **P2 (design-influencing):** unified `FileModify` + `DirectoryScan` refactor (§5.2 Rule 4; spec 0011 v2 owns the `FileModify` shape). **P3 (FTS5 baseline, hybrid contract, FTS5-vs-vector-vs-hybrid bench, gated sqlite-vec adapter, sqlite-vec corpus comparison, and gated bootstrap hybrid wiring shipped):** remaining work is semantic/external embedding provider ownership and unified JSON emission (§5.5 step 4). | The deleted review surfaced ~20 follow-up items; landing them all in one slice would blow the ~600 LoC / ~6 file guideline by an order of magnitude. The remaining items are copied here with current code/spec targets so future agents do not need the deleted artifact. | Land in priority order, one or two items per slice. Each slice (a) writes its own history, (b) updates the relevant production doc + this row to remove the closed bullets, (c) cites the historical §number as provenance when useful. Per [`rules/docs-in-sync.md`](../rules/docs-in-sync.md) the same commit closes both code and docs. Future deep-review artifacts follow the lifecycle in [`rules/deep-review.md`](../rules/deep-review.md). The roadmap home for the *future* (non-cleanup) work surfaced by the second deep review is now [`product-specs/0011-file-view-and-caching.md`](../product-specs/0011-file-view-and-caching.md) + [`0012-tool-scheduler-and-state.md`](../product-specs/0012-tool-scheduler-and-state.md) + [`0013-workspace-and-path-policy.md`](../product-specs/0013-workspace-and-path-policy.md) + [`0014-structured-tool-output.md`](../product-specs/0014-structured-tool-output.md). |
| 2026-05-17 | tool/search | `FileSearch` does not yet ship ripgrep-class optimisations (mmap'd reads, extension-based binary skip, multi-threaded directory walk). | The hand-rolled implementation is adequate for pre-v1 agent workloads, and slices 48-49 now prune common ignored trees via a built-in skip list plus `.gitignore` / `.ignore` rule files. It is still 3-10x slower than a tuned scanner on repo-scale (10k+ files) inputs because reads are stream-based and the walk is single-threaded. Optimisation is premature without a real workload measurement. | Re-bench against a representative repo workload once the full `oran-agent` loop produces one; slice 72's SessionState-only fixture is not that workload. The handler is in its own TU; the upgrade can replace the implementation without touching the dispatch surface. Optimisation order per [`rules/testing-and-bench.md`](../rules/testing-and-bench.md): algorithmic (Boyer-Moore-Horspool for long patterns) → cache-friendly (mmap) → memoisation (extension-aware binary filters) → parallelism (per-subtree work-stealing on `runtime.cpu_executor()`). |
| 2026-05-20 | build | `scripts/check-compile-budget.sh` exists and works (slice 28) but is not wired into `scripts/ci.sh`. | Two preconditions: (1) `ci.sh` does not yet run xmake — the trailing comment documents the gap; (2) `docs/rules/compile-budget.md`'s reference hardware (8-core / NVMe / native Linux) is not what every CI runner provides today. Wiring the gate without calibrating would either fail every PR on slower hardware or require `--no-strict` (which defeats the purpose). | When CI provisions xmake on the documented reference hardware, the next slice replaces the trailing "Once xmake is provisioned" comment block in `scripts/ci.sh` with real `xmake build` + `xmake test` + `scripts/check-compile-budget.sh` invocations. If hardware diverges, recalibrate `compile_budget.json` in the same commit. |
| 2026-05-14 | docs | Generated `docs/generated/config.schema.json` is not yet implemented. | The first config slice ships a typed loader and checked `config.example.json`; schema generation waits for broader config section models. | Generate from `oran-config` C++ types via build step. |
| 2026-05-14 | bench | A-vs-B scenarios listed in `bench/<lib>/README.md` are placeholders. | Bench code follows each library's MVP. | Land with the corresponding library's first PR. |
