## [YYYY-MM-DD HH:mm] | Task: <short task title>

### Execution Context

- Agent: `<fill in>`
- Base model: `<fill in>`
- Runtime: `<fill in>`
- Linked plan: `docs/exec-plans/active/...md`

### User Query

> <paste or summarize the user request>

### Changes Overview

- Areas:
- Key actions:

### Design Intent

Why this change was made and what tradeoff it chooses. Reference the matching design
doc when relevant.

### Files Modified

- `path/to/file`

### Docs Updated In This PR (Prime Directive — see `docs/rules/docs-in-sync.md`)

List every doc edited in the same PR as part of this change. If the change
invalidated a doc and the matching edit is *missing*, the PR is incomplete.

- `docs/design-docs/<area>.md` — <what changed>
- `docs/product-specs/<id>.md` — <what changed>
- `docs/rules/<rule>.md` — <if a rule shifted>
- `docs/ARCHITECTURE.md` — <if library / boundary shifted>
- `docs/releases/feature-release-notes.md` — <if user-visible>
- `README.md` — <if quick-start affected>
- … etc.

If no docs needed editing, state explicitly: "No external doc invalidation; change is
internal to `<file>` and does not affect any documented contract."

### Validation

- Commands run:
- Tests added/changed:
- Bench impact:
- Compile-budget delta:

### Follow-ups

- Issues opened:
- Tech-debt entries:
- Linked release note:
