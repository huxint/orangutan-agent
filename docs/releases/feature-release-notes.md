# Feature Release Notes

## 2026-06

| Date | Area | User Impact | Change Summary | History |
| ---- | ---- | ----------- | -------------- | ------- |
| 2026-06-04 | trace-retention | Configured trace retention now has runtime effect. On startup, `trace.retention_days` derives a cutoff and old `trace_turns` rows are purged before the trace repository is exposed; permission/audit rows are left intact because audit retention is a separate policy. | Slice 150 adds `TraceRepository::purge_turns_started_before(...)`, threads an explicit retention cutoff through `RuntimeAssemblyOptions`, and has `bootstrap::run` compute that cutoff from `config.trace().retention_days`. The binary slice tag is now `2.0.0-slice150`. Focused validation: `test-storage` 76 cases / 986 assertions; `test-bootstrap` 101 / 711. | [history](../histories/2026-06/20260604-1123-trace-retention-enforcement.md) |
| 2026-06-03 | skill-deactivate | The agent can now turn a skill off mid-session without a config edit. Calling `skill.deactivate("<name>")` records a deactivation in the conversation so the skill stops appearing as active in the prompt's skill catalog from the next turn onward; invoking it again reactivates it (most recent action wins). The skill stays available to invoke. | Slice 147 adds the permissioned `skill.deactivate` built-in (capability `deactivate_skill`, input `{"name": <string>}`), `skill::render_deactivation_data_json` / `deactivated_skill_from_data_json`, and an order-aware `skill::active_skills_from_transcript` that nets `skill.invoke` activations against `skill.deactivate` deactivations in transcript order. Bootstrap installs the `DispatchContext::skill_deactivate` callback and the tool joins the default active catalog (`oran-prompt` / `oran-agent`). The binary slice tag is now `2.0.0-slice147`. Focused validation: `test-skill` 24 cases / 155 assertions; `test-tool` 191 / 1919; `test-core` 71 / 459; `test-bootstrap` 99 / 667. | [history](../histories/2026-06/20260603-1526-skill-deactivate.md) |
| 2026-06-03 | config-skill-activation-policy | Operators can now steer skill active markers from config. A selected agent's `agents.<name>.skills_deactivated` skills are never marked active, and `agents.<name>.skills_expirations` rows drop a skill's active marker once its `expires_at` passes. The section-4 renderer stays clock-free; the runner reads the wall clock once per prompt as the evaluation time. Default and no-agent runs are unchanged. | Slice 146 adds typed `agents.<name>.skills_deactivated` and `agents.<name>.skills_expirations` (`{name, expires_at}`, strict UTC ISO-8601) parsing to `oran-config`, and has `AgentPromptRunner` build a non-empty `skill::ActivationPolicy` from the selected agent config (mapping `config::SkillExpirationConfig` to `skill::SkillExpiration`, supplying `evaluation_time = core::time::now_utc()` at the prompt boundary only when expirations are present). The binary slice tag is now `2.0.0-slice146`. Focused validation: `test-config` 39 cases / 299 assertions; `test-bootstrap` 98 / 646. | [history](../histories/2026-06/20260603-1035-config-skill-activation-policy.md) |
| 2026-06-02 | skill-expiration-policy | Skill activation policy now has an explicit prompt-boundary expiration input for future runtime callers. Current configured-route behavior is unchanged because bootstrap supplies no expiration rows or evaluation time until a runtime event source owns those inputs. | Slice 145 adds `skill::SkillExpiration`, optional `ActivationPolicy::evaluation_time`, validates expiration names as unique single-line policy inputs, requires explicit evaluation time when expirations are supplied, and has `skill::resolve_active_skills(...)` subtract expired names from transcript-derived active markers after loaded/allowed catalog filtering. The binary slice tag is now `2.0.0-slice145`. Focused validation: `test-skill` 21 cases / 133 assertions. | [history](../histories/2026-06/20260602-0253-skill-expiration-policy.md) |
| 2026-06-02 | skill-deactivation-policy | Skill activation policy now has an explicit prompt-boundary deactivation input for future runtime callers. Current configured-route behavior is unchanged because bootstrap supplies an empty deactivation set until a runtime event source owns those inputs. | Slice 144 adds `ActivationPolicy::deactivated_skill_names`, validates names as unique single-line policy inputs, and has `skill::resolve_active_skills(...)` subtract them from transcript-derived active markers after loaded/allowed catalog filtering. The binary slice tag is now `2.0.0-slice144`. Focused validation: `test-skill` 19 cases / 121 assertions; `test-bootstrap` 95 / 617. | [history](../histories/2026-06/20260602-0224-skill-deactivation-policy.md) |
| 2026-06-02 | skill-activation-policy | Skill active markers now have an explicit policy owner before expiration or deactivation behavior is added. Current behavior is unchanged for operators: successful `skill.invoke` calls still mark the skill active on the next prompt only when that skill remains loaded and allowed. | Slice 143 adds `skill::ActivationPolicy` and `skill::resolve_active_skills(...)` to `oran-skill`, moves the loaded/allowed catalog filtering out of bootstrap-local helper code, and pins disabled-policy plus duplicate-catalog validation coverage. The binary slice tag is now `2.0.0-slice143`. Focused validation: `test-skill` 17 cases / 111 assertions; `test-bootstrap` 95 / 617. | [history](../histories/2026-06/20260602-0139-skill-activation-policy.md) |
| 2026-06-02 | skill-active-markers | After the agent successfully invokes a loaded skill, the next configured-route prompt now marks that skill active in section 4 instead of relying only on the prior tool-result text. The active turn's cached prefix still stays unchanged, and removed or disallowed skills do not remain active. | Slice 142 adds `skill::ActiveSkill`, versioned `skill.invoke` activation `data_json`, transcript-derived active marker extraction in `oran-skill`, and bootstrap prompt-runner filtering against the current loaded/allowed skill snapshot before rendering `Active Skill: <name>` rows. The binary slice tag is now `2.0.0-slice142`. Focused validation: `test-skill` 14 cases / 102 assertions; `test-bootstrap` 95 / 617. | [history](../histories/2026-06/20260602-0055-skill-active-markers.md) |
| 2026-06-01 | agent-prompt-overlay | Configured-route prompts can now carry stable per-agent section-6 instructions from config. A selected `agents.<name>.prompt_overlay` becomes the prompt overlay for that agent, while exact embedder/test-supplied overlay bytes still win. | Slice 141 adds `agents.<name>.prompt_overlay` to `oran-config`, documents it in `config.example.json`, and has `AgentPromptRunner` read the same selected `agents.<name>` entry for both prompt overlay and `skills_enabled`. Permission materialization remains on the existing permission-agent selector. The binary slice tag is now `2.0.0-slice141`. Focused validation: `test-config` 37 cases / 278 assertions; `test-bootstrap` 94 / 597. | [history](../histories/2026-06/20260601-2327-agent-prompt-overlay.md) |
| 2026-06-01 | runtime-agent-selector | Configured-route binary prompts can now run as a named agent from config. `--agent <name>` selects that agent's permission overlay, skill allowlist, audit/session key, and provider hook metadata; `--mode <strict\|default\|permissive\|sandboxed>` selects the permission baseline for the same run. The no-provider deterministic shell rejects selector flags unless `--explain-rules` is active. | Slice 140 maps the existing bootstrap selectors into `AgentPromptRunnerOptions`, keeps the diagnostic `--explain-rules` materializer on the same selector semantics, updates usage text and README quick-start examples, and adds bootstrap coverage for no-route rejection, per-agent skill filtering, and strict-mode tool denial through a localhost provider/tool loop. The binary slice tag is now `2.0.0-slice140`. Focused validation: `test-bootstrap` 93 cases / 586 assertions. | [history](../histories/2026-06/20260601-2250-runtime-agent-selector.md) |
| 2026-06-01 | per-agent-skill-enable | Embedders and future runtime-agent selectors can now give different agents different visible workspace skills. A selected agent with `skills_enabled` sees only those skills in the prompt catalog, and `skill.invoke` treats filtered-out skills as not loaded. | Slice 139 adds `agents.<name>.skills_enabled` to `oran-config`, validates it as an optional string array of non-empty skill names, and has `AgentPromptRunner` apply the selected agent config's allowlist after each workspace skill snapshot refresh. Exact caller-supplied `skills_catalog` bytes still bypass directory loading, and ordinary binary startup still constructs the default CLI runner. The binary slice tag is now `2.0.0-slice139`. Focused validation: `test-config` 37 cases / 274 assertions; `test-bootstrap` 90 / 566. | [history](../histories/2026-06/20260601-2214-per-agent-skill-enable.md) |
| 2026-06-01 | skill-hot-reload | Configured-route prompts now notice skill files added, updated, or removed under `<workspace>/.orangutan/skills` before the next prompt without restarting the process. The active turn still sees one coherent skill catalog and body snapshot, and invoked skill bodies remain ordinary tool-result text instead of entering the system preamble. | Slice 138 adds `skill::WorkspaceSkillSnapshot`, which reloads the compact section-4 catalog plus `skill.invoke` document snapshot together. It uses Linux inotify when available, falls back to a bounded content-aware directory signature before each prompt, invalidates `oran-io` file-view cache entries before changed skill markdown is re-read, and keeps exact caller-supplied `skills_catalog` bytes as the test/embedder bypass. The binary slice tag is now `2.0.0-slice138`. Focused validation: `test-skill` 11 cases / 89 assertions; `test-bootstrap` 88 / 539. | [history](../histories/2026-06/20260601-2124-skill-hot-reload.md) |
| 2026-06-01 | skill-invoke | The agent can now call a loaded markdown skill through the normal permissioned tool path. Skill bodies are returned as model-visible tool-result text for the next provider iteration, while the compact skill catalog and stable system preamble remain body-free. | Slice 137 adds the `skill.invoke` built-in to the default active catalog, declares the `invoke_skill` capability, parses `{"name": <string>, "inputs"?: <json>}`, and delegates lookup through bootstrap's loaded skill snapshot so `oran-tool` stays independent from `oran-skill`. The binary slice tag is now `2.0.0-slice137`. Focused validation: `test-tool` 188 cases / 1893 assertions; `test-bootstrap` 87 / 518; `test-prompt` 10 / 98; `test-agent` 56 / 10 744. | [history](../histories/2026-06/20260601-2034-skill-invoke.md) |
| 2026-06-01 | skill-loader-snapshot | Configured-route prompts can now discover markdown skills already present under `<workspace>/.orangutan/skills` without callers pre-rendering catalog bytes. Skill bodies are loaded as bounded snapshots for future invoke work but stay out of the prompt catalog and system preamble. | Slice 136 adds `skill::Loader` / `SkillDocument` / `SkillMetadata`, parses frontmatter fields (`name`, `description`, `triggers`, `inputs`, `model_hint`) from `*.md` files, treats a missing skills directory as an empty snapshot, and has `AgentPromptRunner` load that directory once before the first prompt unless exact `skills_catalog` bytes were supplied. The binary slice tag is now `2.0.0-slice136`. Focused validation: `test-skill` 9 cases / 55 assertions; `test-bootstrap` 86 / 504. | [history](../histories/2026-06/20260601-1946-skill-loader-snapshot.md) |
| 2026-06-01 | skill-catalog-renderer | Configured-route prompts now carry a real section-4 skill catalog owner instead of an empty string placeholder. The catalog stays compact and deterministic, while skill bodies remain out of the system preamble and out of the prompt runner's stable section-1 contract. | Slice 135 adds `oran-skill` with `skill::CatalogRenderer` plus `skill::CatalogOwner`, has `AgentPromptRunner` render the skill catalog once before loop entry, threads the pre-rendered section through `agent::Loop`'s existing prompt-builder boundary, and exposes a new runner render counter. The binary slice tag is now `2.0.0-slice135`. Focused validation: `test-skill` 5 cases / 19 assertions; `bench-skill` deterministic renderer vs. local order-trusting baseline; `test-bootstrap` 85 / 491. | [history](../histories/2026-06/20260601-0745-skill-catalog-renderer.md) |
| 2026-06-01 | stable-system-preamble | Configured-route prompts now use a versioned Orangutan system preamble instead of caller-provided placeholder bytes. The preamble stays small and stable, while tools, memory, skills, per-agent overlays, and conversation history remain in their own prompt sections. | Slice 134 adds `agent::SystemPreamble` / `agent::SystemPreambleOwner` plus the default section-1 text, has `agent::Loop` select its owned default for empty `RunTurnInputs::system_preamble`, and has `AgentPromptRunner` render the stable preamble once before loop entry with a diagnostic counter. `scripts/check-prompt-preamble.sh` now runs in CI to reject clocks, ids, and cross-section prompt bytes in the default preamble. The binary slice tag is now `2.0.0-slice134`. Focused validation: `test-agent` 56 cases / 10 744 assertions; `test-bootstrap` 84 / 477. | [history](../histories/2026-06/20260601-0709-stable-system-preamble.md) |
| 2026-06-01 | prompt-memory-framing | Configured-route prompts now have a dedicated once-per-turn owner for the memory framing section. This keeps future long-term memory recall out of provider/tool iteration loops while preserving the current stable prompt-builder section contract. | Slice 133 adds `memory::Framing` / `memory::FramingOwner` to `oran-memory`, has `AgentPromptRunner` render that section once before calling `agent::Loop`, and exposes a runner render counter for diagnostics. The binary slice tag is now `2.0.0-slice133`. Focused validation: `test-memory` 8 cases / 559 assertions; `test-bootstrap` 83 / 464. | [history](../histories/2026-06/20260601-0622-prompt-memory-framing.md) |
| 2026-06-01 | runner-session-persistence | Configured-route prompts now survive process restarts within the same session identity instead of starting from an in-process-only transcript. Built-in no-route startup still stays deterministic and does not touch session storage. | Slice 132 wires `AgentPromptRunner` to load persisted history from `RuntimeAssembly::session_store()` before each prompt and append only the successful transcript suffix afterward. The runner keeps its in-process transcript as the fallback when session memory is disabled, and the binary slice tag is now `2.0.0-slice132`. Focused validation: `test-bootstrap` 82 cases / 452 assertions. | [history](../histories/2026-06/20260601-0536-runner-session-persistence.md) |
| 2026-06-01 | runtime-session-memory | Configured-route startup now provisions a real session-memory database before agent prompts run, while built-in no-route startup still avoids session state. This makes conversation persistence a runner wiring problem instead of a bootstrap/storage assembly problem. | Slice 131 extends `RuntimeAssemblyOptions` with `sessions_db_path`, `session_memory_enabled`, sessions reader count, and statement-cache sizing; `RuntimeAssembly` now migrates and owns a separate `<workspace>/.orangutan/sessions.db` pool/repository/store and exposes `session_store()`, `session_memory_enabled()`, and `sessions_path()`. `bootstrap::run` enables session memory only when a provider route is configured, and the startup banner reports session state/path. The binary slice tag is now `2.0.0-slice131`. Focused validation: `test-bootstrap` 80 cases / 422 assertions. | [history](../histories/2026-06/20260601-0513-runtime-session-memory.md) |
| 2026-06-01 | memory-session-store | Orangutan now has a real memory-layer session store target ready for runtime wiring. The new wrapper round-trips typed `core::Message` values over the existing SQLite session repository without making storage understand message JSON. | Slice 130 adds `<oran/memory.hpp>` and `memory::session::Store` in the new `oran-memory` library, with typed `SessionId`, `AgentKey`, list summaries, private versioned JSON serialization for text/thinking/tool-use/tool-result blocks, malformed-row parsing errors, and required-id validation. The binary slice tag is now `2.0.0-slice130`. Focused validation: `test-memory` 5 cases / 550 assertions; `bench-memory` raw repository append/load about 849.8 us per 64-message batch vs. typed store append/load about 1022.4 us. Runtime assembly sessions DB wiring follows in slice 131. | [history](../histories/2026-06/20260601-0446-memory-session-store.md) |
| 2026-06-01 | provider-profile-costs | Provider usage rows can now include a cost estimate even when the upstream provider response omits one, as long as the selected profile declares token pricing. Trace rows, provider response hooks, and usage rollups see the computed value; provider-supplied costs remain authoritative. | Slice 129 adds optional `profiles.<name>.pricing` fields for input, output, cache-creation, and cache-read USD-per-million-token prices, carries that metadata through `provider::resolve_route_profiles` / `provider::Route`, and has `agent::Loop` compute missing `Usage::cost_estimate` before hooks, result aggregation, and trace writes. The binary slice tag is now `2.0.0-slice129`. Focused validation: `test-config` 37 cases / 270 assertions; `test-provider` 86 / 652; `test-agent` 52 / 10 705. | [history](../histories/2026-06/20260601-0412-provider-profile-costs.md) |
| 2026-06-01 | cli-repl-slash-commands | REPL operators can now use `/help` to list built-in REPL commands and `/exit` or `/quit` to leave without sending those lines to the agent. This works in provider-backed interactive REPLs and scripted REPL inputs used by tests or drivers. | Slice 128 adds CLI-owned slash-command recognition before prompt-runner dispatch. Recognized commands trim leading/trailing ASCII whitespace, do not increment `prompts_processed`, and leave normal prompts unchanged across `run` and `run_async`. The binary slice tag is now `2.0.0-slice128`. Focused validation: `test-cli` 26 cases / 205 assertions. | [history](../histories/2026-06/20260601-0339-cli-repl-slash-commands.md) |
| 2026-06-01 | provider-usage-rollups | Trace consumers can now ask storage for provider usage grouped by UTC day, agent, route profile, and route model instead of scanning raw `trace_turns` rows by hand. This exposes token/cache totals and any provider-supplied cost estimates already recorded on turns; slice 129 later adds profile-priced cost calculation for providers that omit a cost. | Slice 127 adds `storage::ProviderUsageRollup`, `ListProviderUsageRollupsOptions`, and `TraceRepository::list_provider_usage_rollups`, a read-only aggregate over existing trace rows with optional agent/profile/model filters and a bounded limit. The binary slice tag is now `2.0.0-slice127`. Focused validation: `test-storage` 73 cases / 938 assertions. | [history](../histories/2026-06/20260601-0309-provider-usage-rollups.md) |
| 2026-06-01 | provider-lifecycle-hooks | Configured-route agent turns now publish typed provider lifecycle events on the process hook bus. Local observers can see request, response, error, and fallback metadata for provider calls without receiving prompt text, messages, headers, credentials, or raw provider bodies. | Slice 126 adds metadata-only provider payloads to `oran-hook`, publishes advisory `provider_request`, `provider_response`, `provider_error`, and `provider_fallback` events from `agent::Loop` around each provider await, wires `RuntimeAssembly::hook_bus()` through `AgentPromptRunner`, and keeps `oran-provider` hook-free. The binary slice tag is now `2.0.0-slice126`. Focused validation: `test-hook` 30 cases / 207 assertions; `test-agent` 50 cases / 10 689 assertions; `test-bootstrap` 77 cases / 380 assertions. | [history](../histories/2026-06/20260601-0240-provider-lifecycle-hooks.md) |
| 2026-06-01 | cli-interactive-repl | Configured-route `orangutan --config ...` with no `--prompt` now stays in a provider-backed REPL instead of stopping at the placeholder shell. Enter prompts line-by-line; an empty line or EOF exits cleanly. Built-in empty defaults still use the deterministic no-runner shell and do not read provider credentials. | Slice 125 adds `CliOptions::interactive_repl` and enables it only on the configured-route bootstrap handoff that owns an `AgentPromptRunner`. `cli::run_async` reads terminal stdin through a persistent asio descriptor/stream buffer, dispatches each non-empty line to the supplied `PromptRunner` as `CliMode::repl`, keeps scripted `repl_lines` authoritative for tests/noninteractive drivers, and preserves nonblocking behavior when no runner exists. The binary slice tag is now `2.0.0-slice125`. Focused validation: `test-cli` 23 cases / 176 assertions; `test-bootstrap` 76 cases / 355 assertions, including a local configured-route REPL handoff. | [history](../histories/2026-06/20260601-0102-cli-interactive-repl.md) |
| 2026-06-01 | openai-responses-sse | Configured-route prompts that use the OpenAI Responses protocol can now stream through the same provider/CLI path as Anthropic, so live terminal rendering is no longer Anthropic-only when the selected transport supports SSE. | Slice 124 adds internal `OpenAiResponsesSseDecoder` beside the Anthropic decoder. It forwards `response.output_text.delta`, reasoning deltas, and function-call argument deltas to the existing `provider::EventSink`, treats terminal `response.completed` / `response.incomplete` / `response.failed` events as the authoritative assembled response by decoding their embedded `response` object through `decode_protocol_response`, and surfaces `error` events as upstream failures. `ProtocolTransportSystem` now streams OpenAI Responses when `request.stream` is true and the injected transport reports `supports_streaming() == true`; otherwise it keeps the body path. Focused validation: `test-provider` 86 cases / 643 assertions. | [history](../histories/2026-06/20260601-0013-openai-responses-sse-streaming.md) |

## 2026-05

| Date | Area | User Impact | Change Summary | History |
| ---- | ---- | ----------- | -------------- | ------- |
| 2026-05-30 | provider-sse-streaming | Configured-route `orangutan --prompt` over Anthropic Messages now renders the answer to the terminal token-by-token as it arrives, instead of printing the whole reply at once after the turn finishes. Each tool call is announced on its own `[tool: <name>]` line as the model starts it, and a mid-stream `Ctrl-C` cancels the request. This closes spec 0001 AC3 and the spec 0017 v1.1 "Streaming sink" item, completing the provider-SSE-streaming arc (slices 121–123). | Slice C wires the path end-to-end: bootstrap's `HttpProtocolTransport` overrides `supports_streaming()` to `true` and implements `send_streaming` over `http::Client::send_streaming` (translating each `http::SseEvent` into the provider `ProtocolSseCallback`); new `cli::StreamingPromptSink` (a `provider::EventSink`) renders answer/thinking deltas to an injectable `std::ostream` (default `std::cout`, flushed per delta) plus the tool marker; and `AgentPromptRunner` builds the sink for non-quiet streaming runs, passes it to `agent::Loop::run_turn`, and clears the assembled `PromptRunResult::text` once it streamed live so the CLI does not double-print. `oran-cli` now depends on `oran-provider` (downward, interface → composition). Retry/fallback suppression reuses the slice-97 `AttemptSink` (no new code). Focused validation: `test-cli` 18 cases / 110 assertions, `test-bootstrap` 75 cases / 344 assertions; all 14 buckets green. Docs: `STATUS.md`, the completed provider-SSE-streaming exec-plan, `ARCHITECTURE.md`, API portability, specs 0001/0017, `QUALITY_SCORE.md`, and this release note reflect the shipped wiring. | [history](../histories/2026-05/20260530-1519-cli-streaming-render.md) |
| 2026-05-29 | agent-scheduler-loop-wiring | Parallel tool calls now run through `agent::ToolScheduler` on the production ReAct path. When a provider returns multiple `tool_use` blocks, `agent::Loop` dispatches the whole batch through `scheduler.run_batch` instead of one call at a time: read-only tools run concurrently up to `runtime.tool_scheduler.max_parallel_tools` (default 4), writes/edits/deletes to the same canonical path serialise on a per-path lock, each call is bounded by `per_call_timeout_ms` (default 60000), and cancelling the turn aborts every cancel-aware in-flight call within 100 ms (a tool that ignores its cancellation slot is named in a `cancellation_lag` audit row). Results stay in the model's original `tool_use` order. `bootstrap::AgentPromptRunner` owns the scheduler from config; `config.example.json` documents the new `runtime.tool_scheduler.{max_parallel_tools, per_call_timeout_ms, idle_lock_ttl_ms}` block. This closes the spec-0012 tool-scheduler v1 arc (slices 116-120). | `RunTurnInputs` gains `ToolScheduler* scheduler`; the loop uses it when supplied and otherwise builds a per-turn fallback with default options. A single-call batch threads `approval_token_output` for blocking-ask replay; a parallel batch drops it (one slot cannot disambiguate N issued tokens). The scheduler's semaphore-cancel early-return now resets cancellation before reporting, so a queued-then-cancelled call is not mis-named a laggard. New `oran-config` surface `ToolSchedulerRuntimeConfig`; new benches `bench/agent/scheduler_overhead` (single-call `run_batch` ≈ 6.7 µs vs ≈ 2.4 µs direct dispatch — under the ≤ 75 µs AC12 allowance) and `scheduler_audit_fanout` (8-call batch ≈ 134 µs via `StorageAuditSink` vs ≈ 44 µs via `NullAuditSink`). Focused validation: `test-config` 36 cases / 258 assertions, `test-agent` 47 / 10 618, `test-bootstrap` 72 / 316. Docs: `STATUS.md`, `ARCHITECTURE.md`, `QUALITY_SCORE.md`, `tool-runtime.md`, `async-model.md`, spec 0012 (v1 complete), the completed exec-plan, and this release note. | [history](../histories/2026-05/20260529-2325-agent-loop-scheduler-wiring.md) |
| 2026-05-26 | bootstrap-provider-handoff | Configured-route `orangutan --prompt ...` now drives the real agent loop through the HTTP-backed provider system instead of stopping at the deterministic no-runner shell. When config declares a `default` route, `bootstrap::run` builds `HttpProviderBackend`, reads the configured API-key environment variables at the explicit credential boundary, creates `AgentPromptRunner`, and calls `cli::run_async`. Built-in empty defaults still run without credentials and report `provider route: none configured`. `xmake run orangutan` now reports slice 112. | `bootstrap::run` now uses the process runtime's CPU executor for provider body requests, applies `runtime.request_timeout_ms`, and preserves missing credentials as `ErrorKind::auth` with only non-secret context. Focused validation: `test-bootstrap` 68 cases / 297 assertions, including a localhost Anthropic Messages round trip through ordinary binary handoff and a missing-credential failure before CLI async execution. Docs: `STATUS.md`, completed provider-adapter plan, `ARCHITECTURE.md`, `BUILD_SYSTEM.md`, `QUALITY_SCORE.md`, API portability, bootstrap/CLI/agent/tool design docs, specs 0001/0014/0017, README, and this release note reflect the shipped handoff. | [history](../histories/2026-05/20260526-0738-bootstrap-provider-handoff.md) |
| 2026-05-26 | bootstrap-provider-backend | Bootstrap now has an explicit HTTP-backed provider construction seam ready for the ordinary prompt handoff. `HttpProviderBackend::build(config, options)` resolves the configured route profiles, reads the configured API-key environment variables at the credential boundary, owns an `http::Client` on the caller-provided blocking executor, adapts that client to `provider::ProtocolTransport`, registers the built-in Anthropic Messages and OpenAI Responses protocol factories, and returns a profile-routed provider system plus resolved route for `AgentPromptRunner` callers. `xmake run orangutan` now reports slice 111. Ordinary `bootstrap::run` still does not call this seam, so normal prompts remain on the deterministic no-runner shell until the next handoff slice. | New `<oran/bootstrap/provider_backend.hpp>` exports `HttpProviderBackendOptions` and movable `HttpProviderBackend`; `<oran/bootstrap.hpp>` re-exports it, and `oran-bootstrap` now depends on `oran-http`. Focused validation: `test-bootstrap` 67 cases / 287 assertions, including a localhost Anthropic Messages round trip through libcurl and a missing-credential error with only non-secret context. Docs: `STATUS.md`, active provider-adapter plan, `ARCHITECTURE.md`, `BUILD_SYSTEM.md`, `QUALITY_SCORE.md`, API portability, bootstrap/agent/tool design docs, specs 0001/0014, public header comments, and this release note reflect the shipped backend seam. | [history](../histories/2026-05/20260526-0627-bootstrap-provider-backend.md) |
| 2026-05-26 | http-body-client | Orangutan now has a platform HTTP client target ready for the provider adapter handoff. `http::Client` sends non-streaming body requests through private libcurl handles on a caller-supplied blocking executor, validates request shape before curl dispatch, collects response status/headers/body, and reports cancellation through the existing `core::Result` contract. `xmake run orangutan` now reports slice 110. Ordinary bootstrap still does not read credentials, construct adapters, allocate this client, send network traffic, or start the agent loop for normal prompts. | New `<oran/http.hpp>` / `<oran/http/client.hpp>` export `Header`, `BodyRequest`, `BodyResponse`, and `Client`; `oran-http` is registered in xmake with system `libcurl >=8.11.0`, plus `test-http` and `bench-http` buckets. Focused validation: `test-http` 3 cases / 21 assertions. Docs: `STATUS.md`, active provider-adapter plan, `ARCHITECTURE.md`, `BUILD_SYSTEM.md`, `QUALITY_SCORE.md`, API portability, bootstrap runtime, specs 0001/0017, dependency docs, and this release note reflect the shipped body client. | [history](../histories/2026-05/20260526-0426-http-body-client.md) |
| 2026-05-26 | provider-protocol-transport | Provider protocol factories gained an injected transport-backed construction seam at slice 109. `ProtocolTransportAdapterFactory` builds Anthropic Messages or OpenAI Responses `provider::System` backends that serialize typed requests, attach provider auth headers, send one HTTP-shaped request through an injected `ProtocolTransport`, map HTTP status classes to provider error categories, and decode successful JSON bodies back into `provider::Response`. `xmake run orangutan` reported slice 109. Ordinary bootstrap still did not read credentials, construct adapters, send network traffic, or start the agent loop for normal prompts. | New `<oran/provider/protocol_transport.hpp>` exports `ProtocolTransport`, `ProtocolHttpRequest`, `ProtocolHttpResponse`, `ProtocolTransportAdapterFactory`, and `protocol_transport_factory_bindings`. The seam is body-response only and forces non-streaming vendor requests; `oran-http` body transport lands in slice 110, while SSE streaming, provider hooks, binary adapter construction, and ordinary `cli::run_async` handoff remain downstream. Focused validation: `test-provider` 63 cases / 512 assertions. Docs: `STATUS.md`, active provider-adapter plan, `ARCHITECTURE.md`, `QUALITY_SCORE.md`, API portability, bootstrap runtime, agent-platform, specs 0001/0014/0017, tool runtime, and this release note reflect the shipped transport seam. | [history](../histories/2026-05/20260526-0302-provider-protocol-transport.md) |
| 2026-05-26 | provider-protocol-response | Provider response decoding now has an offline, provider-owned protocol boundary before HTTP transport. `provider::decode_protocol_response(body_json, target)` turns Anthropic Messages or OpenAI Responses JSON response bytes back into the typed `provider::Response` contract, including text, thinking/reasoning summaries, tool-use blocks, usage counters, model ids, and stop reasons. `xmake run orangutan` now reports slice 108. Ordinary bootstrap still does not read credentials, construct adapters, send network traffic, or start the agent loop for normal prompts. | New `<oran/provider/protocol_response.hpp>` exports `decode_protocol_response`. The decoder keeps `nlohmann_json` private to `oran-provider`, maps unknown vendor stop/status values to `StopReason::error`, and returns parsing errors with non-secret context for malformed JSON, malformed tool inputs, or unsupported response item shapes. Focused validation: `test-provider` 57 cases / 442 assertions. Docs: `STATUS.md`, active provider-adapter plan, `ARCHITECTURE.md`, `QUALITY_SCORE.md`, API portability, agent-platform, specs 0001/0017, and this release note reflect the shipped response decoder. | [history](../histories/2026-05/20260526-0228-provider-protocol-response.md) |
| 2026-05-26 | provider-protocol-request | Provider request serialization now has an offline, provider-owned protocol boundary before HTTP transport. `provider::make_protocol_request(request, target)` turns the typed `provider::Request` / `core::Message` / `core::Content` contract into Anthropic Messages or OpenAI Responses JSON body bytes, including structured tool-result bytes when `ToolResultContent::data_json` is present. `xmake run orangutan` now reports slice 107. Ordinary bootstrap still does not read credentials, construct adapters, send network traffic, or start the agent loop for normal prompts. | New `<oran/provider/protocol_request.hpp>` exports `ProtocolRequest { method, path, body_json }` and `make_protocol_request`. `core::ToolResultContent` now preserves optional `data_json`, and `agent::Loop` copies successful `tool::Output::data_json` into provider-facing tool-result transcript blocks. `oran-provider` uses `nlohmann_json` privately for serialization while public headers continue to expose only bytes and stdlib value types. Focused validation: `test-core` 71 cases / 455 assertions, `test-provider` 51 cases / 398 assertions, and `test-agent` 25 cases / 401 assertions. Docs: `STATUS.md`, active provider-adapter plan, `ARCHITECTURE.md`, `QUALITY_SCORE.md`, API portability, tool/runtime docs, specs 0001/0014/0017, build/dependency docs, and this release note reflect the shipped request mapper. | [history](../histories/2026-05/20260526-0114-provider-protocol-request.md) |
| 2026-05-26 | provider-adapter-factory | Provider adapter construction now has a provider-owned factory dispatch seam after credential resolution. `provider::make_adapter_system(credentials, factories)` matches each resolved credential target to a registered protocol factory, builds one backend per route profile, and returns a profile-routed `provider::System` that expects execution-layer single-target calls. `xmake run orangutan` now reports slice 106. Ordinary bootstrap still does not call the credential resolver or this factory, so startup remains no-secret/no-network until concrete protocol factories and transport exist. | New `<oran/provider/adapter_factory.hpp>` exports `ProtocolAdapterFactory`, `ProtocolAdapterFactoryBinding`, and `make_adapter_system`; `<oran/provider.hpp>` re-exports the seam. The factory validates missing/null/duplicate bindings, duplicate route profiles, null factory outputs, factory errors, unknown routed profiles, and accidental multi-target routed calls as config errors. Focused validation: `test-provider` 45 cases / 329 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `QUALITY_SCORE.md`, API portability, bootstrap runtime, secrets/state, security, spec 0001, spec 0017, and this release note reflect the shipped factory seam. | [history](../histories/2026-05/20260526-0008-provider-adapter-factory.md) |
| 2026-05-25 | provider-credentials | Provider adapter construction now has an explicit credential-resolution boundary. `provider::resolve_adapter_credentials(plan)` reads the API-key environment variables named by an adapter construction plan, returns in-memory credentials beside the matching primary/fallback plan targets, derives the same loop-facing `provider::Route`, and reports missing or empty key env vars as `ErrorKind::auth` without putting secret values in error context. `xmake run orangutan` now reports slice 105. Ordinary bootstrap still does not call this resolver, so startup remains a no-secret/no-network preflight until real adapters exist. | New `<oran/provider/credentials.hpp>` exports `AdapterCredentialTarget`, `AdapterCredentialBundle`, and `resolve_adapter_credentials`. `<oran/provider.hpp>` re-exports the credential surface. Focused validation: `test-provider` 36 cases / 259 assertions covering successful primary/fallback env reads, missing env vars, empty env vars, malformed plan metadata, and route derivation. Docs: `STATUS.md`, `ARCHITECTURE.md`, `QUALITY_SCORE.md`, API portability, bootstrap runtime, secrets/state, security, reliability, specs 0001/0017, and this release note reflect the shipped credential boundary. | [history](../histories/2026-05/20260525-2356-provider-credentials.md) |
| 2026-05-25 | provider-adapter-plan | Provider route startup now reaches the offline adapter construction boundary. `provider::make_adapter_construction_plan(resolution)` turns a resolved route-profile bundle into one primary/fallback plan with an adapter-family name per target, validates required endpoint metadata and `http://` / `https://` base URLs, and still derives the loop-facing `provider::Route`. `xmake run orangutan` now reports slice 104. Real HTTP clients, secret reads, concrete provider adapters, network traffic, and ordinary binary `agent::Loop` handoff remain downstream. | New `<oran/provider/adapter_plan.hpp>` exports `AdapterConstructionTarget`, `AdapterConstructionPlan`, and `make_adapter_construction_plan`. `<oran/provider.hpp>` re-exports the plan surface. `bootstrap::run` now builds the plan for the configured `default` route before CLI handoff while preserving the non-secret startup summary and deterministic no-runner CLI path. Focused validation: `test-provider` 32 cases / 233 assertions and `test-bootstrap` 65 cases / 269 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `QUALITY_SCORE.md`, design docs for API portability and bootstrap runtime, spec 0001, and this release note reflect the shipped adapter-plan seam. | [history](../histories/2026-05/20260525-2215-provider-adapter-plan.md) |
| 2026-05-25 | provider-route-profiles | Provider route resolution now has an adapter-factory-ready companion surface. `provider::resolve_route_profiles(config, route_name)` returns the same resolved primary/fallback `ModelTarget`s as `resolve_route`, plus the profile endpoint metadata (`provider`, `base_url`, `api_key_env`) needed to construct real provider adapters later. `RouteProfileResolution::route()` derives the existing loop-facing `provider::Route`, so `agent::Loop` and `provider::execution::Runtime` keep their current contract. `xmake run orangutan` now reports slice 103. Real adapter construction and ordinary binary `agent::Loop` handoff remain downstream. | New `ResolvedProfileTarget` and `RouteProfileResolution` are exported from `<oran/provider/route_resolver.hpp>` and re-exported by `<oran/provider.hpp>`. Bootstrap now preflights the richer route-profile bundle for the configured `default` route while preserving the same non-secret startup summary; it does not read environment variables, decrypt credentials, construct adapters, or send network traffic. Focused validation: `test-provider` 28 cases / 210 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `QUALITY_SCORE.md`, design docs for API portability and bootstrap runtime, spec 0001, and this release note reflect the shipped resolver companion. | [history](../histories/2026-05/20260525-2134-provider-route-profiles.md) |
| 2026-05-25 | provider-profile-protocol | Provider profile configuration can now keep the operator/vendor label separate from the wire protocol. `profiles.<name>.protocol` is optional; when present, `provider::resolve_route` parses it as an exact `ProtocolKind` spelling and uses it before provider-name aliases. This lets self-hosted gateways or custom vendor labels reuse shipped protocols without pretending to be `openai` or `anthropic`. `xmake run orangutan` now reports slice 102. Real adapter construction and ordinary binary `agent::Loop` handoff remain downstream. | `config::ProfileConfig` adds `std::optional<std::string> protocol`, `oran-config` validates the field as a non-empty string, `config.example.json` documents `anthropic_messages`, and the route resolver now reports unknown explicit protocols with `route` / `profile` / `role` / `protocol` context while preserving the old provider-alias fallback when the field is absent. Focused validation: `test-config` 33 cases / 241 assertions, `test-provider` 26 / 181, and `test-bootstrap` 64 / 264. Docs: `STATUS.md`, `ARCHITECTURE.md`, `QUALITY_SCORE.md`, design docs for API portability, bootstrap runtime, and secrets/state, spec 0001, and this release note reflect the shipped config contract. | [history](../histories/2026-05/20260525-2108-provider-profile-protocol.md) |
| 2026-05-25 | bootstrap-agent-prompt-runner | Bootstrap now has the first real `cli::PromptRunner` owner for tests and future provider adapters. `AgentPromptRunner` borrows a `RuntimeAssembly`, typed config, executor, resolved provider route, and caller-supplied provider backend; wraps the backend in `provider::execution::Runtime`; binds the terminal approval sink; drives `agent::Loop`; and preserves transcript state across prompts. `xmake run orangutan` now reports slice 101. Regular `bootstrap::run` still uses the deterministic no-runner CLI path, so the shipped binary reads no provider credentials, constructs no real adapter, sends no network traffic, and does not start `agent::Loop` for ordinary prompts yet. | New `<oran/bootstrap/prompt_runner.hpp>` exports `AgentPromptRunnerOptions` and `AgentPromptRunner`. The implementation registers the builtin tool catalog, materializes config permissions plus optional agent overlays, applies runtime tool-output caps, threads workspace/audit/broker/hook/trace services into `tool::DispatchContext` and `RunTurnInputs::trace`, and exposes prompt/approval counters for tests. Focused validation: `test-bootstrap` 63 cases / 259 assertions covering CLI-to-loop trace persistence, provider retry through the execution wrapper, broker-backed `file.read` approval through `OperatorPromptSink`, and invalid permission overlay rejection. Docs: `STATUS.md`, `ARCHITECTURE.md`, `BUILD_SYSTEM.md`, design docs, specs 0001/0017/0018, `QUALITY_SCORE.md`, and this release note reflect the shipped runner. | [history](../histories/2026-05/20260525-2022-bootstrap-agent-prompt-runner.md) |
| 2026-05-25 | cli-prompt-runner | CLI prompt parsing now has an async handoff seam for the upcoming agent-loop/binary binding. `cli::run_async(CliOptions, PromptRunner*)` reuses the existing mode parser, delegates one-shot prompts and non-empty scripted REPL lines to a caller-owned runner in order, prints non-empty runner text when not quiet, and propagates runner errors unchanged. `xmake run orangutan` now reports slice 100. Bootstrap still calls the no-runner shell, so no credentials are read, no adapter is constructed, no network traffic is sent, and `agent::Loop` is not started yet. | `<oran/cli/cli.hpp>` now exports `PromptRunRequest`, `PromptRunResult`, `PromptRunner`, and `run_async`; `cli::run` remains the deterministic shell path. Focused validation: `test-cli` 14 cases / 97 assertions covering single-shot runner delegation, scripted REPL ordering, no-runner async shell preservation, and runner error propagation. Docs: `STATUS.md`, `ARCHITECTURE.md`, `QUALITY_SCORE.md`, `cli-runtime.md`, and this release note reflect the shipped seam. | [history](../histories/2026-05/20260525-0744-cli-prompt-runner.md) |
| 2026-05-25 | bootstrap-provider-route-preflight | Startup now catches bad provider route configuration before the CLI prompt shell runs. When config declares routes, `bootstrap::run` resolves the configured `default` route through `provider::resolve_route`, prints the resolved primary/fallback profile/model/protocol summary, and returns the resolver's config error for bad references or provider spellings. Built-in empty defaults still run and report no provider route. `xmake run orangutan` now reports slice 99. Real adapter construction and `agent::Loop` execution remain downstream. | `oran-bootstrap` now depends on `oran-provider` for route preflight only; no credentials are read, no adapter is constructed, and no network traffic is sent. Focused validation: `test-bootstrap` 59 cases / 230 assertions covering successful configured route preflight and invalid provider spelling rejection before CLI handoff. Docs: `STATUS.md`, `ARCHITECTURE.md`, `BUILD_SYSTEM.md`, `bootstrap-runtime.md`, spec 0001, `QUALITY_SCORE.md`, and this release note reflect the handoff preflight. | [history](../histories/2026-05/20260525-0726-bootstrap-provider-route-preflight.md) |
| 2026-05-25 | provider-route-resolver | Provider routes can now be resolved from the typed config surface before the agent-loop/binary handoff. `provider::resolve_route(config, route_name)` maps configured `profiles` / `routes` into the existing `provider::Route` value, preserves fallback order, accepts current provider aliases plus exact `ProtocolKind` spellings, and returns config errors with route/profile/role context for bad references. `xmake run orangutan` now reports slice 98. Wiring the resolved route plus `provider::execution::Runtime` into the loop and binary remains downstream. | New `<oran/provider/route_resolver.hpp>` is re-exported by `<oran/provider.hpp>`, and `oran-provider` now declares its direct `oran-config` dependency. Focused validation: `test-provider` 24 cases / 170 assertions covering primary/fallback mapping, exact protocol spelling, missing route, unknown profile, unknown provider spelling, and empty route name. Docs: `STATUS.md`, `ARCHITECTURE.md`, `BUILD_SYSTEM.md`, `QUALITY_SCORE.md`, `api-portability.md`, and this release note reflect the shipped resolver. | [history](../histories/2026-05/20260525-0709-provider-route-resolver.md) |
| 2026-05-25 | provider-execution-runtime | Provider calls now have a reusable retry/fallback layer before real adapters land. `provider::execution::Runtime` wraps any backend `provider::System`, consumes `Request::retry`, retries retryable network/rate-limit/timeout/upstream errors per target, tries route fallbacks after primary exhaustion, respects cancellation during backoff, suppresses retry/fallback after visible stream output, and fills missing `model_used` so later trace rows can distinguish fallback wins. `xmake run orangutan` now reports slice 97. Wiring this wrapper into the agent-loop/binary runtime remains downstream. | New `<oran/provider/execution.hpp>` exports the execution decorator and `<oran/provider.hpp>` re-exports it. The runtime delegates each concrete attempt with a single-target `Route`, leaving fallback out of future protocol adapters. Focused validation: `test-provider` 18 cases / 132 assertions covering same-target retry, fallback success, provider-supplied model preservation, fatal stop, visible-stream retry suppression, zero-attempt validation, and cancellation during retry backoff. Docs: `STATUS.md`, `ARCHITECTURE.md`, `QUALITY_SCORE.md`, `api-portability.md`, `agent-platform.md`, spec 0017, and this release note reflect the shipped execution layer. | [history](../histories/2026-05/20260525-0622-provider-execution-runtime.md) |
| 2026-05-25 | agent-loop-approval-clock | Fake-provider loop turns can now complete broker-backed `permission_ask_rendered` approvals through direct tool dispatch without inheriting a stale caller timestamp. `agent::Loop` refreshes `DispatchContext::now` for every direct dispatch, restores the reusable context afterward, and `xmake run orangutan` now reports slice 96. Binding the existing CLI operator-prompt sink into the real binary agent-loop runtime remains downstream. | The new `test-agent` case drives a `file.read` ask through `permission::ApprovalBroker` plus a blocking `hook::InProcessSink`, asserts prompt identity/replay/TTL/request time, records `metadata_json.permission_ask_decisions[]`, returns the approved tool result to the provider, and verifies the issued token against the prompt time. Focused validation: `test-agent` 24 cases / 391 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `QUALITY_SCORE.md`, agent/platform and permissions/hooks design docs, specs 0015/0017, and this release note reflect the shipped approval-clock bridge. | [history](../histories/2026-05/20260525-0557-agent-loop-approval-clock.md) |
| 2026-05-25 | cli-operator-prompt-sink | Terminal-facing runtimes now have a concrete approval renderer for `permission_ask_rendered`. `cli::OperatorPromptSink` can be bound to the hook bus, renders the approval payload, accepts yes/approve/proceed or no/deny/reject answers, and returns `operator_approved:<identity>` / `operator_denied:<identity>` through the blocking hook decision trace. `xmake run orangutan` now reports slice 95. Binding this sink into a real CLI agent loop remains part of the later binary handoff. | `oran-cli` now depends on `oran-async` and `oran-hook`, exports `<oran/cli/operator_prompt_sink.hpp>`, and tests scripted approval, denial, identity fallback, invalid scripted answers, and non-ask blocking events. Focused validation: `test-cli` 10 cases / 68 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `BUILD_SYSTEM.md`, `QUALITY_SCORE.md`, CLI/hook/tool design docs, specs 0008/0012/0015/0017, the tech-debt tracker, and this release note reflect the shipped sink. | [history](../histories/2026-05/20260525-0524-cli-operator-prompt-sink.md) |
| 2026-05-25 | permission-ask-dispatch-bridge | Direct tool dispatch can now complete an `ask` permission decision through the blocking hook bus instead of always returning `approval_required`. When a broker and bus are present but no replay token was supplied, dispatch publishes typed `permission_ask_rendered`; a subscribed sink returning `proceed` issues/checks an approval token, optionally hands it back through `DispatchContext::approval_token_output`, records `outcome=approved`, and runs the handler. A `veto` records `outcome=rejected` / `reason=operator_denied`. With no ask sink subscribed, the old `approval_required` behavior is unchanged. `xmake run orangutan` now reports slice 94. The concrete terminal operator-prompt sink remains downstream. | `hook::Payload` adds `PermissionAskRenderedPayload`, `DispatchContext` adds `approval_token_output`, and `Registry::dispatch` serializes ask-sink traces under `metadata_json.permission_ask_decisions[]`. Focused validation: `test-tool` 178 cases / 1838 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `QUALITY_SCORE.md`, design docs for permissions/hooks and tool runtime, specs 0008/0015, the tech-debt tracker, and this release note reflect the shipped dispatch bridge. | [history](../histories/2026-05/20260525-0442-permission-ask-dispatch-bridge.md) |
| 2026-05-25 | hook-publish-audit-rows | Trace inspection now shows which blocking `tool_before` hook sink decided a traced direct tool dispatch. `audit_events` has an `event_kind` discriminator, direct blocking publishes with a `parent_turn_id` write a joinable `event_kind=hook_publish` row before the ordinary permission-decision row, and `orangutan --trace <turn-id>` prints `kind=<event_kind>` on every joined audit row. `xmake run orangutan` now reports slice 93. The first operator-prompt `permission_ask_rendered` sink remains downstream. | Audit DB migration version 4 adds `audit_events.event_kind TEXT NOT NULL DEFAULT 'permission_decision'` plus a parent-turn index for hook rows. Storage, permission, and tool audit types carry `event_kind`; metadata updates match on it so usage enrichment cannot clobber a same-tool `hook_publish` row. `Registry::dispatch` records traced blocking `tool_before` publish metadata (`event`, `sink_id`, `decision_kind`, `reason`, optional `elapsed_ms` / `error`, and `hook_decisions[]`) through the existing audit sink before the permission row. Focused validation: `test-storage` 72 cases / 899 assertions, `test-permission` 89 / 426, `test-tool` 174 / 1769, and `test-bootstrap` 57 / 226. Docs: `STATUS.md`, `ARCHITECTURE.md`, `QUALITY_SCORE.md`, design docs for bootstrap/permissions/storage/tool, specs 0015/0018, the tech-debt tracker, and this release note reflect the shipped hook-publish audit rows. | [history](../histories/2026-05/20260525-0331-hook-publish-audit-rows.md) |
| 2026-05-25 | hook-blocking-timeout-policy | Blocking hook sinks now have a configured deadline instead of being able to stall direct tool dispatch indefinitely. `hooks.timeout_ms` defaults to 2000 in `config.example.json`, `Config::hooks()` exposes the parsed policy, `RuntimeAssembly` owns the process `hook::Bus` with that timeout applied, and the startup banner reports `hook-timeout=<ms>`. A blocking sink that exceeds the deadline is treated as a hook veto: direct dispatch skips the handler, returns `Error::permission_denied`, publishes advisory failure events, and writes audit metadata with `reason=hook_timeout` plus `elapsed_ms`. `xmake run orangutan` now reports slice 92. The first operator-prompt `permission_ask_rendered` sink and spec-0018 `hook_publish` audit rows remain downstream. | `hook::BusOptions` adds `blocking_timeout`, `HookDecisionTrace` gains optional `elapsed`, and `Bus::publish_blocking<E>` races each consulted sink against `async::sleep_for` on the coroutine executor. `oran-config` parses positive-integer `hooks.timeout_ms`; `bootstrap::run` threads it into `RuntimeAssemblyOptions::hook_blocking_timeout`; `RuntimeAssembly` exposes `hook_bus()`. Audit metadata serialization now includes `hook_decisions[].elapsed_ms` when present. Focused validation: `test-config` 32 cases / 235 assertions, `test-hook` 30 / 207, `test-bootstrap` 57 / 224, and `test-tool` 173 / 1739. Docs: `STATUS.md`, `ARCHITECTURE.md`, `QUALITY_SCORE.md`, `SECURITY.md`, `design-docs/{bootstrap-runtime,permissions-and-hooks,secrets-and-state,tool-runtime}.md`, specs 0012/0015, the tech-debt tracker, and this release note reflect the shipped timeout policy. | [history](../histories/2026-05/20260525-0203-hook-blocking-timeout-policy.md) |
| 2026-05-25 | tool-before-blocking-dispatch | Blocking `tool_before` hooks now control direct tool dispatch. A sink can veto a call before any workspace path resolution or permission evaluation, rewrite the effective input before the permission/handler path sees it, or force an otherwise-allowed call through the existing approval broker. Hook vetoes and hook failures return `Error::permission_denied`, skip handlers, publish advisory failure events, and write audit rows with `outcome=blocked_by_hook`; allowed rewrites write `outcome=rewritten` and preserve both original and rewritten input hashes in audit metadata. `xmake run orangutan` now reports slice 91. Timeout enforcement, the first operator-prompt `permission_ask_rendered` sink, and spec-0018 `hook_publish` audit rows remain downstream. | `HookDecision` now carries `trace` entries for every consulted sink, and `Bus::publish_blocking<E>` fills them in subscription order. `Registry::dispatch` consumes `publish_blocking<Event::tool_before>` before workspace resolution, permission evaluation, broker checks, audit recording, handler execution, and later hook payloads. `permission::AuditOutcome` adds `blocked_by_hook` and `rewritten`; audit metadata helpers serialize `hook_decisions`, `original_input_hash`, and `rewritten_input_hash`; storage audit docs include the new wire outcomes. `bench-hook` adds blocking publish scenarios for no sinks, one sink, three all-proceed sinks, and a second-sink short-circuit. Focused validation: `test-hook` 29 cases / 196 assertions, `test-permission` 89 / 426, and `test-tool` 172 / 1722. Docs: `STATUS.md`, `ARCHITECTURE.md`, `QUALITY_SCORE.md`, `SECURITY.md`, `design-docs/{permissions-and-hooks,tool-runtime}.md`, specs 0012/0015/0017, the tech-debt tracker, and this release note reflect the shipped dispatch consumer. | [history](../histories/2026-05/20260525-0045-tool-before-blocking-dispatch.md) |
| 2026-05-24 | hook-publish-blocking-foundation | Spec 0015 v1's blocking-hook surface ships at the `oran-hook` bus. `Bus::publish_blocking<E>` lets `tool_before` / `permission_ask_rendered` / `memory_write_before` sinks return `HookDecision { kind, reason, optional<string> rewritten_input_json, optional<core::Time> approval_expires_at }`; the bus walks subscribed sinks in subscription order, short-circuits at the first non-`proceed` decision, applies the same `ToolAfterPayload::data_json` redaction the advisory path uses, and converts sink errors / thrown exceptions into a veto with `reason="hook_error: <message> [sink=<id>]"`. `EventTraits<E>` + the `HasBlockingDecision<E>` concept enforce the v1 whitelist at compile time: `publish_blocking<Event::tool_after>` fails the `requires` clause. `xmake run orangutan` now reports slice 90. The slice deliberately stops at the bus surface — `Registry::dispatch` consumption (new `permission::AuditOutcome::blocked_by_hook` / `rewritten` enumerators, the seven-step canonical dispatch order, the `AuditEvent::context.hook_decisions` array), `config.hooks.timeout_ms` enforcement, the first operator-prompt sink owned by `oran-cli`, and the spec-0018 AC5 `hook_publish` audit-row writer all remain downstream as the next slices in sequence. | `include/oran/hook/decision.hpp` (new) carries `HookDecisionKind` and `HookDecision`. `include/oran/hook/event_traits.hpp` (new) carries the empty primary `EventTraits<E>` template, the three v1 specialisations, and `HasBlockingDecision<E>`. `include/oran/hook/sink.hpp` adds a virtual `handle_blocking` defaulting to `proceed` (body in the new `src/oran-hook/sink.cpp` so the coroutine machinery stays out of the public header); `include/oran/hook/in_process_sink.hpp` adds an optional `BlockingCallback` installed via `set_blocking_handler`. `include/oran/hook/bus.hpp` adds the constrained `publish_blocking<E>` template that delegates to a private `publish_blocking_impl(Event, Payload)` in `src/oran-hook/bus.cpp`. `include/oran/hook.hpp` re-exports the new headers. `tests/hook/test_publish_blocking.cpp` (new) covers proceed/veto/rewrite/require_approval, multi-sink short-circuit + consult-next, all-proceed continuation, error and exception classification, `InProcessSink::set_blocking_handler`, and `STATIC_REQUIRE`/`STATIC_REQUIRE_FALSE` over the blocking whitelist. `test-hook` now reports 29 cases / 172 assertions (was 17 / 109). The tech-debt tracker's 2026-05-18 hook row narrows to the dispatch consumer + audit enumerators + first operator-prompt sink. Docs: `STATUS.md`, `ARCHITECTURE.md`, `QUALITY_SCORE.md`, `design-docs/permissions-and-hooks.md`, spec 0015 (v1 status block + per-AC pointers), `exec-plans/tech-debt-tracker.md`, and this release note reflect the shipped foundation. | [history](../histories/2026-05/20260524-1700-hook-publish-blocking-foundation.md) |
| 2026-05-24 | bench-trace-turn-insert | Operators now have measured evidence that spec-0018's per-turn trace insert fits inside its ≤ 50 µs budget. `bench/storage/scenarios/trace_turn_insert.cpp` adds a single-insert A-vs-B pair against `trace_turns`: `storage.trace_turn_insert_raw_pool` runs one raw `Pool` + `StatementCache` INSERT per nanobench iteration, and `storage.trace_turn_insert_repository` runs one `TraceRepository::append_turn` per iteration. Initial WSL2 numbers report about 13 µs / insert for the raw path and about 16 µs / insert for the repository wrapper. The existing batch-based `scenarios/trace_repository.cpp` needed an `id_for` collision fix in the same slice (the original overlapping-sum encoding produced identical bytes for distinct `(batch, row)` tuples and tripped the trace `PRIMARY KEY` guard on the second nanobench epoch); both batch scenarios now run alongside the new single-insert pair. `xmake run orangutan` now reports slice 89. Hook publish rows (AC5) and binary handoff that drives `agent::Loop` from inside the binary remain downstream. | New `bench/storage/scenarios/trace_turn_insert.cpp` registers two single-insert scenarios that each open their own temp DB, run the embedded trace migrations, and insert unique per-iteration turn ids (iter-counter packed into the trailing 8 bytes of the 16-byte BLOB) so nanobench can advance indefinitely. `bench/storage/main.cpp` forwards-declares and registers the new pair, and `bench/storage/README.md` documents the new scenario row alongside the batch trace_repository row. `bench/storage/scenarios/trace_repository.cpp` swaps the buggy overlapping-sum `id_for` for a `(salt, row, batch)` non-overlapping byte layout. `test-storage` still reports 72 cases / 886 assertions; the change is bench-only. Docs: `STATUS.md`, spec 0018 (AC12 status block), and this release note reflect the shipped per-insert bench and the adjacent bug fix. | [history](../histories/2026-05/20260524-1600-bench-trace-turn-insert.md) |
| 2026-05-24 | bootstrap-trace-inspector | Operators can now read spec-0018 trace evidence directly from the CLI. `orangutan --trace <turn-id>` (32-char lowercase hex) opens the workspace audit DB, runs the idempotent migration, looks up the `trace_turns` row, joins audit rows ordered by their original dispatch sequence, and renders both in `--explain-rules`-style lines before exiting `0`. A missing audit DB or unknown turn id exits non-zero with `Error::not_found`; SIGINT/SIGTERM follow the existing `128 + signum` exit-code contract. `xmake run orangutan` now reports slice 88. Hook publish rows, the bench `trace_turn_insert` scenario, and binary handoff that drives `agent::Loop` from inside the binary remain downstream. | `oran-storage` adds `AuditRepository::list_events_for_turn(TurnId, limit)` — an operator-level read scoped by `parent_turn_id` only and ordered `id ASC` so the original `tool_use` order from spec-0017 multi-tool turns survives the join. `oran-bootstrap` adds `--trace <turn-id>` / `--trace=<turn-id>` argument parsing plus a private `run_trace_inspect()` shaped like `run_audit_init` (single-reader `Pool`, idempotent migration, parsed turn id, `TraceRepository::get_turn` + the new audit method, rendered output). `tests/storage/test_audit_repository.cpp` adds dispatch-order, cross-scope, and validation coverage for the new query; `tests/bootstrap/test_bootstrap.cpp` adds parsing-rejection (missing/empty/wrong-length/uppercase/non-hex/all-zero), missing-DB, missing-turn, and happy-path cases (both `--trace <hex>` and `--trace=<hex>`). `test-storage` reports 72 cases / 886 assertions; `test-bootstrap` reports 56 cases / 221 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, spec 0018 (status block + AC10), `QUALITY_SCORE.md`, and this release note reflect the shipped inspector. | [history](../histories/2026-05/20260524-1500-bootstrap-trace-inspector.md) |
| 2026-05-24 | bootstrap-trace-wiring | Operators can now toggle spec-0018's trace surface from the JSON config and have bootstrap apply the policy end-to-end. `bootstrap::run` reads `config.trace().enabled` and, when both audit and trace are enabled, `RuntimeAssembly::build` constructs a `storage::TraceRepository` on the shared audit `Pool`. The startup banner now reports `trace=enabled|disabled`, and the assembly exposes the repository pointer so the upcoming agent-loop owner can plug it into `agent::TraceContext`. `xmake run orangutan` now reports slice 87. Hook publish rows, the CLI `--trace` inspector, and threading the assembly-owned repository into `RunTurnInputs::trace` remain downstream. | `include/oran/bootstrap/runtime_assembly.hpp` adds a forward declaration for `storage::TraceRepository`, the `RuntimeAssemblyOptions::trace_enabled` boolean (default true), and the new `trace_repository()` / `trace_enabled()` accessors. `src/oran-bootstrap/runtime_assembly.cpp` stores the repository in `Impl` and only constructs it when audit is enabled and the option is true. `src/oran-bootstrap/bootstrap.cpp` wires `config.trace().enabled` through the option and extends the runtime-assembly banner. `tests/bootstrap/test_runtime_assembly.cpp` adds three trace cases (default-on with a smoke `append_turn`, explicit trace-off, and audit-disabled forces trace off); `test-bootstrap` reports 51 cases / 188 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, spec 0018 (status block + AC9), `QUALITY_SCORE.md`, and this release note reflect the shipped wiring. | [history](../histories/2026-05/20260524-1400-bootstrap-trace-wiring.md) |
| 2026-05-24 | agent-iteration-cap-trace-rows | Trace-enabled fake-provider loop turns that exhaust `LoopOptions::max_iterations` now leave durable spec-0018 evidence. When the cap fires because the model keeps returning tool_use, the loop writes one `trace_turns` row with `stop_reason=error`, `iteration_count = max_iterations`, the final iteration's prompt hashes/bytes, the aggregated provider usage, and the last response's model id (falling back to the primary route model) before returning the existing `Error::internal` with `reason=iteration_cap`. `xmake run orangutan` now reports slice 86. Config-to-loop wiring, hook publish rows, CLI `--trace`, and binary handoff remain downstream. | `agent::Loop` now tracks the last successful rendered prompt and response model across iterations so the iteration-cap branch can reuse the existing body-free error trace writer. The returned error shape is unchanged, so callers still see `reason=iteration_cap` / `max_iterations`, and trace-disabled or repository-less callers keep the legacy no-row path. `tests/agent/test_loop.cpp` adds a storage-backed iteration-cap turn that asserts the error context and the trace row's stop reason, iteration count, model id, and aggregated usage; `test-agent` reports 23 cases / 363 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, spec 0018, `QUALITY_SCORE.md`, and this release note reflect the shipped iteration-cap writer. | [history](../histories/2026-05/20260524-1340-agent-iteration-cap-trace-rows.md) |
| 2026-05-24 | agent-trace-turn-id-generation | Trace-enabled fake-provider loop turns no longer require callers to pre-fill a turn id. When `RunTurnInputs::trace` has an enabled `TraceRepository` and `RunTurnInputs::turn_id` is unset, the loop generates a non-zero 16-byte turn id before prompt rendering, writes it to `trace_turns.turn_id`, and stamps direct tool audit rows with the same `audit_events.parent_turn_id`. `xmake run orangutan` now reports slice 85. Config-to-loop wiring, hook rows, CLI `--trace`, iteration-cap rows, and binary handoff remain downstream. | `agent::Loop` now owns the first local turn-id generator for configured trace writers and keeps trace-disabled / repository-less callers on the previous NULL-parent audit shape unless they explicitly supply a turn id. `tests/agent/test_loop.cpp` adds a storage-backed generated-id tool turn asserting one non-zero trace row and one matching audit row; `test-agent` reports 22 cases / 345 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `BUILD_SYSTEM.md`, design docs for agent/tool/storage, specs 0017/0018, `QUALITY_SCORE.md`, and this release note reflect the generated-id path. | [history](../histories/2026-05/20260524-1325-agent-trace-turn-id-generation.md) |
| 2026-05-24 | agent-error-trace-rows | Trace-enabled fake-provider loop failures now leave durable spec-0018 error evidence. Non-cancelled provider errors write one `trace_turns` row with `stop_reason=error` before the original error is returned unchanged. Response-backed loop-boundary failures, including `tool_use` responses without dispatch services and unsupported non-terminal stop reasons, also write `stop_reason=error` rows with the provider response model and observed usage. `xmake run orangutan` now reports slice 84. Iteration-cap trace rows, hook rows, CLI `--trace`, config-to-loop wiring, and binary handoff remain downstream. | `agent::Loop` now routes ordinary non-cancelled error exits through the same body-free trace writer used by terminal-success and cancellation rows, while avoiding extra awaits on active parent-cancellation paths. `tests/agent/test_loop.cpp` adds provider-error and loop-boundary-error trace-row cases; `test-agent` reports 21 cases / 328 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `BUILD_SYSTEM.md`, `design-docs/agent-platform.md`, specs 0017/0018, `QUALITY_SCORE.md`, and this release note reflect the shipped error trace rows. | [history](../histories/2026-05/20260524-1310-agent-error-trace-rows.md) |
| 2026-05-24 | agent-cancellation-trace-rows | Parent-cancelled fake-provider loop turns now leave durable spec-0018 trace evidence. When trace is enabled, cancellation during the provider await writes one `trace_turns` row with `stop_reason=cancelled` / `cancellation_phase=provider`; cancellation during direct tool dispatch writes the same stop reason with `cancellation_phase=tools` and the provider usage observed before the tool wait. The returned error still remains `ErrorKind::cancelled` with `reason=parent_cancelled`. `xmake run orangutan` now reports slice 83. Ordinary provider/internal error trace rows, hook rows, CLI `--trace`, config-to-loop wiring, and binary handoff remain downstream. | `agent::Loop` now shields only the trace insert from the already-fired parent cancellation state before appending cancelled rows through `TraceRepository`. Provider-phase rows use the primary route model because no provider response exists; tool-phase rows use the response model and aggregate usage. `tests/agent/test_loop.cpp` adds provider/tool cancellation trace-row cases; `test-agent` reports 19 cases / 290 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `BUILD_SYSTEM.md`, `design-docs/agent-platform.md`, specs 0017/0018, `QUALITY_SCORE.md`, and this release note reflect the shipped cancellation trace rows. | [history](../histories/2026-05/20260524-1255-agent-cancellation-trace-rows.md) |
| 2026-05-24 | agent-trace-disable-policy | Trace-disabled fake-provider loop turns now preserve the trace-off audit shape at the loop boundary. Callers can set `RunTurnInputs::trace.enabled=false` to write zero `trace_turns` rows, keep direct-dispatch audit `parent_turn_id` NULL even when a turn id is present, and restore any reusable dispatch-context parent id after the tool call. `xmake run orangutan` now reports slice 82. Bootstrap still does not map `config::TraceConfig` into loop inputs; cancellation/error trace rows, hook rows, CLI `--trace`, and binary handoff remain downstream. | `agent::TraceContext` gained an `enabled` switch that defaults true. `agent::Loop` gates the terminal-success trace writer and direct-dispatch parent id stamping through that switch while preserving existing trace-enabled behavior. `tests/agent/test_loop.cpp` adds a storage-backed trace-disabled single-tool turn that asserts zero trace rows, NULL audit parent ids, and context restoration; `test-agent` reports 17 cases / 247 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/agent-platform.md`, specs 0017/0018, `QUALITY_SCORE.md`, and this release note reflect the shipped loop-side disabled policy. | [history](../histories/2026-05/20260524-1240-agent-trace-disable-policy.md) |
| 2026-05-24 | trace-config-policy | Operators now have a typed config surface for spec-0018 trace policy. `config.example.json` documents the top-level `trace` block, and `Config::trace()` exposes `enabled`, `store_raw_bodies`, and `retention_days` with defaults `{true, false, 30}`. `xmake run orangutan` now reports slice 81. Bootstrap and `agent::Loop` do not consume this policy yet; trace-enabled loop tests still configure `RunTurnInputs::trace` directly. | `config::TraceConfig` was added to `<oran/config.hpp>` and parsed from top-level JSON with config-time validation: `enabled` and `store_raw_bodies` must be booleans, and `retention_days` must be a positive integer. `tests/config/test_config.cpp` covers explicit policy, malformed policy, and the example file; `test-config` reports 30 cases / 225 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/secrets-and-state.md`, spec 0018, `QUALITY_SCORE.md`, and this release note reflect the shipped typed policy surface. | [history](../histories/2026-05/20260524-1225-trace-config-policy.md) |
| 2026-05-24 | agent-loop-trace-row | Trace-enabled fake-provider loop turns now write the first durable spec-0018 turn row. Callers that provide `RunTurnInputs::turn_id` plus `RunTurnInputs::trace` get one body-free `trace_turns` row before terminal-success responses return, and direct tool-dispatch audit rows already carry the same turn id for storage-side joins. `xmake run orangutan` now reports slice 80. Cancellation/error trace rows, turn-id generation, hook publish rows, trace config, CLI `--trace`, and binary handoff remain future work. | `agent::TraceContext` carries a non-owning `storage::TraceRepository*`, session id, optional parent turn id, agent key, origin, and redacted context JSON. `oran-agent` now depends downward on `oran-storage`; `agent::Loop` converts rendered prompt hashes/bytes, active/deferred catalog hashes, aggregate provider usage, route profile/model, stop reason, iteration count, and timestamps into `AppendTraceTurnRequest`. `tests/agent/test_loop.cpp` adds a single-text trace row case and a storage-backed single-tool join case (`trace_turns.turn_id = audit_events.parent_turn_id`); `test-agent` reports 16 cases / 233 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `BUILD_SYSTEM.md`, `design-docs/{agent-platform,storage-runtime}.md`, specs 0017/0018, `QUALITY_SCORE.md`, and this release note reflect the shipped terminal-success trace writer. | [history](../histories/2026-05/20260524-1210-agent-loop-trace-row.md) |
| 2026-05-24 | trace-audit-parent-turn-id | Tool permission audit rows can now carry the typed parent turn id that will join them to spec-0018 trace rows. Direct fake-provider loop turns that supply `RunTurnInputs::turn_id` stamp every dispatched tool row with that id; trace-disabled turns clear the field for the dispatch duration so audit rows keep `parent_turn_id = NULL`. `xmake run orangutan` now reports slice 79. Loop-owned `trace_turns` writes, hook publish rows, trace config, and `--trace` inspection remain future work. | New `core::TurnId` provides the shared 16-byte value shape used by `storage::TraceId` and audit correlation. Audit DB migration `0003-audit-parent-turn-id.sql` adds nullable `audit_events.parent_turn_id` plus an index, and `AuditRepository`, `permission::AuditEvent`, `AuditMetadataUpdate`, `StorageAuditSink`, and `Registry::dispatch` propagate it through append and metadata-enrichment paths. `agent::Loop` temporarily installs `RunTurnInputs::turn_id` onto the reusable `DispatchContext` for each direct dispatch and restores the previous context afterward. Focused tests cover value-type validation, version-3 migration, BLOB round-trip, parent-turn-scoped metadata updates, storage/recording sink propagation, registry audit stamping, and direct-loop stale-context clearing/restoration. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/{agent-platform,permissions-and-hooks,storage-runtime,tool-runtime}.md`, specs 0017/0018, `QUALITY_SCORE.md`, and this release note reflect the shipped audit join side. | [history](../histories/2026-05/20260524-1145-trace-audit-parent-turn-id.md) |
| 2026-05-24 | storage-trace-repository | The storage layer now has the durable per-turn trace table required by spec 0018. `oran-storage` can migrate and append/get/list/count redacted `trace_turns` rows keyed by 16-byte BLOB turn/session ids, with prompt/cache hashes, usage rollups, cost, optional cancellation phase, and opaque context bytes. `xmake run orangutan` now reports slice 78. Agent-loop writes, audit parent-turn joins, hook publish rows, trace config, and `--trace` inspection remain future work. | New `include/oran/storage/trace_repository.hpp`, `src/oran-storage/trace_repository.cpp`, and `src/oran-storage/migrations/audit/0002-trace-turns-initial.sql` add `TraceRepository`, `TraceId`, request/record/list types, and the embedded `built_in_trace_migrations()` asset. Trace is audit DB migration version 2, so `AuditRepository::migrate()` provisions both `audit_events` and `trace_turns`, and `TraceRepository::migrate()` upgrades existing audit version-1 databases in place; bootstrap `--audit-init` / `RuntimeAssembly::build` now provision the same version-2 audit DB. `Statement` gains `bind_blob` / `column_blob` so IDs match the spec BLOB contract. `tests/storage/test_trace_repository.cpp` covers migration, explicit migration dirs, audit-v1-to-trace-v2 upgrade, round-trip, list filters/order, missing rows, and validation; `test_sqlite.cpp` covers BLOB round-trip; `test-storage` reports 68 cases / 827 assertions. `test-bootstrap` reports 48 cases / 173 assertions with trace-table provisioning checks, and `bench-storage` gains a raw SQL vs. typed repository trace insert A/B. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/{storage-runtime,permissions-and-hooks}.md`, spec 0018, `QUALITY_SCORE.md`, and this release note reflect the shipped storage foundation. | [history](../histories/2026-05/20260524-1125-storage-trace-repository.md) |
| 2026-05-24 | agent-loop-cancellation-phase | The fake-provider agent loop now reports where a parent cancellation landed. Provider-await cancellations return `ErrorKind::cancelled` with `reason=parent_cancelled` and `cancellation_phase=provider`; direct tool-dispatch cancellations return the same reason with `cancellation_phase=tools`. `xmake run orangutan` now reports slice 77. Turn-level trace/audit rows, blocking approval rendering, scheduler handoff, provider retry/fallback, and CLI/binary handoff remain future work. | `src/oran-agent/loop.cpp` wraps only cancellation errors from `provider::System::send` and `tool::Registry::dispatch`, leaving ordinary provider failures and model-repairable tool errors unchanged. `tests/agent/test_loop.cpp` adds two parent-cancellation cases that bind an `asio::cancellation_signal` over the loop and verify provider/tool phase context; `test-agent` reports 14 cases / 154 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `BUILD_SYSTEM.md`, `design-docs/agent-platform.md`, specs 0017/0018, `QUALITY_SCORE.md`, and this release note reflect the shipped cancellation phase source. | [history](../histories/2026-05/20260524-1110-agent-loop-cancellation-phase.md) |
| 2026-05-24 | agent-loop-tool-dispatch | The fake-provider agent loop can now execute the first real tool-using turn offline. When supplied the existing `tool::Registry` and `tool::DispatchContext`, `agent::Loop` dispatches `tool_use` blocks sequentially, appends ordered `tool_result` blocks, rebuilds the prompt, and re-enters the provider until a terminal answer or iteration cap. `xmake run orangutan` now reports slice 76. Turn-level audit rows, cancellation-specific tool-dispatch coverage, blocking approval rendering, scheduler handoff, provider retry/fallback, and CLI/binary handoff remain future work. | `RunTurnInputs` gains optional non-owning `tool::Registry*` / `tool::DispatchContext*`, and `RunTurnResult` now returns the complete transcript tail. `src/oran-agent/loop.cpp` now rebuilds the prompt per iteration from the updated transcript, aggregates provider usage, converts model-repairable tool errors into error `ToolResultContent` blocks, propagates cancellation/storage/internal dispatch failures, and enforces `LoopOptions::max_iterations` with `reason=iteration_cap`. `tests/agent/test_loop.cpp` adds five cases covering one-tool provider re-entry, ordered multi-tool results, missing-tool repair, infrastructure-error propagation, and iteration-cap termination; `test-agent` reports 12 cases / 140 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `BUILD_SYSTEM.md`, `design-docs/agent-platform.md`, spec 0017, `QUALITY_SCORE.md`, and this release note reflect the shipped sequential tool loop. | [history](../histories/2026-05/20260524-1050-agent-loop-tool-dispatch.md) |
| 2026-05-24 | agent-loop-text-turn | The agent runtime now has its first real `agent::Loop` entry point over `provider::System`. It can drive a fake-provider text-only turn offline, returning assembled assistant text plus prompt/cache/usage metadata while loudly rejecting tool-use responses until the next multi-iteration slice. `xmake run orangutan` now reports slice 75. Tool dispatch, turn audit rows, cancellation scenarios, and CLI/binary handoff remain future work. | New `include/oran/agent/loop.hpp` and `src/oran-agent/loop.cpp` expose `LoopOptions`, `RunTurnInputs`, `RunTurnResult`, and move-only `agent::Loop`. The implementation owns a `prompt::Builder`, renders the seven prompt sections from caller-provided stable inputs and conversation tail, maps prefix cache hints through `provider::make_prompt_cache_hints`, mirrors active/promoted tools into name-sorted `provider::Request::tools`, sends exactly one request through the supplied `provider::System` / resolved `provider::Route`, forwards provider errors unchanged, and returns `Error::internal` for `StopReason::tool_use` or any `ToolUseContent` block so the first slice cannot be confused with complete ReAct dispatch. `oran-agent` now depends on `oran-async` and `oran-provider`; `tests/agent/test_loop.cpp` adds four cases covering FakeProvider text turn, request mapping, provider error forwarding, and the not-yet-implemented tool boundary; `test-agent` reports 7 cases / 64 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `BUILD_SYSTEM.md`, `design-docs/agent-platform.md`, spec 0017, `QUALITY_SCORE.md`, and this release note reflect the shipped text-turn loop. | [history](../histories/2026-05/20260524-1015-agent-loop-text-turn.md) |
| 2026-05-24 | provider-fake-foundation | Spec 0017's provider prework is now complete. `oran-provider` ships the abstract `provider::System` plus the streaming `provider::EventSink`, the `ProtocolKind` / `ModelTarget` / `Route` value shapes the agent loop will resolve once per turn, and the first concrete `provider::FakeProvider` — a scripted-turn fixture with cancel-aware latency that the upcoming `agent::Loop` will drive through the spec-0017 ten-scenario matrix without touching a real vendor. `xmake run orangutan` now reports slice 74. The real Anthropic/OpenAI adapters and the agent loop itself remain future work. | New `include/oran/provider/system.hpp` exposes `ProtocolKind`, `ModelTarget`, `Route`, the non-virtual-no-op `EventSink`, and the abstract `System::send(Request, Route, EventSink*) const`. New `include/oran/provider/fake.hpp` + `src/oran-provider/fake.cpp` add the `StreamDelta` variant (`TextDelta`, `ThinkingDelta`, `ToolStart`, `ToolInputDelta`, `StreamEnd`), the `ScriptedTurn` value type, and `FakeProvider` — a pimpl `System` that walks the plan one turn at a time, opens/extends `TextContent`/`ThinkingContent`/`ToolUseContent` blocks from the deltas, fans the same deltas through the supplied sink, awaits scripted latency via `async::sleep_for` for cancel-aware tests, serialises concurrent `send` calls behind a `mutable std::mutex`, and returns `Error::internal` on plan exhaustion or empty-body turns. `xmake/targets.lua` adds `oran-async` to `oran-provider`'s dep set. `tests/provider/test_fake.cpp` adds eight cases covering canned-response replay, delta assembly with multi-block transitions, scripted error injection, plan exhaustion, empty-turn rejection, multi-turn drive, null-sink tolerance, and parent cancellation during scripted latency; `test-provider` reports 11 cases / 89 assertions. The slice does not add a new bench scenario — fake-provider perf is intentionally deferred until the agent loop produces a realistic workload measurement. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/{api-portability,agent-platform}.md`, spec 0017, `QUALITY_SCORE.md`, and this release note reflect the shipped provider surface. | [history](../histories/2026-05/20260524-1000-provider-fake-foundation.md) |
| 2026-05-24 | provider-cache-hints | Provider adapters now have a typed cache-hint contract before the fake-provider loop lands. `oran-provider` maps a `prompt::RenderedPrompt` prefix into adapter-facing `(id, content_hash, cache_version)` keys, rejects malformed cache breakpoints, and leaves the conversation tail uncached. `xmake run orangutan` now reports slice 73. FakeProvider, transport, retry/fallback execution, and real vendor cache-control mapping remain future work. | New `oran-provider` target with `<oran/provider.hpp>`, `<oran/provider/cache.hpp>`, and `<oran/provider/types.hpp>` exposes provider-domain `Request` / `Response` values plus `PromptCacheHints` / `PromptCacheOptions`. `src/oran-provider/cache.cpp` validates the seven-section prompt shape, single section-6 breakpoint, and prefix byte count before producing cache hints or skipping disabled/small-prefix routes. `xmake/targets.lua`, `xmake/tests.lua`, `xmake/bench.lua`, and `scripts/check-deps.sh` register the target and its intentional `provider -> prompt` same-layer dependency. `test-provider` reports 3 cases / 25 assertions; `bench-provider` reports `provider.cache_hints_enabled` about 394 ns / mapping and `provider.cache_hints_disabled` about 317 ns / mapping. Docs: `STATUS.md`, `ARCHITECTURE.md`, `BUILD_SYSTEM.md`, `design-docs/{api-portability,agent-platform,tool-runtime,module-boundaries}.md`, specs 0010/0014/0016/0017, `rules/testing-and-bench.md`, `QUALITY_SCORE.md`, and this release note reflect the shipped provider cache surface. | [history](../histories/2026-05/20260524-0740-provider-cache-hints.md) |
| 2026-05-24 | agent-session-promotion | Deferred-tool discovery now has its first agent-owned session state. `agent::SessionState` observes successful `tool.search` results, promotes deferred matches into the next prompt snapshot, and keeps malformed successful structured output from partially mutating state. `xmake run orangutan` now reports slice 72. The full ReAct loop and provider cache mapping remain future work. | New `oran-agent` target with `<oran/agent.hpp>` and `<oran/agent/session_state.hpp>` exposes `ToolPromotionReport` and move-only `SessionState`. `src/oran-agent/session_state.cpp` parses `tool.search` `Output::data_json` privately with `nlohmann_json`, validates `kind=="tool_search"` plus `matches[]`, and promotes only deferred matches by name. `xmake/targets.lua`, `xmake/tests.lua`, and `xmake/bench.lua` register `oran-agent`, `test-agent`, and `bench-agent`; `test-agent` reports 3 cases / 23 assertions; `bench-agent` reports `agent.prompt_cache_no_promotions` about 54.4 us / fixture and `agent.prompt_cache_after_promotion` about 63.1 us / fixture while aborting on prompt-prefix hash drift. Docs: `STATUS.md`, `ARCHITECTURE.md`, `BUILD_SYSTEM.md`, `design-docs/{agent-platform,tool-runtime}.md`, specs 0016/0017, `rules/{libraries,prompt-design}.md`, `QUALITY_SCORE.md`, and this release note reflect the shipped session owner. | [history](../histories/2026-05/20260524-0725-agent-session-promotion.md) |
| 2026-05-24 | prompt-promotion-state | Deferred-tool discoveries can now be represented as bounded session state before the first agent loop lands. `prompt::PromotionState` keeps promoted tool names in a 16-entry, 24-hour LRU/TTL set, and `prompt::Builder` can consume a sorted promotion snapshot so the next prompt moves those tools into the full-schema active catalog. `xmake run orangutan` now reports slice 71. `tool.search` still needs an `oran-agent` session owner to call `promote` after a lookup. | New `<oran/prompt/promotion_state.hpp>` plus `src/oran-prompt/promotion_state.cpp` add a move-only promotion state with explicit `core::Time` inputs, lifetime stats, validation for empty names, LRU eviction, TTL reaping, and deterministic sorted snapshots. `BuilderInputs` gains `promoted_tools`, and `src/oran-prompt/builder.cpp` treats those names as active for section 2 without validating stale/missing snapshot entries against the catalog. `test-prompt` reports 10 cases / 98 assertions; `bench-prompt` now covers default, explicit, promoted, and promotion-state snapshot scenarios. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/{agent-platform,tool-runtime}.md`, specs 0012/0016/0017, `QUALITY_SCORE.md`, and this release note reflect the shipped prompt-side promotion state. | [history](../histories/2026-05/20260524-0715-prompt-promotion-state.md) |
| 2026-05-24 | prompt-builder-skeleton | The runtime now has its first prompt assembly library. `prompt::Builder` produces seven ordered cache sections, active/deferred tool catalog text from `runtime.prompt.active_tools`, stable section hashes, a prefix hash/byte count for sections 1-6, and exactly one cache breakpoint before the conversation tail. `xmake run orangutan` now reports slice 70. Session promotion and the fake-provider agent loop remain future spec-0016/0017 work. | New `oran-prompt` target with `<oran/prompt/builder.hpp>` / `<oran/prompt.hpp>` plus `src/oran-prompt/builder.cpp`. The builder consumes `config::PromptActiveToolsConfig`, validates explicit tool names against the catalog snapshot, delegates catalog bytes to `tool::CatalogRenderer`, and renders conversation-tail messages deterministically outside the cached prefix. `xmake/targets.lua`, `xmake/tests.lua`, `xmake/bench.lua`, and `scripts/check-deps.sh` register the new library and the intentional `prompt -> tool` same-layer dependency. `test-prompt` reports 6 cases / 71 assertions; `bench-prompt` compares default active-set assembly (~4.4 µs) with an explicit two-tool subset (~3.2 µs). Docs: `STATUS.md`, `ARCHITECTURE.md`, `BUILD_SYSTEM.md`, `design-docs/{agent-platform,tool-runtime,api-portability}.md`, specs 0016/0017, `rules/prompt-design.md`, `QUALITY_SCORE.md`, and this release note reflect the shipped prompt surface. | [history](../histories/2026-05/20260524-0705-prompt-builder-skeleton.md) |
| 2026-05-24 | prompt-active-tools-config | Operators can now declare the prompt active-tool policy in `config.example.json` and real config files before the prompt builder lands. `runtime.prompt.active_tools` accepts `"defaults"` for the documented default active set or an explicit array of tool names for future section-2 rendering. `xmake run orangutan` now reports slice 69. Prompt-builder consumption and session promotion remain future spec-0016 work. | `include/oran/config/config.hpp` adds `PromptActiveToolsConfig` / `PromptRuntimeConfig`; `src/oran-config/config.cpp` parses and validates `"defaults"` or an explicit allowlist, preserving author order and rejecting empty names without resolving against `tool::Registry`; `config.example.json` documents the default sentinel; `tests/config/test_config.cpp` covers defaults, explicit and empty allowlists, malformed prompt blocks, wrong active-tool shapes, non-string names, and empty names; and the config loading bench fixture now exercises the prompt block. `test-config` reports 28 cases / 207 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/{agent-platform,secrets-and-state,tool-runtime}.md`, spec 0016, `rules/prompt-design.md`, `QUALITY_SCORE.md`, and this release note reflect the shipped config surface. | [history](../histories/2026-05/20260524-0655-prompt-active-tools-config.md) |
| 2026-05-24 | tool-search-registry-lookup | Agents and future prompt builders now have a callable, non-deferred `tool.search` primitive for discovering registered tool metadata by exact name, category, and/or required capability. The tool returns a text fallback plus structured `data_json` containing the query, match count, and matched tool definitions. `xmake run orangutan` now reports slice 68. Session promotion remains future spec-0016 work because `oran-agent::SessionState` does not exist yet. | `include/oran/tool/builtins.hpp` adds `kToolSearchName` and `register_tool_search`; `src/oran-tool/tool_search.cpp` implements the capability-free lookup over `Registry::catalog()`; `register_builtins` now includes `tool.search`; `Registry::dispatch` exposes the current dispatching registry to handlers without self-capturing a movable registry; and `tests/tool/test_registry.cpp` covers advertising, aggregate registration, structured lookup by name, late-registered deferred lookup by category/capability, post-move registry lookup, and malformed selectors. `test-tool` reports 166 cases / 1588 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/{agent-platform,tool-runtime}.md`, specs 0002/0016, `rules/prompt-design.md`, `QUALITY_SCORE.md`, and this release note reflect the shipped registry lookup plus the remaining prompt/session promotion gap. | [history](../histories/2026-05/20260524-0645-tool-search-registry-lookup.md) |
| 2026-05-24 | tool-audit-usage-metadata | Successful direct tool dispatches now enrich their existing permission audit row with measured usage metadata. Operators can query `metadata_json.usage` for bytes read/written, touched files, match counts, wall time, and cap flags without seeing duplicate permission-decision rows. `xmake run orangutan` now reports slice 67. Provider adapter mapping remains future spec-0014 work; batched-call audit correlation belongs to the upcoming scheduler. | `permission::AuditSink` gains default no-op `update_metadata(AuditMetadataUpdate)`, with `RecordingAuditSink` and `StorageAuditSink` implementations. `storage::AuditRepository` adds `update_event_metadata` to replace the newest matching row's metadata. `Registry::dispatch` preserves pre-handler decision recording, applies output caps after successful handler returns, then best-effort writes non-empty `Output::usage` under `metadata_json.usage`. Tests raise `test-storage` to 61 cases / 718 assertions, `test-permission` to 88 cases / 403 assertions, and `test-tool` to 161 cases / 1524 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/{tool-runtime,permissions-and-hooks,storage-runtime}.md`, specs 0008/0012/0014, `QUALITY_SCORE.md`, and this release note reflect the shipped audit usage fan-out. | [history](../histories/2026-05/20260524-0635-tool-audit-usage-metadata.md) |
| 2026-05-24 | tool-output-byte-caps | Structured tool output is now bounded at the dispatch boundary. Successful tool output text is truncated when it exceeds the configured/default text cap, oversized `data_json` is dropped while the text fallback remains available, and `tool_after` observers see the capped output plus `usage.truncated` / `usage.data_dropped`. `xmake run orangutan` now reports slice 66. Provider adapter mapping and audit usage fan-out remain future spec-0014 work. | `include/oran/tool/output.hpp` adds `OutputCapOptions`, `OutputCapReport`, and `apply_output_caps`; `src/oran-tool/output.cpp` uses the existing UTF-8 boundary helper so text caps do not split code points; `DispatchContext` gains `output_caps`; and `Registry::dispatch` applies caps after successful handler returns and before `tool_after`. `oran-config` parses `runtime.tool_output.max_text_bytes` / `max_data_bytes`, and `config.example.json` documents the 256 KiB / 1 MiB defaults. Tests raise `test-config` to 26 cases / 184 assertions and `test-tool` to 161 cases / 1515 assertions; `bench-tool` adds `output.apply_caps`. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/tool-runtime.md`, specs 0012/0014, `design-docs/secrets-and-state.md`, `QUALITY_SCORE.md`, and this release note reflect the shipped cap boundary. | [history](../histories/2026-05/20260524-0617-tool-output-byte-caps.md) |
| 2026-05-24 | hook-structured-output-redaction | Hook observers can now receive structured tool output safely. Default hook sinks continue to see only the text fallback, timing/error fields, and usage counters; trusted local sinks may opt in to raw `data_json` delivery by returning `SinkKind::trusted_local`. `xmake run orangutan` now reports slice 65. Provider mapping, scheduler byte caps, and audit usage fan-out remain future spec-0014 work. | `hook::Sink` gains `kind()` plus `SinkKind::default_` / `SinkKind::trusted_local`; `hook::InProcessSink` accepts an optional sink kind; `ToolAfterPayload` gains optional `data_json`; `Registry::dispatch` copies successful `Output::data_json` into `tool_after`; and `Bus::publish_advisory` makes per-sink payload copies that clear raw structured data for non-trusted-local sinks. Tests cover bus-level default-vs-trusted redaction, `InProcessSink` kind selection, and registry hook fan-out, raising `test-hook` to 17 cases / 109 assertions and `test-tool` to 157 cases / 1485 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/permissions-and-hooks.md`, `design-docs/tool-runtime.md`, spec 0014, `QUALITY_SCORE.md`, and this release note reflect the shipped redaction boundary. | [history](../histories/2026-05/20260524-0600-hook-structured-output-redaction.md) |
| 2026-05-24 | tool-directory-list-structured-output | `directory.list` now returns a structured JSON payload alongside its existing text fallback. Callers still receive the same sorted `<path>:<kind>:<size_bytes or '-'>` lines, and structured-capable adapters can read `data_json` for `kind`, `path`, `include_hidden`, `max_entries`, `entry_count`, and an `entries[]` array of `{name, path, kind, size_bytes}` (with JSON null `size_bytes` for non-regular kinds). Successful calls also report `files_touched=1` (the directory itself) and `match_count` (entry count) in `Output::usage`. `xmake run orangutan` now reports slice 64; with this slice every shipped filesystem built-in has finished its v1 migration to the structured envelope. Provider mapping, scheduler byte caps, audit fan-out, and hook raw-data redaction remain future work. | `src/oran-tool/directory_list.cpp` serializes the entries vector into `Output::data_json` while preserving the slice-29 text rendering and records directory-walk usage counters. `include/oran/tool/builtins.hpp` and the tool catalog description document the structured fields. `tests/tool/test_registry.cpp` asserts happy-path entries[], empty-directory empty array, hidden-flag round-trip in data_json, and the max_entries echo, raising `test-tool` to 156 cases / 1470 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/tool-runtime.md`, spec 0014, `QUALITY_SCORE.md`, and this release note reflect the shipped built-in migration step. | [history](../histories/2026-05/20260524-0540-tool-directory-list-structured-output.md) |
| 2026-05-24 | tool-file-search-structured-output | `file.search` now returns a structured JSON payload alongside its existing text fallback. Callers still receive the same `path:line:text` lines and trailing truncation summary, and structured-capable adapters can read `data_json` for `kind`, `path`, `pattern`, `regex`, `matches[]` (`{path, line_number, text}` per match), `match_count`, `truncated`, `truncation_reason` (null / `matches` / `bytes`), `files_scanned`, and `bytes_read`. Successful searches also report `bytes_read` (cumulative scanned file bytes), `files_touched` (non-binary scanned files), `match_count` (post-truncation), and the `truncated` cap flag in `Output::usage`. `xmake run orangutan` now reports slice 63; provider mapping, scheduler byte caps, audit fan-out, hook raw-data redaction, and the structured `directory.list` payload remain future work. | `src/oran-tool/file_search.cpp` serializes the walk outcome (matches array + per-walk usage) into `Output::data_json` while preserving the slice-20 text rendering and the slice-47 byte-cap message. `include/oran/tool/builtins.hpp` and the tool catalog description document the structured fields. `tests/tool/test_registry.cpp` asserts single-file matches, recursive walk usage, match-cap truncation, byte-cap truncation, the empty-matches structured payload, and the `regex` flag round-trip, raising `test-tool` to 152 cases / 1415 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/tool-runtime.md`, spec 0014, `QUALITY_SCORE.md`, and this release note reflect the shipped built-in migration step. | [history](../histories/2026-05/20260524-0420-tool-file-search-structured-output.md) |
| 2026-05-24 | tool-file-read-structured-output | `file.read` now returns a structured JSON payload alongside its existing text fallback. Callers still receive the stable spec-0011 header plus requested body, and structured-capable adapters can read `data_json` for `kind`, `path`, requested `text`, `fingerprint`, range metadata, returned byte count, and truncation. Successful reads also report `bytes_read`, one touched file, and the truncation flag in `Output::usage`. `xmake run orangutan` now reports slice 62; provider mapping, scheduler byte caps, audit fan-out, hook raw-data redaction, and structured `file.search` / `directory.list` payloads remain future work. | `src/oran-tool/file_read.cpp` serializes the requested read result into `Output::data_json` while preserving the text header/body shape and records read usage counters. `include/oran/tool/builtins.hpp` and the tool catalog description document the structured fields. `tests/tool/test_registry.cpp` asserts whole-file, line-range, byte-range, and truncated reads, raising `test-tool` to 146 cases / 1323 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/tool-runtime.md`, specs 0011/0014, `QUALITY_SCORE.md`, and this release note reflect the shipped built-in migration step. | [history](../histories/2026-05/20260524-0245-tool-file-read-structured-output.md) |
| 2026-05-24 | tool-mutation-usage-counters | File mutation tools now return measured usage counters in the structured output envelope. `file.write` reports bytes written and one touched file, `file.edit` reports bytes read/written, one touched file, and replacement count, and `file.delete` reports one touched file with zero bytes written. Their existing text confirmations are unchanged and `data_json` remains absent for this v1 migration. `xmake run orangutan` now reports slice 61; provider mapping, scheduler byte caps, audit fan-out, and structured `data_json` payloads remain future work. | `src/oran-tool/file_write.cpp`, `file_edit.cpp`, and `file_delete.cpp` fill `Output::usage` on successful handler returns; public built-in comments and catalog descriptions document the counters. `tests/tool/test_registry.cpp` asserts the counters and pins `data_json == nullopt` for the three mutation tools. `test-tool` reports 145 cases / 1279 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/tool-runtime.md`, spec 0014, `QUALITY_SCORE.md`, and this release note reflect the shipped built-in migration step. | [history](../histories/2026-05/20260524-0230-tool-mutation-usage-counters.md) |
| 2026-05-24 | tool-output-envelope | Tool handlers now have a forward-compatible result envelope instead of a text-only shape. Current built-ins still behave text-first, but future provider adapters, schedulers, hooks, and audit sinks can carry structured JSON bytes, attachment metadata, and usage counters without changing the handler contract again. `xmake run orangutan` now reports slice 60; provider mapping, byte caps, audit fan-out, raw-data hook redaction, and built-in structured payload migrations remain future work. | New `include/oran/tool/output.hpp` defines `tool::Output`, `ToolUsage`, and `Attachment`; `include/oran/tool.hpp` exports it and `Registry::Handler` now returns that envelope. `Output::text_only(...)` preserves the v1 path and `Output::error(...)` marks structured-capable errors. `hook::ToolAfterPayload` gains hook-local `ToolUsage`, and `Registry::dispatch` copies successful `Output::usage` into the hook payload without coupling `oran-hook` to `oran-tool`. `tests/tool/test_output.cpp` covers the envelope helpers and metadata fields; `tests/tool/test_registry.cpp` covers hook usage propagation; `bench-tool` adds `output.text_only` vs. `output.with_data_16kib`. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/tool-runtime.md`, `design-docs/permissions-and-hooks.md`, spec 0014, the tech-debt tracker, and `QUALITY_SCORE.md` reflect the shipped base envelope. | [history](../histories/2026-05/20260524-0215-tool-output-envelope.md) |
| 2026-05-24 | tool-catalog-renderer | Prompt-facing tool catalog bytes now have a deterministic renderer before `oran-prompt` lands. Tool declarations can mark deferred tools and categories; the renderer keeps active full-schema blocks separate from deferred name/description rows and exposes aggregate cache stats for the bounded rendered-block cache. `xmake run orangutan` now reports slice 59; active-tool config, `tool.search`, and promotion state remain future work. | `core::ToolDef` gains `deferred` and `category`; shipped filesystem built-ins set category metadata. New `include/oran/tool/catalog.hpp` / `src/oran-tool/catalog.cpp` add `tool::CatalogRenderer`, canonical JSON Schema rendering in the `.cpp`, sorted active/deferred output, and a 256-entry rendered-block cache keyed by rendered-block fields plus renderer version. `tests/tool/test_registry.cpp` adds four `[catalog]` regressions, `tests/core/test_tool_def.cpp` covers the new fields, and `bench-tool` adds cold-vs-hot catalog rendering. Focused tests report `test-core` 69 cases / 450 assertions and `test-tool` 140 cases / 1209 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/tool-runtime.md`, specs 0012/0016, and `QUALITY_SCORE.md` reflect the shipped prework. | [history](../histories/2026-05/20260524-0200-tool-catalog-renderer.md) |
| 2026-05-24 | io-read-cache-watcher | External edits can now proactively invalidate `read_text_file_ranged` caches through an IO-layer watcher instead of relying only on per-hit `stat` revalidation. The public API can watch one directory or an existing recursive tree, reports only aggregate watcher health, and remains cancellation-aware for long-running runtime ownership. `xmake run orangutan` now reports slice 58; bootstrap/config does not start the watcher automatically yet. | `include/oran/io/file.hpp` adds `ReadTextFileWatchOptions`, `ReadTextFileWatchStats`, and `watch_read_text_file_ranged_cache(executor, root, options)`. `src/oran-io/file.cpp` implements the Linux/inotify backend behind `.cpp`-local system headers, validates the root directory, registers recursive watches, drains events through `asio::posix::stream_descriptor`, and calls `invalidate_read_text_file_ranged_cache(path)` for each changed event path; inotify queue overflow conservatively invalidates all private range-read caches because exact paths were lost. `tests/io/test_file.cpp` adds three `[watch]` regressions covering external rewrite invalidation with restored size/mtime, recursive child-directory registration, and cancellation while waiting; `test-io` reports 49 cases / 286 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/io-runtime.md`, specs 0011/0012, and `QUALITY_SCORE.md` reflect that spec 0011 v1.1's IO-layer cache/watch work is complete while bootstrap startup wiring remains downstream. | [history](../histories/2026-05/20260524-0145-io-read-cache-watcher.md) |
| 2026-05-24 | io-read-cache-path-invalidation | Range-read caches can now be invalidated for one file instead of flushing every cached file view. Successful `write_text_file` / `delete_file` calls keep unrelated `read_text_file_ranged` cache entries hot, and future watcher callbacks have a public `invalidate_read_text_file_ranged_cache(path)` seam to mark one canonical path stale. `xmake run orangutan` now reports slice 57; CLI surface unchanged. | `core::BoundedCache` adds `erase_if(predicate)` for explicit caller-driven invalidation without incrementing policy eviction counters. `oran-io` exposes `io::invalidate_read_text_file_ranged_cache(path)`, canonicalises paths through the existing cache-key helper, erases matching line-offset-index and file-view entries, and routes successful writes/deletes through that seam. `tests/core/test_bounded_cache.cpp` adds an `erase_if` regression, and `tests/io/test_file.cpp` adds three cache invalidation regressions; `test-core` reports 69 cases / 446 assertions and `test-io` reports 46 cases / 260 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/io-runtime.md`, specs 0011/0012, and `QUALITY_SCORE.md` reflect the path-stale seam while the concrete watcher event source remains downstream. | [history](../histories/2026-05/20260524-0130-io-read-cache-path-invalidation.md) |
| 2026-05-24 | permission-approval-grant-cap | Approval replay state is now bounded per identity. A long-running process can retain at most 64 live approval grants for the same identity; issuing a new distinct grant beyond that cap evicts that identity's oldest grant. Evicted tokens still verify cryptographically, but replay fails as `reason=no_grant`. `xmake run orangutan` now reports slice 56; CLI surface unchanged. | `ApprovalBroker::approve` now calls `reap_expired(now)` before enforcing `ApprovalBroker::max_grants_per_identity`, tracks grant insertion order with a sequence counter, and evicts only the oldest same-identity grant when inserting a new distinct triple would exceed the cap. `tests/permission/test_approval_broker.cpp` adds three `[bounded]` regressions covering oldest-grant eviction, per-identity isolation, and reap-before-cap behavior. `test-permission` reports 86 cases / 390 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/permissions-and-hooks.md`, specs 0008/0012, and `QUALITY_SCORE.md` reflect the shipped bounded-state item. | [history](../histories/2026-05/20260524-0115-permission-approval-grant-cap.md) |
| 2026-05-24 | tool-workspace-pre-resolve-audit | Workspace path policy now runs at the registry boundary for current filesystem built-ins when `DispatchContext::workspace` is supplied. `file.read`, `file.write`, `file.edit`, `file.delete`, `file.search`, and `directory.list` still accept the same inputs, but their `path` field is resolved before permission evaluation and before any handler reaches `oran-io`. Resolver failures are audited with the permission verdict, return without running the handler, and do not spend ask-approval replay. Audit rows now carry redacted `metadata_json.path_resolution` fields instead of raw input paths. `xmake run orangutan` now reports slice 55; CLI surface unchanged. | `Registry::dispatch` calls the new private `src/oran-tool/path_resolution.cpp` helper after `tool_before` and before `RuleSet::evaluate`, stores successful paths on `DispatchContext::resolved_path`, and writes hashed path/root plus resolver flags or error reason into `permission::AuditEvent::metadata_json`. The six filesystem handlers consume `ctx.resolved_path` when present and keep the previous in-handler workspace fallback for workspace-less callers. `tests/tool/test_workspace.cpp` adds four audit/pre-resolution regressions; focused `[workspace][audit]` reports 4 cases / 66 assertions and full `test-tool` reports 136 cases / 1170 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/tool-runtime.md`, `product-specs/0013-workspace-and-path-policy.md`, `SECURITY.md`, and `QUALITY_SCORE.md` reflect the closed v1 0013 boundary. | [history](../histories/2026-05/20260524-0100-tool-workspace-pre-resolve-audit.md) |
| 2026-05-24 | io-read-cache-stats | `read_text_file_ranged` now exposes aggregate health for its two bounded caches: the large-file line-offset index and the file-view cache. Operators and future debug surfaces can see hits, misses, evictions, oversize rejections, current entries, and current bytes without seeing canonical paths, ranges, or file contents. `xmake run orangutan` now reports slice 54; CLI surface unchanged. | `include/oran/io/file.hpp` adds `ReadTextBoundedCacheStats`, `ReadTextFileCacheStats`, and `io::read_text_file_ranged_cache_stats()`. `src/oran-io/file.cpp` snapshots each private `core::BoundedCache` under its existing mutex and maps the generic stats into the public path-free shape. `tests/io/test_file.cpp` adds a `[range][cache][stats]` regression covering file-view cold/hot reads and line-offset index reuse. `test-io` reports 43 cases / 228 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/io-runtime.md`, specs 0011/0012, and `QUALITY_SCORE.md` reflect the observability surface. | [history](../histories/2026-05/20260524-0045-io-read-cache-stats.md) |
| 2026-05-24 | io-read-singleflight | Concurrent cold `file.read` / `read_text_file_ranged` calls for the same unchanged file view now share one in-flight filesystem read instead of stampeding the executor. Hot file-view cache hits still return before touching the in-flight table, and the table is capped at 64 entries with public aggregate stats only, so the optimization does not introduce unbounded or path-leaking runtime state. `xmake run orangutan` now reports slice 53; CLI surface unchanged. | `src/oran-io/file.cpp` splits range reads into prepare/cache-hit and cold-read phases, adds a private 64-entry singleflight table keyed by canonical path, range, `max_bytes`, and `(size_bytes, mtime_ns)`, and exposes `io::read_text_file_ranged_singleflight_stats()` via `include/oran/io/file.hpp`. `tests/io/test_file.cpp` adds a `[range][singleflight]` regression covering two concurrent cold reads collapsing to one leader plus one follower. `test-io` reports 42 cases / 212 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/io-runtime.md`, specs 0011/0012, and `QUALITY_SCORE.md` reflect the shipped v1.1 item. | [history](../histories/2026-05/20260524-0030-io-read-singleflight.md) |
| 2026-05-24 | io-file-view-cache | Repeated `file.read` / `read_text_file_ranged` calls on an unchanged file can now reuse a bounded in-memory file-view cache instead of reopening and re-streaming the same payload every time. Hits still validate the cheap file fingerprint before returning, and successful in-process writes/deletes clear both the file-view cache and the line-offset index, so the cache improves steady-state reads without weakening stale-file protection. `xmake run orangutan` now reports slice 52; CLI surface unchanged. | `src/oran-io/file.cpp` adds a private `core::BoundedCache<FileViewCacheKey, ReadTextResult>` keyed by canonical path, range, `max_bytes`, and `(size_bytes, mtime_ns)`, capped at 64 entries / 16 MiB / 10 minutes and guarded by an explicit mutex. `tests/io/test_file.cpp` adds two `[range][cache]` regressions covering external rewrite refresh and in-process write invalidation even when size and mtime are restored to the old fingerprint. `test-io` reports 41 cases / 198 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/io-runtime.md`, `product-specs/0011-file-view-and-caching.md`, and `QUALITY_SCORE.md` reflect the shipped v1.1 item. | [history](../histories/2026-05/20260524-0015-io-file-view-cache.md) |
| 2026-05-24 | tool-search-regex-cache | Repeated `file.search` calls with `regex=true` no longer recompile the same pattern every dispatch. Compiled `permission::InputPattern` instances are reused from a bounded process-local cache keyed by pattern plus line-match mode. The cache is capped at 64 entries / 64 KiB / 10 minutes, so repeated agent searches get the steady-state path without introducing unbounded runtime state. `xmake run orangutan` now reports slice 51; CLI surface unchanged. | `src/oran-tool/file_search.cpp` adds a private `core::BoundedCache<RegexCacheKey, shared_ptr<const InputPattern>>` plus an explicit mutex, looks up the cache before compiling, and stores successful compiles after validation. Literal searches are unchanged. `tests/tool/test_registry.cpp` adds a `[file_search][regex]` regression covering the same pattern reused across two dispatches; focused `test-tool` reports 132 cases / 1104 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/tool-runtime.md`, `product-specs/0011-file-view-and-caching.md`, `QUALITY_SCORE.md`, and `bench/tool` notes reflect the shipped v1.1 item. | [history](../histories/2026-05/20260524-0005-tool-search-regex-cache.md) |
| 2026-05-23 | io-line-offset-index | Large line-range reads are now indexed in `oran-io`: files larger than 256 KiB lazily build a bounded line-offset table so a later `{start_line, line_count}` read can seek directly to the requested span instead of streaming from the top. The cache is in-memory only, bounded to 32 entries / 8 MiB / 10 minutes, and is cleared after successful in-process writes/deletes so stale offsets do not survive mutations. `xmake run orangutan` now reports slice 50; CLI surface unchanged. | `src/oran-io/file.cpp` adds the private `LineOffsetIndex` cache keyed by canonical path plus `(size_bytes, mtime_ns)`, builds offsets in chunks, dispatches large line ranges through the index-backed path, and invalidates the cache after successful `write_text_file` / `delete_file`. `tests/io/test_file.cpp` adds three `[range][lines]` regressions covering indexed reads plus write/delete invalidation even when size and mtime are restored to the old fingerprint. Focused `test-io` reports 39 cases / 182 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/io-runtime.md`, `product-specs/0011-file-view-and-caching.md`, and `QUALITY_SCORE.md` reflect the shipped v1.1 item. | [history](../histories/2026-05/20260523-2325-io-line-offset-index.md) |
| 2026-05-23 | tool-search-ignore-files | `file.search` now honours `.gitignore` and `.ignore` files during recursive directory walks when `respect_ignore=true` (default). Rules are loaded from the search root downward and evaluated as a per-directory stack on top of slice 48's built-in skip list. The supported subset covers blank lines, `#` comments, escaped leading `#` / `!` literals, `!` negation, trailing `/` directory rules, slash-relative patterns, basename patterns, and fnmatch-style globs. `respect_ignore=false` remains the forensic opt-out and explicit single-file searches still scan the named file directly. `xmake run orangutan` now reports slice 49; the CLI surface is unchanged. | `src/oran-tool/file_search.cpp` adds a private ignore-rule parser, per-directory `IgnoreStack`, `fnmatch`-backed matching, nested `.ignore` loading, and escaped leading marker handling. The recursive walk now syncs the ignore stack for each entry parent before deciding whether to skip the entry or prune recursion. `include/oran/tool/builtins.hpp` and the tool description document the expanded `respect_ignore` behavior. `tests/tool/test_registry.cpp` adds four `[respect_ignore]` cases covering root `.gitignore` basename/path/directory/negation rules, escaped marker literals, nested `.ignore` descendant scope, and `respect_ignore=false`; focused `test-tool "[file_search]"` reports 131 cases / 1093 assertions. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/tool-runtime.md`, specs 0011/0013, `QUALITY_SCORE.md`, and the tech-debt tracker reflect that the remaining search debt is performance-oriented rather than ignore-file parsing. | [history](../histories/2026-05/20260523-2315-tool-search-ignore-files.md) |
| 2026-05-23 | tool-search-ignore-predicate | `file.search`'s recursive directory walk now honours a built-in ignore predicate. The walk skips `.git`, `.xmake`, `.orangutan`, `build`, and `node_modules` directories by default — well-known VCS / vendor / build directories whose contents have very low signal density per byte for an agent and whose presence in a tree is near universal. The skip list fires *regardless* of `include_hidden`, so an opt-in to scan hidden files (e.g., `.env`) no longer unleashes a full descent through `.git/`. A new optional `respect_ignore` field (default `true`) lets callers opt out for forensic searches. Spec 0011 v1.1's "`file.search` ignore predicate" item's built-in subset is now complete; honouring `.gitignore` / `.ignore` files (per-directory parsing, glob matching, ancestor walking) is the larger follow-up. `xmake run orangutan` now reports slice 48; the CLI surface is unchanged. | `src/oran-tool/file_search.cpp` adds `kIgnoredDirectoryNames` (the five-entry skip list) and the `is_ignored_directory_name(path)` helper; `SearchOptions` grows `bool respect_ignore{true}`; the JSON Schema and tool description advertise the new field; the recursive walk gates `disable_recursion_pending()` on the ignored predicate immediately after the hidden-file filter. `include/oran/tool/builtins.hpp` docstring documents the new field and the default skip list. `tests/tool/test_registry.cpp` adds three `[respect_ignore]` cases (default skips build + node_modules, opt-out re-includes them, `include_hidden=true` still skips `.git`) and extends the malformed-input matrix with `respect_ignore` non-boolean (sink event tally 12 → 13). `tests/tool` now reports 127 cases / 1067 assertions (was 124 / 1047). Docs: `STATUS.md`, `ARCHITECTURE.md` (`file.search` inventory row gains the new field + skip list), `product-specs/0011-file-view-and-caching.md` (slice-48 status block), and `QUALITY_SCORE.md` reflect the shipped piece. | [history](../histories/2026-05/20260523-2200-tool-search-ignore-predicate.md) |
| 2026-05-23 | tool-search-max-output-bytes | `file.search` now accepts an optional `max_output_bytes` integer field (default 1 MiB) that caps the rendered `path:line:text` payload independent of `max_matches`. Spec 0011 v1.1's "Output cap on `file.search`" item now ships: a handful of very long matching lines can no longer flood the prompt even when the match count is small. The scan stops the moment the next candidate match's exact rendered cost (path + ":" + line_number + ":" + text plus the inter-match `\n`) would push the running total past the budget, and the trailing summary spells `(truncated; output capped at <N> bytes)`. When both caps could have fired in the same call, the match-cap message still wins — pinned by a deterministic tie test so an agent that sets both can rely on a single dominant message per call. The default-shape callsite (no `max_output_bytes` supplied) is unchanged, so slice-20/24/39 happy-path callers see no behavior change. `xmake run orangutan` now reports slice 47; the CLI surface is unchanged. | `src/oran-tool/file_search.cpp` extends the JSON Schema, adds `max_output_bytes` to `SearchOptions`, replaces the `bool truncated` outcome with a `TruncReason { none, matches, bytes }` enum, adds `count_digits` + `rendered_cost` helpers so each candidate's exact byte cost is computed inline, threads an `accumulated_bytes` counter through `scan_text`, gates the directory walk on both budgets, and routes truncation through a renderer that knows which cap fired (match-cap still dominates a tie). `include/oran/tool/builtins.hpp` docstring documents the new field. `tests/tool/test_registry.cpp` adds three positive `[max_output_bytes]` cases (tight byte cap stops at the first match with the byte-cap message; generous default leaves a multi-match response untouched; match-cap-vs-byte-cap tie pins the documented precedence) and extends the malformed-input matrix with `max_output_bytes` non-integer and zero (sink event count bumps 10 → 12). `tests/tool` now reports 124 cases / 1047 assertions (was 121 / 1022). Docs: `STATUS.md`, `ARCHITECTURE.md` (`file.search` inventory row gains the new field + truncation-precedence note), `product-specs/0011-file-view-and-caching.md` (slice-47 status block), and `QUALITY_SCORE.md` reflect the shipped piece. | [history](../histories/2026-05/20260523-2100-tool-search-max-output-bytes.md) |
| 2026-05-23 | tool-edit-write-expected-version | `file.write` and `file.edit` now accept the same opaque version token `file.read` produces, and refuse to mutate a file whose on-disk fingerprint no longer matches the supplied token. Spec 0011 v1's acceptance criterion 4 (stale-edit detection) is now mechanically enforced: when `expected_version` is supplied and the current `v1:<sha256(canonical_path)>:<size>:<mtime_ns>` token differs (or the target vanished), the call fails with `Error::conflict` carrying `reason=stale_fingerprint`, `expected=<supplied>`, and the current `fingerprint=<token>` in context — the agent can hand the fresh token straight to a follow-up `file.read`. The pre-mutation check runs *before* the temp-then-rename or read, so a stale token never leaves a partially-written file behind. The opaque-token helper that slice 45 introduced for `file.read` is lifted into the private `src/oran-tool/version_token.hpp` header so all three file built-ins now share the same wire spelling. v1 of the file-view system is complete; v1.1 (line-offset index, file-view cache, regex compile cache, singleflight, external-edit awareness) is next on top of the slice-44 `BoundedCache` primitive. `xmake run orangutan` now reports slice 46; CLI surface unchanged. | `src/oran-tool/version_token.hpp` (new private header). `src/oran-tool/file_read.cpp`, `src/oran-tool/file_write.cpp`, and `src/oran-tool/file_edit.cpp` consume the helper; the two mutation tools gain the optional `expected_version` field in their JSON Schema and a pre-write/pre-read fingerprint guard. `include/oran/tool/builtins.hpp` docstrings document the new optional field. `tests/tool/test_registry.cpp` adds five `[if_version]` cases (`file.write` matching + stale + missing-path conflict; `file.edit` matching + stale conflict — file untouched); `tests/tool` reports 121 cases / 1022 assertions (was 116 / 980). Docs: `STATUS.md`, `ARCHITECTURE.md` (slice-status block + `file.write` / `file.edit` inventory rows), `product-specs/0011-file-view-and-caching.md` (slice-46 status block), and `QUALITY_SCORE.md` reflect the shipped contract. | [history](../histories/2026-05/20260523-0010-tool-edit-write-expected-version.md) |
| 2026-05-22 | tool-file-read-v2 | The `file.read` tool now exposes the third piece of spec 0011 v1: a range-aware, fingerprint-stamped read schema with an `if_version` short-circuit. Input shape becomes `{path, start_line?, line_count?, offset_bytes?, length_bytes?, max_bytes?, if_version?}`; the line and byte ranges are mutually exclusive both at schema level (`Registry::add` rejects malformed declarations before insertion) and at handler entry. The handler dispatches through `io::read_text_file_ranged` and wraps the returned slice in a single header line `<path>:<start>-<end> fingerprint=<token> bytes=<n>[ truncated]` so today's text-only `tool::Output` can still surface the metadata until `Output v2` lands. The opaque version token is `v1:<sha256(canonical_path)>:<size>:<mtime_ns>` — the path-hash discrimination reuses the existing libsodium SHA-256 via `permission::ApprovalAuthority::input_hash` so a token from file A cannot be aliased to file B. A new `core::ErrorKind::not_modified` enumerator + `Error::not_modified()` builder join the cross-boundary error vocabulary; `if_version` matching the current fingerprint short-circuits to that kind (carrying `path` + `fingerprint` context) without paying the body read cost. `xmake run orangutan` now reports slice 45; CLI surface unchanged. | `src/oran-tool/file_read.cpp` is rewritten for the v2 input/output shapes; `include/oran/core/error.hpp` + `src/oran-core/error.cpp` gain `ErrorKind::not_modified` + the matching builder. `include/oran/tool/builtins.hpp` docstring documents the new schema. `tests/tool/test_registry.cpp` and `tests/tool/test_workspace.cpp` gain five new cases (line range, byte range, mixed line+byte range rejected, `if_version` short-circuit + mismatched-token re-send + token-in-context, token changes when the file is rewritten) plus an updated happy-path assertion that splits the new header line off the body; `tests/tool` now reports 116 cases / 980 assertions (was 111 / 931). `tests/core/test_error.cpp` gains `not_modified` assertions; `tests/core` reports 68 / 437 (was 68 / 435). Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/io-runtime.md`, `product-specs/0011-file-view-and-caching.md`, `rules/error-handling.md` (`ErrorKind` enum), and `QUALITY_SCORE.md` reflect the shipped piece. | [history](../histories/2026-05/20260522-2350-tool-file-read-v2.md) |
| 2026-05-22 | core-bounded-cache | The `oran-core` surface now ships the generic bounded-state primitive both spec 0011 v1.1 (file-view cache, line-offset index, regex cache) and spec 0012 (tool scheduler bounded state) reach for: `core::BoundedCache<Key, Value, ByteSizeOf = BoundedCacheNoByteBudget>` (`<oran/core/bounded_cache.hpp>`). LRU on access, insert-based TTL, byte-budget eviction with `Stats { hits, misses, evictions_lru, evictions_ttl, evictions_bytes, rejected_oversize, current_entries, current_bytes }` observability. Single-strand by contract; `get` returns `Value*` (non-owning, nullable) so move-only types like `std::unique_ptr<re2::RE2>` work without `shared_ptr`. The byte-budget is opt-in via a template parameter — the default is "no byte budget" and stays zero-cost via `[[no_unique_address]]`. `xmake run orangutan` now reports slice 44; the CLI surface is unchanged. | New `<oran/core/bounded_cache.hpp>` (~250 LoC, header-only template). `tests/core/test_bounded_cache.cpp` adds 14 cases covering basic put/get, LRU eviction, TTL on get, bulk reap, oversize rejection, oversize-puts-invalidate, re-put refresh, move-only Value type (`unique_ptr<string>`), clear preserves lifetime counters, stats accuracy, and LRU position refresh on get; `tests/core` now reports 68 cases / 435 assertions (was 54 / 370). `bench/core/scenarios/bounded_cache.cpp` pins `unordered_map_insert_lookup_256` (~7.7 µs / 1024 ops) vs. `bounded_cache_insert_lookup_256` (~16.5 µs — ~2× LRU bookkeeping overhead) plus `bounded_cache_overflow_lru_eviction` (~30.8 µs) for the eviction-on-every-put hot path. Docs: `STATUS.md`, `ARCHITECTURE.md`, `product-specs/0011-file-view-and-caching.md` (v1.1 prerequisite note), `product-specs/0012-tool-scheduler-and-state.md` (status block plus shipped/spec deltas), and `QUALITY_SCORE.md` reflect the shipped primitive. | [history](../histories/2026-05/20260522-2330-core-bounded-cache.md) |
| 2026-05-22 | io-file-range | The `oran-io` surface now ships the second piece of spec 0011's file-view system: `io::FileRange { LineSpan \| ByteSpan }`, `io::ReadTextResult { text, fingerprint, start_line, end_line, returned_bytes, truncated }`, and the range-aware `io::read_text_file_ranged(executor, path, options)` overload. The blocking impl captures a fingerprint before AND after the read; size or mtime drift either retries once (whole-file reads of files < 64 KiB) or surfaces as `Error::conflict`. Byte ranges align both ends to UTF-8 code-point boundaries so a `length_bytes` cap that lands mid-codepoint shrinks the returned text instead of smuggling invalid bytes downstream. The legacy `io::read_text_file` returning `Result<std::string>` becomes a thin wrapper that surfaces `Error::invalid_argument` when the rich result reports `truncated=true`. `xmake run orangutan` now reports slice 43; CLI surface unchanged. | New `<oran/io/range.hpp>` carries the input/output shapes. `src/oran-io/file.cpp` adds `validate_range`, `align_to_utf8_boundaries` (drops leading continuation bytes AND trims trailing partial sequences), `read_whole_file_into`, `read_whole_file_blocking`, `read_line_range_blocking`, `read_byte_range_blocking`, `dispatch_read`, and the new `read_text_file_blocking` (returning `ReadTextResult`) plus `read_text_file_blocking_text_only` (the legacy wrapper). The async public path adds `read_text_file_ranged`. `tests/io/test_file.cpp` adds 13 cases (whole-file metadata, trailing partial line, `max_bytes` truncation, line range, line range capped by `max_bytes`, line range past EOF, byte range, byte range past EOF, UTF-8-mid-boundary byte range, empty range rejection, both-set range rejection, zero-field rejection, and a best-effort mid-read race test); `tests/io` now reports 36 cases / 152 assertions. New `bench/io/scenarios/read_range.cpp` pins the 10 MiB whole-file (~11.5 ms) vs. 80-line range (~18.9 µs) A/B — about 610× faster for the ranged path. Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/io-runtime.md`, `product-specs/0011-file-view-and-caching.md`, and `QUALITY_SCORE.md` track the shipped v1 piece. | [history](../histories/2026-05/20260522-2300-io-file-range.md) |
| 2026-05-22 | io-file-fingerprint | The `oran-io` surface now ships the lowest rung of spec 0011's file-view system: `io::FileFingerprint` (`size_bytes`, `mtime_ns`, reserved `optional<string> sha256`) plus a synchronous `io::compute_file_fingerprint(path)` helper. Future range-aware `read_text_file` and the agent loop's `if_version` token both consume this shape. CLI surface unchanged; `xmake run orangutan` now reports slice 42. SHA-256 hashing is reserved for a future slice. | New `<oran/io/fingerprint.hpp>` + `src/oran-io/fingerprint.cpp` (libstdc++'s ENOENT-with-`status()` quirk handled so missing files surface as `Error::not_found`, not `Error::io`). `tests/io/test_fingerprint.cpp` adds seven cases (size + mtime correctness, stability, mtime change, size change, missing/not-regular/empty-path rejection); `tests/io` now reports 23 cases / 90 assertions. `bench/io/scenarios/fingerprint.cpp` pins the A/B baseline against the raw `direct_stat_pair` (~430 ns helper overhead). Docs: `STATUS.md`, `ARCHITECTURE.md`, `design-docs/io-runtime.md`, `product-specs/0011-file-view-and-caching.md`, and `QUALITY_SCORE.md` reflect the shipped foundation. | [history](../histories/2026-05/20260522-2113-io-file-fingerprint.md) |
| 2026-05-22 | bootstrap-workspace-config-wiring | The runtime workspace is now operator-configurable. `config.permissions.workspace.extra_read_roots` and `permissions.workspace.extra_write_roots` are recognised at the global and per-agent layers; bootstrap canonicalises the override roots once at boot through the new `bootstrap::RuntimeAssembly`-owned `tool::Workspace`. A misconfigured root fails the boot rather than the first dispatched tool call. `xmake run orangutan` now reports slice 41 and the startup banner prints the canonical workspace root. CLI surface unchanged. Spec 0013's remaining work narrows to audit metadata + pre-permission resolution. | `oran-config` parses the new `permissions.workspace` block into `config::WorkspacePermissionsConfig`; `bootstrap::RuntimeAssembly::build` constructs and owns a `tool::Workspace` from `RuntimeAssemblyOptions::workspace_options`; `bootstrap::run` translates the typed config into `tool::WorkspaceOptions`. A latent bug in `tool::Workspace::create` (libstdc++'s `status` ENOENT being surfaced as `Error::io`) was fixed in the same slice so a missing workspace reports `Error::not_found`. Tests: `tests/config` reports 24 cases / 171 assertions and `tests/bootstrap` reports 48 cases / 153 assertions. Docs in `STATUS.md`, `ARCHITECTURE.md`, `design-docs/tool-runtime.md`, `product-specs/0013-workspace-and-path-policy.md`, `SECURITY.md`, `QUALITY_SCORE.md`, and `config.example.json` track the new typed surface. | [history](../histories/2026-05/20260522-2042-bootstrap-workspace-config-wiring.md) |
| 2026-05-22 | tool-workspace-directory-list | Workspace-aware listing is now in place for `directory.list` when the runtime supplies `DispatchContext::workspace`. Inputs accept the same JSON shape; relative in-workspace paths resolve through `tool::Workspace::resolve_list`, traversal outside the workspace rejects with `reason=outside_workspace`, and root-side symlinks that escape the workspace reject with `reason=symlink_escape`. After this slice every filesystem built-in (`file.read`, `file.write`, `file.edit`, `file.delete`, `file.search`, `directory.list`) consumes the workspace seam at the handler entry, so spec 0013's per-tool migration is done; the remaining work is purely structural (bootstrap ownership, audit metadata, pre-permission resolution, config wiring for override roots). `xmake run orangutan` now reports slice 40; the CLI surface is unchanged. | `src/oran-tool/directory_list.cpp` resolves the input path through `Workspace::resolve_list` before `io::list_directory` when `ctx.workspace` is non-null. `tests/tool/test_workspace.cpp` adds two dispatch regressions for relative success + traversal refusal and root-side symlink-escape refusal; `tests/tool` now reports 111 cases / 931 assertions. `docs/STATUS.md`, `docs/ARCHITECTURE.md`, `docs/design-docs/tool-runtime.md`, `docs/product-specs/0013-workspace-and-path-policy.md`, `docs/SECURITY.md`, and `docs/QUALITY_SCORE.md` reflect the closed per-tool migration. | [history](../histories/2026-05/20260522-1730-tool-workspace-directory-list.md) |
| 2026-05-22 | tool-workspace-file-search | Workspace-aware search is now in place for `file.search` when the runtime supplies `DispatchContext::workspace`. Inputs accept the same JSON shape; relative in-workspace paths resolve through `tool::Workspace::resolve_list`, traversal outside the workspace rejects with `reason=outside_workspace`, root-side symlinks that escape the workspace reject with `reason=symlink_escape`, and `permissions.workspace.extra_read_roots` widens the search domain the same way it already widens `file.read`. Nested entries during a recursive walk continue to skip symlinks wholesale — a stricter form of the same workspace policy. `xmake run orangutan` now reports slice 39; the CLI surface is unchanged. | `src/oran-tool/file_search.cpp` resolves the input path through `Workspace::resolve_list` before the executor hop when `ctx.workspace` is non-null. `tests/tool/test_workspace.cpp` adds three dispatch regressions for relative success, traversal refusal, symlink-escape refusal, and `extra_read_roots` override; `tests/tool` now reports 109 cases / 911 assertions. `docs/STATUS.md`, `docs/ARCHITECTURE.md`, `docs/design-docs/tool-runtime.md`, `docs/product-specs/0013-workspace-and-path-policy.md`, `docs/SECURITY.md`, and `docs/QUALITY_SCORE.md` reflect the shipped/pending split that now leaves `directory.list` as the last raw-path read built-in. | [history](../histories/2026-05/20260522-1700-tool-workspace-file-search.md) |
| 2026-05-22 | tool-workspace-mutations | Workspace-aware mutation is now in place for the core write/edit/delete tools when the runtime supplies `DispatchContext::workspace`. `file.write`, `file.edit`, and `file.delete` accept the same inputs as before, but relative in-workspace paths resolve through `tool::Workspace`; traversal outside the root rejects with `reason=outside_workspace`, and mutating symlink targets reject with `reason=symlink_target`. `file.search`, `directory.list`, bootstrap ownership, audit metadata, and pre-permission resolution remain follow-up workspace work. `xmake run orangutan` now reports slice 38. | `src/oran-tool/file_write.cpp`, `src/oran-tool/file_edit.cpp`, and `src/oran-tool/file_delete.cpp` now call `Workspace::resolve_write` / `resolve_delete` before reaching `oran-io` when `ctx.workspace` is non-null. `tests/tool/test_workspace.cpp` adds five dispatch regressions for relative success, traversal refusal, and mutating symlink refusal across the three tools; `tests/tool` now reports 106 cases / 887 assertions. Docs/status/security/release notes were updated for the shipped/pending split in spec 0013. | [history](../histories/2026-05/20260522-1559-tool-workspace-mutations.md) |
| 2026-05-22 | tool-workspace-foundation | The file-tool path policy now has its first concrete runtime surface. `file.read` can be constrained to a workspace when the caller supplies `DispatchContext::workspace`; the rest of the filesystem built-ins still need migration before workspace confinement is complete. `xmake run orangutan` now reports slice 37. | Added `tool::Workspace` with intent-specific path resolution, canonical roots, traversal rejection, symlink policy, extra read/write roots, and `ResolvedPath` metadata. `DispatchContext` gained an optional non-owning `Workspace*`, and `file.read` resolves through it when present. `tests/tool/test_workspace.cpp` adds the resolver matrix and a `file.read` dispatch regression; `tests/tool` now reports 101 cases / 834 assertions. | [history](../histories/2026-05/20260522-1529-tool-workspace-foundation.md) |
| 2026-05-22 | tool-registry-transparent-lookup | Tool dispatch keeps the same behavior but no longer allocates a temporary `std::string` just to look up a registered tool name. `xmake run orangutan` now reports slice 36; the CLI surface is unchanged. | `Registry::entries_` now uses transparent string hashing, and `Registry::remove`, `Registry::find`, and `Registry::dispatch` call `entries_.find` with the incoming `std::string_view` directly. `docs/design-docs/tool-runtime.md`, `docs/ARCHITECTURE.md`, `docs/QUALITY_SCORE.md`, `docs/STATUS.md`, and `docs/exec-plans/tech-debt-tracker.md` reflect that the last deep-review P0 is closed. | [history](../histories/2026-05/20260522-1456-tool-registry-transparent-lookup.md) |
| 2026-05-22 | tool-registry-schema-validation | Tool registration now fails fast on broken argument schemas. `Registry::add` rejects empty `input_schema_json`, unparseable JSON, non-object top-level schemas, and malformed common JSON Schema keywords before the tool reaches the catalog. `xmake run orangutan` now reports slice 35; the CLI surface is unchanged. | New internal `src/oran-tool/schema_validation.cpp` owns the lightweight schema sanity checker so `registry.cpp` does not pull in `nlohmann/json.hpp`. `include/oran/tool/registry.hpp` documents the expanded `add` failure mode. `tests/tool/test_registry.cpp` adds invalid-JSON and malformed-`required` registration tests; `tests/tool` now reports 94 cases / 760 assertions. `docs/design-docs/tool-runtime.md`, `docs/product-specs/0002-tool-registry.md`, `docs/ARCHITECTURE.md`, `docs/QUALITY_SCORE.md`, `docs/STATUS.md`, and `docs/exec-plans/tech-debt-tracker.md` reflect that the JSON-schema validation P0 is closed and transparent registry lookup hashing is the remaining deep-review P0 item. | [history](../histories/2026-05/20260522-1408-tool-registry-schema-validation.md) |
| 2026-05-22 | tool-write-edit-max-bytes | `file.write` and `file.edit` now have bounded mutation payloads. Both tools accept optional `max_bytes` (default and hard ceiling 16 MiB). `file.write` rejects oversized `content` before touching the target, while `file.edit` caps both the read and the final replacement output so an edit cannot create text a later `file.read` would reject. The stale root deep-review artifact is deleted after its actionable findings were absorbed into current docs, and the framework now ships a binding rule that the next deep-review artifact must follow (versioned name, first-line stamp, delete-on-close). `xmake run orangutan` now reports slice 34; the CLI surface is unchanged. | `src/oran-tool/file_write.cpp` and `src/oran-tool/file_edit.cpp` add schema/description support for `max_bytes`, positive-integer validation, the 16 MiB ceiling, and focused invalid_argument context (`content_bytes` or `output_bytes`). `tests/tool/test_registry.cpp` adds boundary/oversize tests for `file.write`, output-cap tests for `file.edit`, malformed `max_bytes` cases, and closes the tracked `-Wmissing-field-initializers` warnings by default-constructing captured hook rows plus centralising the test-only no-capability `ToolDef`. `tests/tool` now reports 92 cases / 750 assertions. `docs/product-specs/0011-file-view-and-caching.md`, `docs/design-docs/tool-runtime.md`, `docs/ARCHITECTURE.md`, `docs/QUALITY_SCORE.md`, `docs/STATUS.md`, and `docs/exec-plans/tech-debt-tracker.md` reflect the shipped cap and remaining P0 backlog. `docs/rules/prompt-design.md`, `CLAUDE.md` / `AGENTS.md`, `docs/agents/domain.md`, and `docs/product-specs/0016-prompt-and-tool-catalog-cache.md` re-center prompt work on the current rule/spec/status chain rather than stale review text. New `docs/rules/deep-review.md` codifies the lifecycle for the next deep-review artifact; `docs/rules/README.md`, `CLAUDE.md` / `AGENTS.md`, `docs/agents/domain.md`, and `docs/rules/docs-in-sync.md` cross-link it. The pre-existing `/tmp/...` review references in `docs/STATUS.md` and `docs/exec-plans/tech-debt-tracker.md` are replaced with self-contained descriptions so a future agent on a fresh checkout is not asked to read files that never existed in the repo. | [history](../histories/2026-05/20260522-1104-tool-write-edit-max-bytes.md) |
| 2026-05-21 | tool-file-search-cancellation | `file.search` is no longer uncancellable in flight. The deep-review SMELL-4.1.3 footgun — `walk_and_scan` ran fully synchronously after the executor hop, so a long directory walk or a multi-MiB file read pinned the worker thread to the search regardless of any cancellation signal — is closed by threading `asio::cancellation_state` into `walk_and_scan` and `read_text_capped`. The walk polls once per directory entry (just before stat / open); the read loop polls once per 8 KiB chunk, so an 8 MiB file aborts within one chunk of the signal landing. A `cancelled` error from `read_text_capped` mid-walk is treated as terminal rather than the existing "silently skip oversized/unreadable" path so the cancellation cannot be swallowed by the walk loop. The two polls are relaxed atomic loads — single-digit nanoseconds against per-entry stat cost and per-chunk read cost. `xmake run orangutan` now reports slice 33; the CLI surface is unchanged. | `src/oran-tool/file_search.cpp` — added an `is_cancelled(const asio::cancellation_state&)` helper that mirrors the io layer's; `walk_and_scan(opts, cancellation)` polls inside the directory-iterator loop and propagates a `cancelled` error from `read_text_capped`; `read_text_capped(path, cancellation)` polls at the head of every read iteration. The handler now uses the helper for its existing pre/post-hop checks. `tests/tool/test_registry.cpp` grows to 90 cases / 716 assertions (+1 case / +4 assertions: a multi-threaded regression test that arms the chunk-read poll by reading an 8 MiB file on a worker `std::jthread` while the test thread emits the signal after a 5 ms head start; verified deterministic across 10 consecutive runs on the dev box). Includes added: `<thread>` (test-only — allowed by `rules/critical-rules.md` C2) plus four asio cancellation/co_spawn headers already pulled in transitively. `docs/design-docs/tool-runtime.md` records the slice-33 status note. `docs/exec-plans/tech-debt-tracker.md` strikes the cancellation-polling bullet from the `deep-review-2026-05-21` P0 list. Slice version bumped 32 → 33. Driven by `orangutan-deep-review.md` §4.1.3. | [history](../histories/2026-05/20260521-2231-tool-file-search-cancellation.md) |
| 2026-05-21 | tool-io-atomic-write | `file.edit` and the dominant `file.write` mode (`truncate`) are now crash-safe. The deep-review BUG-4.1.1 footgun — `file.edit` composing `io::read_text_file` + `io::write_text_file` with no rollback, so a mid-write failure (signal, ENOSPC, process tear-down) left the target truncated or partially overwritten — is closed by routing the rewrite through a new `oran-io::WriteTextOptions::atomic` opt-in: contents are staged to a sibling `.<name>.orangutan.tmp.<seq>` in the target's parent directory and committed via `std::filesystem::rename`, atomic on POSIX same-filesystem rename(2). A crash between the open and the rename leaves the original target intact; the temp leftover is removed on every error path. The atomic helper rejects `append` and `fail_if_exists` with `invalid_argument` because temp-then-rename has no coherent semantics for either; `file.write` keeps its current semantics on those modes. `xmake run orangutan` now reports slice 32; the CLI surface is unchanged. | `include/oran/io/file.hpp` — added `bool atomic` to `WriteTextOptions` with a docstring linking the POSIX rename(2) rationale and the truncate-only restriction. `src/oran-io/file.cpp` — new `atomic_temp_path` (process-local atomic counter + leading-dot leaf so dotfile-skip directory listings hide the temp), `stream_write` (shared open+write+close), and `atomic_write_blocking` (write temp → rename → cleanup-on-error); refactored `write_text_file_blocking` to dispatch to the atomic helper when requested. `src/oran-tool/file_edit.cpp` always sets `atomic = true` on the rewrite hop; `src/oran-tool/file_write.cpp` sets `atomic = options.mode == WriteMode::truncate` so the dominant overwrite shape rides the safe path. `tests/io/test_file.cpp` grows to 16 cases / 70 assertions (+3 cases / +19 assertions: happy-path commit leaves no `.orangutan.tmp.*` sibling; append+atomic and fail_if_exists+atomic reject pre-IO with `invalid_argument`; commit failure with the target replaced by a directory preserves the directory AND removes the temp). `docs/design-docs/io-runtime.md` gains an "Atomic Writes" section + slice-32 status note; `docs/design-docs/tool-runtime.md` records the slice-32 wiring on `file.edit` / `file.write`. `docs/exec-plans/tech-debt-tracker.md` strikes the atomic-write bullet from the `deep-review-2026-05-21` P0 list. Slice version bumped 31 → 32. Driven by `orangutan-deep-review.md` §4.1.1. | [history](../histories/2026-05/20260521-2139-tool-io-atomic-write.md) |
| 2026-05-21 | rank-0-correctness-fixes | Three latent correctness bugs in the foundation libraries are closed before they get a chance to bite `oran-agent`. (1) `oran-async::Channel<T>` no longer drops a cancellation that fires in the gap between `get_associated_cancellation_slot` and queue push — the slot is now installed under the same mutex as the push, mirroring `storage::Pool::async_acquire_writer`. (2) `oran-async::Channel<T>` now honors `asio::get_associated_executor(handler, executor_)` for completion resumption, so a `co_await channel.receive()` from a strand-bound coroutine resumes on its strand instead of being silently posted to the channel's executor and racing other strand work. (3) `oran-hook::Bus::publish_advisory` now catches exceptions that escape a sink's awaitable and surfaces them as `PublishOutcome::SinkResult { error: Error::internal(...).with("sink", id) }` — the advisory contract documented in the bus header ("never aborts the publish for subsequent sinks") now matches actual behavior, so a throwing extension can no longer crash tool dispatch through its `[[maybe_unused]]` `tool_after` publish. Production docs that referred to "38 lifecycle events" are updated to "41" (5 agent + 4 provider + 4 tool + 6 memory + 6 channel + 7 orchestration + 4 automation + 2 session + 3 permission), matching the enum that grew in slices 22-25 without the prose following. `xmake run orangutan` now reports slice 31; the CLI surface is unchanged. | `include/oran/async/channel.hpp` — added `<asio/associated_executor.hpp>`; restructured `async_send` / `async_receive` so the cancellation slot is installed inside the mutex after the waiter push (with a docstring linking to the `storage::Pool` template); rewrote `make_send_complete` / `make_receive_complete` to capture `asio::get_associated_executor(handler, executor_)` instead of `executor_`. `src/oran-hook/bus.cpp` — wrapped the per-sink `co_await sink->receive(...)` in try/catch with `Error::internal(ex.what()).with("sink", id)` capture for `std::exception` and a fallback for non-std throws. `tests/async/test_async.cpp` grows to 9 cases / 43 assertions (+1 case / +5 assertions: a `strand_a`-hosted channel with a `strand_b`-spawned receiver coroutine that asserts the continuation lands on `strand_b`). `tests/hook/test_bus.cpp` grows to 15 cases / 97 assertions (+1 case / +18 assertions: a new `ThrowingSink` that raises `std::runtime_error` from its awaitable, plus a regression case asserting throwing sinks are captured as `Error::internal` with the offending sink id in context and that the middle sink between two throwers still fires). `docs/ARCHITECTURE.md`, `docs/QUALITY_SCORE.md`, and `docs/design-docs/permissions-and-hooks.md` updated `38` → `41` and noted the slice-31 exception-catch hardening on the `oran-hook` row. Slice version bumped 30 → 31. Driven by `orangutan-deep-review.md` §9.1, §9.2, and §9.5 (rank-0 list). | [history](../histories/2026-05/20260521-0045-channel-bus-rank0-fixes.md) |
| 2026-05-20 | tool-file-delete | `oran-tool` ships a sixth built-in: `file.delete` (registered via `tool::register_file_delete` / `tool::register_builtins`), built on a new `oran-io::delete_file(executor, path)` coroutine helper. Capability `core::Capability::delete_path` (first built-in that exercises this slice-7 capability). Input `{"path": <string>}`; the io helper refuses every path that is not a regular file with `invalid_argument` (covers both directories AND symlinks even when the symlink points at a regular file — the v1 surface is deliberately narrow so a recursive delete or a symlink-follow cannot escape the workspace), returns `not_found` when no entry exists at `path` (and on the rare "vanished between stat and unlink" race so the caller sees a single end-state error kind), and on success returns the literal text `deleted <path>` so the agent loop can quote a concrete fact without parsing a structured payload. Future direction for filesystem mutation built-ins is consolidation into a single delete tool covering files AND folders, not per-kind splits. `xmake run orangutan` now reports slice 30; the CLI surface is unchanged. | New `src/oran-tool/file_delete.cpp` (~75 LoC; JSON parse → `io::delete_file` call → `deleted <path>` success text). `kFileDeleteName` + `register_file_delete` land in `include/oran/tool/builtins.hpp`; `src/oran-tool/builtins.cpp` wires it into the aggregate `register_builtins`. `include/oran/io/file.hpp` declares `delete_file`; `src/oran-io/file.cpp` adds `delete_file_blocking` (refuse-non-regular + `std::filesystem::remove` + classify-race-as-not-found) and the `run_blocking`-wrapped public entry point. `tests/io/test_file.cpp` grows to 13 cases / 51 assertions (+5 cases / +18 assertions covering happy delete, empty-path rejection, missing file `not_found`, directory refusal `invalid_argument`, and the symlink refusal that skips gracefully when the test filesystem does not allow `create_symlink`). `tests/tool/test_registry.cpp` bumps the catalog-order test to 6 entries and adds ~145 LoC across seven cases: registration surface, happy path with the `deleted <path>` success message, missing path, directory refusal, symlink refusal, input-validation matrix, and deny-verdict short-circuit (asserts the file *survives* the permission-denied call). No per-tool bench scenario — perf is dominated by `unlinkat`. Slice version bumped 29 → 30. | [history](../histories/2026-05/20260520-2345-oran-tool-file-delete.md) |
| 2026-05-20 | tool-directory-list | `oran-tool` ships a fifth built-in: `directory.list` (registered via `tool::register_directory_list` / `tool::register_builtins`). It enumerates the immediate children of a directory through `oran-io::list_directory` under a new `core::Capability::list_directory` capability so a permission rule can distinguish "list a directory" from "read a file" (useful in sandbox modes where the agent should see the shape of a tree without reading content). Input `{path, include_hidden?, max_entries?}` (defaults: `include_hidden=false`, `max_entries=256`); output is one `<path>:<kind>:<size_bytes or '-'>` line per entry sorted by path (the order `oran-io::list_directory` already enforces), the literal text `no entries` for an empty directory after the hidden filter, and the `io: directory entry limit exceeded` error propagated verbatim when the directory exceeds `max_entries` — raise the cap and retry. `kind` is the `io::DirectoryEntryKind` wire spelling (`regular_file` / `directory` / `symlink` / `other`); `size_bytes` is a decimal integer for regular files and `-` for every other kind. `xmake run orangutan` now reports slice 29; the CLI surface is unchanged. | New `src/oran-tool/directory_list.cpp` (~120 LoC; JSON parser → typed options → `io::list_directory` call → colon-delimited render). `kDirectoryListName` + `register_directory_list` declarations land in `include/oran/tool/builtins.hpp`; `src/oran-tool/builtins.cpp` wires it into the aggregate `register_builtins`. `include/oran/core/capability.hpp` adds `Capability::list_directory` after `delete_path` (the file-system block stays contiguous; `read_file` keeps `enum_values<Capability>()` `.front()` and `runtime_loader` keeps `.back()`). `tests/core/test_capability.cpp` adds the new enumerator row and bumps `STATIC_REQUIRE(all.size() == 20)`; `tests/tool/test_registry.cpp` bumps the catalog-order test (5 entries) and adds ~190 LoC across eight new cases: registration surface, happy path, empty directory, hidden-file toggle, oversize directory, missing directory, regular-file target, and the input-validation matrix. No per-tool bench scenario — the perf surface is identical to the existing `bench/io` coverage of `list_directory`. Slice version bumped 28 → 29. | [history](../histories/2026-05/20260520-2307-oran-tool-directory-list.md) |
| 2026-05-20 | build-check-compile-budget | `scripts/check-compile-budget.sh` is now a real regression gate (was a stub) and `compile_budget.json` is the new versioned baseline at the repository root. The script drives `scripts/measure-tu.sh --json`, joins each TU against its library's `{median_sec, p95_sec, hard_cap_sec}` row via a single jq pass, classifies into `ok` / `warn` / `fail` / `undef`, prints a summary line (`check-compile-budget: N TUs (N ok, N warn, N fail, N undef)`), and exits 1 on any hard-cap exceedance (matching the `docs/rules/compile-budget.md` contract). Two flags: `--show-all` (print greens) and `--no-strict` (demote fail to warning during calibration). The baseline JSON carries all 20 documented libraries plus 9 per-phase whole-build targets; numbers mirror `compile-budget.md` verbatim so the doc stays the single source of truth. Not wired into `scripts/ci.sh` yet — CI does not provision xmake and the documented reference hardware (8-core NVMe native) is not every runner; a new tech-debt row tracks the wiring. First local run on WSL2: 21 ok / 1 warn / 16 fail / 0 undef — the fails reflect non-reference hardware, not regressions. `xmake run orangutan` now reports slice 28. Closes the slice-28 half of the build-skeleton-scripts row in `exec-plans/tech-debt-tracker.md`. | New `compile_budget.json` (~60 lines; `schema_version: 1`, top-of-file `$comment` array, `categories` for 20 libs, `targets` for 9 phases). New real implementation of `scripts/check-compile-budget.sh` (~150 LoC bash; `--no-strict` / `--show-all` flags, preflight checks, single jq join pass, four-bucket classifier, exit-code dispatch). `docs/exec-plans/tech-debt-tracker.md` build-skeleton-scripts row removed; new CI-wiring row added. `docs/QUALITY_SCORE.md` Compile-time discipline row updated with the slice-28 callout and the local measurement result. `docs/STATUS.md` slice 28, history pointer, refreshed Next intended slice. Slice version bumped 27 → 28. | [history](../histories/2026-05/20260520-2220-check-compile-budget-gate.md) |
| 2026-05-20 | build-measure-tu | `scripts/measure-tu.sh` is now a real per-TU compile-time emitter (was a stub). It reads `compile_commands.json`, filters to `src/oran-*/*.cpp` (library TUs — tests / benches / `src/main.cpp` excluded), iterates each entry, deletes the cached `.o`, re-runs the recorded compile command, and times the wall clock with bash's `EPOCHREALTIME` (microsecond resolution; no GNU `time` binary dependency). Default output is a human-readable slowest-first table; `--json` emits an array of `{library, file, seconds}` ready to feed the paired `scripts/check-compile-budget.sh` slice. Pre-conditions are documented at the top of the script: `xmake f -m release` (so flags match the budget contract) and one prior `xmake build` (so each library's PCH exists). RSS per TU is not measured (no portable `time` binary across CI); follow-up if a memory-budget regression appears. First local baseline on WSL2 / GCC 16.1 / release-mode-LTO surfaced several library TUs already exceeding the documented hard caps in [`docs/rules/compile-budget.md`](../rules/compile-budget.md) — most notably `oran-config/config.cpp` (8.6 s vs. 2.5 s cap) and the four `oran-tool/file_*` TUs (6.5–7.4 s vs. 3.0 s cap); the next slice's `check-compile-budget.sh` will let reviewers see these deltas explicitly rather than silently inheriting them. `xmake run orangutan` now reports slice 27. Narrows the build-skeleton-scripts row in `exec-plans/tech-debt-tracker.md` (the remaining stub is `check-compile-budget.sh`). | New real implementation of `scripts/measure-tu.sh` (~170 LoC bash; `--json` flag, jq-driven extraction of the per-TU arg list from `compile_commands.json`, `EPOCHREALTIME`-bracketed timer, slowest-first sorted output). `docs/exec-plans/tech-debt-tracker.md` row narrowed to the still-stub `check-compile-budget.sh`. `docs/QUALITY_SCORE.md` Compile-time discipline row updated with the slice-27 callout and the empirical baseline range. `docs/STATUS.md` Next intended slice refreshed. Slice version bumped 26 → 27. | [history](../histories/2026-05/20260520-2204-measure-tu-script.md) |
| 2026-05-20 | build-check-deps | `scripts/check-deps.sh` is now a real CI gate (was a stub). It parses `xmake/targets.lua`, extracts each `oran-X` library's `add_deps(...)` list, and validates against the layering documented in `docs/design-docs/module-boundaries.md`: a library may depend only on libraries strictly below its layer; sibling (same-layer) deps require an entry in the script's `ALLOWED_SIBLING` allowlist (mirroring `docs/ARCHITECTURE.md`). Upward deps and undocumented sideways deps fail the build with a single descriptive line each. The gate runs in every `make ci` invocation; the current state passes cleanly with five documented sibling exceptions (`io→async`, `storage→async`, `config→storage`, `tool→permission`, `tool→hook`). `xmake run orangutan` now reports slice 26. Closes the slice-26 half of the build-skeleton-scripts row in `exec-plans/tech-debt-tracker.md`. | New real implementation of `scripts/check-deps.sh` (~120 LoC bash; `LAYER` table for the six-layer hierarchy, `LAYER_NAME` for human-readable failure text, `ALLOWED_SIBLING` for documented same-layer exceptions, a `grep -oE`-based extractor for the quoted `oran-X` tokens inside each `oran_lib(...)` call). `scripts/ci.sh` invokes it alongside the other base checks. `docs/exec-plans/tech-debt-tracker.md` row narrowed to the still-stub `measure-tu.sh` + `check-compile-budget.sh`. `docs/QUALITY_SCORE.md` Build system row gains the slice-26 callout. `docs/STATUS.md` Next intended slice refreshed. Slice version bumped 25 → 26. | [history](../histories/2026-05/20260520-2030-check-deps-script.md) |
| 2026-05-20 | oran-tool-lifecycle-events | `tool::Registry::dispatch` now publishes the full four-event tool lifecycle on the optional `hook::Bus`: `tool_before` (slice 22, after resolution), `tool_dispatched` (slice 25, between audit success and the handler `co_await` on the paths where the handler will actually run — `allow` or ask-approved — payload carries `(tool_name, input_json, who, started_at, verdict)` so sinks can distinguish a free allow from an approved ask without subscribing to the audit pipeline), `tool_error` (slice 25, alongside `tool_after` on every error exit as a failure-only narrow channel for sinks that don't want the `succeeded` filter dance), and `tool_after` (slice 22, at every exit). All four events stay advisory — sink errors are captured in the `PublishOutcome` but cannot change the dispatch result. The `tool_dispatched` publish is suppressed on deny / ask-short-circuit / broker-reject / audit-error paths so subscribers see only the calls whose handlers actually execute. `xmake run orangutan` now reports slice 25. Closes the `tool_dispatched`/`tool_error` row in `exec-plans/tech-debt-tracker.md`. | `include/oran/hook/payload.hpp` adds `ToolDispatchedPayload` (`tool_name`, `input_json`, `who`, `started_at`, `verdict` — verdict carries the `permission::Verdict` enum's wire spelling so an `ask` rule promoted by the broker shows as `verdict="ask"` with the broker promotion visible on the audit row's `outcome="approved"`) and `ToolErrorPayload` (`tool_name`, `input_json`, `who`, `error_kind`, `error_message`, `started_at`, `finished_at`, `duration`) plus extends the `Payload` variant. `src/oran-tool/registry.cpp` adds `build_dispatched_payload` / `build_error_payload` helpers, the `handler_about_to_run` branch that gates `tool_dispatched`, and the failure-only `tool_error` publish that shares `finished_at` with the existing `tool_after` publish. `tests/tool/test_registry.cpp` grows to 74 cases / 599 assertions (+11 cases / +83 assertions: dispatched on allow with `verdict=allow`, dispatched suppressed on deny / ask-short-circuit / broker-reject, dispatched on ask-approved with `verdict=ask`, error on handler failure / deny / ask-short-circuit / broker-reject, error suppressed on allow happy path, and a four-event ordering case asserting before → dispatched → error → after under a failing handler). `tests/hook/test_bus.cpp` extends the `payload_kind` visitor with the two new variant alternatives; existing bus tests remain green. `bench-tool` numbers within noise: `dispatch_allow_no_hooks` ~2.0 µs, `dispatch_allow_with_empty_bus` ~2.5 µs (+~85 ns vs. slice 22 — the third no-op publish), `dispatch_allow_with_two_sinks` ~2.9 µs. Slice version bumped 24 → 25. | [history](../histories/2026-05/20260520-1900-tool-lifecycle-events.md) |
| 2026-05-20 | oran-tool-file-search-regex | `file.search` accepts a new `"regex"?: bool (default false)` input field. With `regex=true` the `pattern` is compiled as a re2 expression and each line is matched via `RE2::PartialMatch`; invalid patterns surface as `invalid_argument` with the re2 error message attached as `regex_error` plus the offending `pattern`. The default path is unchanged (literal substring), so existing callers see no behavior change. `xmake run orangutan` now reports slice 24. Closes the regex row in `exec-plans/tech-debt-tracker.md`. | `src/oran-tool/file_search.cpp` introduces a per-call `LineMatcher` (literal `string_view` or owned `permission::InputPattern`), parses the new field in `parse_input`, and compiles the regex once in `walk_and_scan` before the file/tree walk. The schema and tool description advertise the new option. `oran-tool` reuses `permission::InputPattern` (which forward-declares `re2::RE2` per rule C6 so the dispatch TU stays off `<re2/re2.h>`); no new dependencies. `tests/tool` grows to 63 cases / 516 assertions (+5 cases for regex happy on a single file, regex happy on a recursive walk, invalid-regex `invalid_argument` carrying `regex_error` context, regex + max_matches truncation, and explicit `regex=false` pinning the literal path; +1 entry in the malformed-input table for `regex` non-boolean). `bench-tool` adds `file_search.literal_match_1kib` (~9.2 µs) vs. `file_search.regex_match_1kib` (~18.6 µs) on a 1 KiB / 32-line seed — the ~9.4 µs A/B delta is the re2 compile + per-line PartialMatch cost the agent loop pays when it opts in. Slice version bumped 23 → 24. | [history](../histories/2026-05/20260520-1830-file-search-regex.md) |
| 2026-05-19 | bootstrap-signal-aware-shutdown | Ctrl-C and `kill` now interrupt `orangutan --audit-init` (and every future one-shot `io_context` drain that adopts the new helper) instead of leaving the process running until the drain naturally completes. The process exits with the shell-conventional `128 + signum` code — `130` for SIGINT, `143` for SIGTERM — and prints `orangutan: interrupted by <name> (<num>)` to stderr. The success path is unchanged: `--audit-init` still applies the migration idempotently and exits 0. `xmake run orangutan` now reports slice 23. | New public surface in `<oran/bootstrap/signal_drain.hpp>`: `SignalScope` (RAII `asio::signal_set` wrapper with `release()` + `signum()`), `signal_name(int)`, and `signum_from_error(const Error&)`. `bootstrap::run_audit_init` constructs the scope around its one-shot `io_context`; the migration coroutine calls `release()` at its tail so `io.run()` returns naturally on success and stops via `io.stop()` on signal delivery. `bootstrap::run` translates the resulting `cancelled` error (with `signal=<name>` + `signum=<int>` context entries) into a `Result<int>(128+signum)` and prints the interrupted-by line to stderr. `tests/bootstrap` grows to 44 cases / 140 assertions (+10 cases / +17 assertions covering empty drain, pending work, in-process SIGTERM/SIGINT raise, `release()` idempotence, `signum_from_error` happy + reject-paths, `signal_name` table). New `bench-bootstrap/signal_drain_with_scope` ~11.2 µs vs. `signal_drain_bare` ~443 ns — the ~10.7 µs delta is the per-drain `asio::signal_set` install + `release()` cost, small relative to the existing `assembly_build_with_audit` ~205 µs. Slice version bumped 22 → 23. | [history](../histories/2026-05/20260519-2350-bootstrap-signal-aware-shutdown.md) |
| 2026-05-18 | oran-hook-foundation-and-dispatch-wiring | `oran-hook` is now a real library, and `oran-tool::Registry::dispatch` taps it. `hook::Event` enumerates the 38 lifecycle events the design contemplates (agent / provider / tool / memory / channel / orchestration / automation / session / permission); `hook::Mode` annotates each as `advisory` or `blocking` via `default_mode(Event)`. `hook::Sink` is the abstract base; `hook::InProcessSink` is the first concrete implementation (a `std::function<Awaitable<Result<void>>(Event, Payload)>`). `hook::Bus` exposes `bind` / `unbind` / `publish_advisory(Event, Payload)` — the bus iterates subscribed sinks in subscription order, captures each sink's `Result<void>` in `PublishOutcome::SinkResult { sink_id, error }`, and never aborts the publish on a sink error (advisory contract). `tool::DispatchContext` grew an optional `hook::Bus* bus{nullptr}` field; when non-null, `Registry::dispatch` publishes `hook::Event::tool_before` after the registry resolves the tool def (so a sink sees every known call attempt) and `hook::Event::tool_after` at every exit (handler success, permission deny, broker rejection, audit error) with a `ToolAfterPayload { succeeded, output_text, error_kind, error_message, started_at, finished_at, duration }` that flattens the dispatch outcome. Hooks are advisory in this slice — sinks observe but cannot veto; blocking semantics with veto are tracked in `exec-plans/tech-debt-tracker.md`. Unknown tool names are silently rejected (no hook publish — the dispatch never started). `xmake run orangutan` now reports slice 22; the CLI surface is unchanged. | New library `oran-hook` (`include/oran/hook/{event,payload,sink,bus,in_process_sink}.hpp` + the facade + `src/oran-hook/{bus,in_process_sink}.cpp`). `xmake/{targets,tests,bench}.lua` add the `oran-hook` library + `test-hook` + `bench-hook` targets and grow `oran-tool`'s deps to include it. `include/oran/tool/registry.hpp` adds the `bus` context field, includes `<oran/hook/bus.hpp>`, refreshes the file header, and rewrites the dispatch-contract docstring to document the seven-step flow (lookup → tool_before → eval → broker → audit → branch → tool_after). `src/oran-tool/registry.cpp` rewrites `dispatch` so every exit path lands a single `result` variable used by the `tool_after` publish + the final `co_return`. `tests/hook` (new bucket) ships 14 cases / 79 assertions covering bus bind/unbind idempotence, 0/1/3-sink fan-out, sink-error swallowing, subscription order, monostate payloads, default-mode mapping, and InProcessSink propagation. `tests/tool` grows to 58 cases / 477 assertions (+8 cases / +63 assertions for the slice-22 paths: tool_before+tool_after on allow, tool_after error_kind on deny, tool_after with handler internal error, unknown-tool silent path, failing-sink advisory swallow, null-bus backwards-compat, ask short-circuit publish, broker-reject publish). `bench-hook` ships `publish_no_sinks` (~242 ns) vs. `publish_one_sink` (~446 ns) vs. `publish_three_sinks` (~698 ns); `bench-tool` adds `dispatch_allow_no_hooks` (~2.1 µs) vs. `dispatch_allow_with_empty_bus` (~2.4 µs) vs. `dispatch_allow_with_two_sinks` (~3.0 µs) — bus overhead is small relative to the audit-record cost. Two tech-debt rows filed: advisory-only dispatch semantics; `tool_dispatched` + `tool_error` events enumerated but not yet published. Slice version bumped 21 → 22. | [history](../histories/2026-05/20260518-1100-oran-hook-foundation-and-dispatch-wiring.md) |
| 2026-05-17 | oran-tool-approval-broker | `oran-tool` now mediates `Verdict::ask` decisions through `permission::ApprovalBroker` instead of always short-circuiting. `tool::DispatchContext` grew three optional fields — `approval_broker: ApprovalBroker*`, `approval_token: const ApprovalToken*`, and `now: core::Time` — and `Registry::dispatch` consults them when a rule fires `ask`: success on `broker.check(token, name, input, identity, now)` promotes the audit outcome from `ask` to `approved` and runs the handler; failure demotes it to `rejected`, swaps the audit row's `reason` from the rule reason to the broker's `reason` context entry (`expired` / `tool_mismatch` / `identity_mismatch` / `input_mismatch` / `mac_mismatch` / `no_grant` / `replay_exhausted`), and forwards the broker's error to the caller. When the agent does not yet have a token (or chose not to supply the broker), the slice-17 short-circuit applies — `outcome=ask`, `permission_denied` with `reason=approval_required` — but the error now also carries `decision_reason` + `replay_max` + `approval_ttl_seconds` copied from the matched rule so the agent loop can hand them straight to `ApprovalBroker::approve` without re-running `RuleSet::evaluate`. Allow/deny verdicts ignore the broker entirely. `xmake run orangutan` now reports slice 21; the CLI surface is unchanged. | `include/oran/tool/registry.hpp` adds the three context fields, pulls in `<oran/permission/approval.hpp>` + `<oran/permission/approval_broker.hpp>` + `<oran/core/time.hpp>`, and refreshes the dispatch-contract docstring. `src/oran-tool/registry.cpp` adds a `broker_reason` helper + the verdict=ask broker branch + the new `replay_max` / `approval_ttl_seconds` / `decision_reason` error-context entries on the short-circuit path. `tests/tool/test_registry.cpp` grows to 50 cases / 414 assertions (+10 cases: the existing ask test now asserts the new context entries, a new custom-`replay_max`/`approval_ttl_seconds` propagation case, broker-approved happy path, four broker-rejection paths — `replay_exhausted`, `no_grant`, `expired`, cross-tool `tool_mismatch` — broker-without-token short-circuit, allow/deny ignore tokens, and an end-to-end ask → approve → re-dispatch → exhaust integration). `bench-tool` adds `bench/tool/scenarios/approval.cpp` with `dispatch_ask_short_circuit` (~2.4 µs) vs. `dispatch_ask_approved` (~13.4 µs) vs. `dispatch_ask_rejected` (~13.6 µs) — the broker-attached path costs ~11 µs over the short-circuit, ~10 µs of which is the HMAC verify from the underlying `bench-permission/approval_broker` numbers (~10.7 µs `broker_check_ok`), so the registry-side overhead on top of the broker work is ~875 ns. Slice version bumped 20 → 21. | [history](../histories/2026-05/20260517-2359-oran-tool-approval-broker.md) |
| 2026-05-17 | oran-tool-file-search | `oran-tool` grows the fourth built-in: `file.search` (registered via `tool::register_file_search` / `tool::register_builtins`). It accepts JSON `{"path": <string>, "pattern": <string>, "max_matches"?: uint (default 100), "include_hidden"?: bool (default false)}` and scans either a single UTF-8 text file or (recursively) a directory tree for literal substring matches under `Capability::read_file`. Recursive walks use `std::filesystem::recursive_directory_iterator` under `skip_permission_denied`, skip files containing NUL bytes in their first 8 KiB (ripgrep-style binary heuristic), skip dot-prefixed files and directories when `include_hidden=false`, and skip symlinks wholesale (defensive default against loops). Single-file mode does not apply the binary heuristic because the caller named the file explicitly. Output is one `path:line:text` line per match (newline-separated), with a trailing `(truncated; matches capped at <N>)` summary when the cap is hit; zero matches return the literal text `no matches` (non-error). Truncation is detected by scanning to `max_matches + 1` and resizing down — the single-extra-match overshoot makes "exactly max" vs. "more were available" mathematically unambiguous. Regex support and ripgrep-class optimisations (mmap, extension-based binary skip, `.gitignore`, multi-threaded walk) are deferred to follow-up slices tracked in `exec-plans/tech-debt-tracker.md`; the MVP is sufficient for pre-v1 agent workloads. `xmake run orangutan` now reports slice 20; the CLI surface is otherwise unchanged. | New `src/oran-tool/file_search.cpp` and `kFileSearchName`/`register_file_search` symbols in `include/oran/tool/builtins.hpp`. `register_builtins` now seeds the catalog with `file.read` then `file.write` then `file.edit` then `file.search`. `tests/tool` grows to 40 cases / 309 assertions (+9 cases: schema/capability advertisement, single-file happy path with line number, recursive walk over a 3-file / 4-match tree, max_matches truncation + summary, binary NUL-skip, include_hidden=false default vs. true opt-in, zero-match `no matches` text, missing-path not_found, 9-shot malformed-input table including empty-pattern and max_matches=0 rejections). `bench-tool` adds `file_search.single_file_one_match` (~8.2 µs) vs. `file_search.recursive_dir_many_matches` (~27.6 µs) on a 4-file / 14-line tree — the ~19 µs A/B delta is the `recursive_directory_iterator` walk + per-file open/read overhead the agent loop pays when a tool call reaches for a tree. Two new tech-debt rows filed: regex support deferred, ripgrep-class optimisations deferred. Slice version bumped 19 → 20. | [history](../histories/2026-05/20260517-2330-oran-tool-file-search.md) |
| 2026-05-17 | oran-tool-file-edit | `oran-tool` grows the third built-in: `file.edit` (registered via `tool::register_file_edit` / `tool::register_builtins`). It accepts JSON `{"path": <string>, "old_string": <string>, "new_string": <string>, "replace_all"?: bool (default false)}`, reads the file via `io::read_text_file`, scans for non-overlapping occurrences of `old_string`, rebuilds the contents around them, and writes back via `io::write_text_file` under `Capability::edit_file`. By default the call refuses with `Error::conflict` (`match_count` carried in the error context) when `old_string` appears more than once; passing `replace_all=true` rewrites every match. Empty `old_string` or `old_string == new_string` reject as `Error::invalid_argument`; missing-substring rejects as `Error::not_found` so an editing LLM sees a clear "no match here" signal rather than a silent no-op. Success text is `edited <path>: N replacement(s)` so the agent loop can quote a concrete fact without parsing structured output. `xmake run orangutan` now reports slice 19; the CLI surface is otherwise unchanged. | New `src/oran-tool/file_edit.cpp` and `kFileEditName`/`register_file_edit` symbols in `include/oran/tool/builtins.hpp`. `register_builtins` now seeds the catalog with `file.read` then `file.write` then `file.edit`. `tests/tool` grows to 31 cases / 227 assertions (+7 cases: schema/capability advertisement, happy unique-replacement with count-1 wording, replace_all multi-rewrite with count-N wording, non-unique conflict with `match_count` context, not_found on missing substring, not_found on missing file, eight-shot malformed-input table covering empty-old-string and identical-old-new no-op rejections). `bench-tool` adds `file_edit.dispatch_unique_replace` (~15.1 µs) vs. `file_edit.dispatch_replace_all_many` (~16.1 µs) on a 1 KiB seed — the ~1 µs A/B delta confirms 63 extra substitutions cost less than ~16 ns each over the shared read+write floor. Slice version bumped 18 → 19. | [history](../histories/2026-05/20260517-2300-oran-tool-file-edit.md) |
| 2026-05-17 | oran-tool-file-write | `oran-tool` grows the second built-in: `file.write` (registered via `tool::register_file_write` / `tool::register_builtins`). It accepts JSON `{"path": <string>, "content": <string>, "mode"?: "truncate"|"append"|"fail_if_exists" (default truncate), "create_parents"?: bool (default false)}` and writes via `io::write_text_file` under `Capability::write_file`. On success the tool emits a compact text confirmation (`wrote N bytes to <path>`) so the agent loop can quote disk-write progress back to the user without parsing a structured payload. Mode and create-parents arguments map directly onto `io::WriteTextOptions`; conflicting writes under `fail_if_exists` surface as `Error::conflict`; malformed input (bad JSON, missing/wrong-type fields, unknown mode string, non-bool create_parents) rejects as `Error::invalid_argument` with the offending value in the error context. `xmake run orangutan` now reports slice 18; the CLI surface is otherwise unchanged. | New `src/oran-tool/file_write.cpp` and `kFileWriteName`/`register_file_write` symbols in `include/oran/tool/builtins.hpp`. `register_builtins` now seeds the catalog with `file.read` then `file.write`. `tests/tool` grows to 24 cases / 161 assertions (+7 cases: schema/capability advertisement, happy-path write with size-in-text, default truncate overwrites, append, fail_if_exists conflict, create_parents creates missing dirs, the eight-shot malformed-input table). `bench-tool` adds `file_write.dispatch_truncate` (~12.4 µs) vs. `file_write.dispatch_append` (~12.0 µs) showing the IO-mode choice is within noise at the 64-byte payload that dominates ordinary tool output (existing `registry.lookup` ~8 ns and `registry.dispatch_allow` ~2,000 ns are unchanged). `xmake/tests.lua` grew an optional packages arg on `oran_test` so `test-tool` can see `nlohmann_json` for the test-side JSON authoring. Slice version bumped 17 → 18. | [history](../histories/2026-05/20260517-2100-oran-tool-file-write.md) |
| 2026-05-17 | oran-tool-registry-file-read | The runtime now has a tool registry. `tool::Registry` (in the new `oran-tool` library) lets a caller register a `core::ToolDef` + a coroutine handler and then `co_await registry.dispatch(name, input_json, ctx)` — the dispatch walks one call through `permission::RuleSet::evaluate(name, input_json, def.required_capabilities, ctx.mode)`, records one `permission::AuditEvent` with `input_hash = SHA-256(input_json)` onto the supplied `permission::AuditSink`, then branches: `allow` invokes the handler; `deny` returns `Error::permission_denied`; `ask` returns `Error::permission_denied` with `reason=approval_required` (the approval-broker flow lands in a later slice). First built-in `file.read` registers via `tool::register_file_read` / `tool::register_builtins`; it reads a UTF-8 file from a path-shaped JSON input (`{"path": <string>}`) under `Capability::read_file`. `core::ToolDef` grew `required_capabilities` (the design-doc `requires` field, renamed because `requires` is a reserved C++20 keyword). `xmake run orangutan` now reports slice 17. The binary's CLI surface is unchanged — the upcoming agent loop is what consumes the registry. | New library `oran-tool` (`include/oran/tool/{registry,builtins}.hpp` + `include/oran/tool.hpp` + `src/oran-tool/{registry,file_read,builtins}.cpp`); new public types `tool::Output`, `tool::DispatchContext` (references the executor, mode, `RuleSet&`, `AuditSink&`, and the per-call scope/agent/identity strings), `tool::Handler`, `tool::Registry`. `tests/tool` ships 17 cases / 97 assertions covering add/remove/find/catalog/dispatch (unknown tool, allow path with input-hash equality vs. `ApprovalAuthority::input_hash`, deny path, ask path with `reason=approval_required`, capability scope match-vs-miss under `Mode::strict`, sink-error propagation), `register_file_read`, `register_builtins`, and `file.read` (happy / malformed JSON / missing path / non-string path / not-found path). New `bench-tool` bucket: `registry.lookup` ~7.77 ns vs. `registry.dispatch_allow` ~2,048 ns (overhead dominated by the libsodium SHA-256 input hash + coroutine spawn + audit record). Slice version bumped 16 → 17. | [history](../histories/2026-05/20260517-1930-oran-tool-registry-file-read.md) |
| 2026-05-17 | bootstrap-explain-rules-mode-agent | `orangutan --explain-rules` now accepts `--mode <strict\|default\|permissive\|sandboxed>` and `--agent <name>` selectors so operators can preview any mode against any configured agent before the agent loop ever runs. Unknown mode spellings reject as `Error::invalid_argument`; unknown agent names reject as `Error::not_found` with the offending value attached. The unflagged form is unchanged (default mode, no overlay). The output header now reads `materialized rules (mode=<m>[, agent=<name>]): N total`, with the agent suffix only present when the overlay was selected. `xmake run orangutan` now reports slice 16. | New public surface on `<oran/bootstrap/bootstrap.hpp>`: `ExplainRulesSelector`, `parse_explain_rules_selector(args)`, and `materialize_rules(cfg, selector)`. `oran-bootstrap` argument parsing gains `--mode` / `--agent` (space-separated and `=`-form) with a shared `consume_value` helper; the parser uses `core::parse_enum<permission::Mode>` so the wire spellings stay generic. `tests/bootstrap` grows to 34 cases / 123 assertions (+13 cases / +45 assertions covering selector parsing, materialize behaviour, and end-to-end `run` paths including the unknown-mode / unknown-agent rejection invariants). No new bench bucket: the new path runs once per invocation and `permission::materialize` already has bench coverage. `--help` text refreshed to document the new flags. | [history](../histories/2026-05/20260517-1720-bootstrap-explain-rules-mode-agent.md) |
| 2026-05-17 | bootstrap-audit-migration-packaging | `orangutan` now provisions `<workspace>/.orangutan/audit.db` regardless of CWD: `bootstrap::run` defaults the `RuntimeAssembly` to `audit_enabled=true` because `oran-storage` ships its audit + sessions migrations compile-time-embedded via C++26 `#embed`. Operators no longer need to launch from inside the source tree, and the migration assets stay in sync with the canonical `migrations/<kind>/*.sql` files — those `.sql` files are the input the compiler embeds. The `--audit-init` operator command and the explicit `migrations_directory` override path are unchanged. `xmake run orangutan` now reports slice 15. | New `src/oran-storage/migration_assets.cpp` exposing `storage::built_in_audit_migrations()` / `storage::built_in_session_migrations()` (slice-local accessors declared in `include/oran/storage/migrations.hpp`). `AuditRepository::migrate` and `SessionRepository::migrate` use the built-ins by default and fall back to disk only when `migrations_directory` is non-empty. The closed tech-debt row "Audit migrations directory is found by walking up from CWD" is removed from `docs/exec-plans/tech-debt-tracker.md`. `bootstrap::run` drops the `audit_enabled=false` override and the explanatory CWD comment. `tests/bootstrap` grows to 21 cases / 78 assertions (+1 CWD-independence case using a `CwdGuard` to chdir to a tempdir and assert `RuntimeAssembly::build` still provisions the audit DB). Bench `assembly_build_with_audit` shifts from ~212 µs to ~202 µs (no directory scan); `assembly_build_without_audit` stays ~400 ns. | [history](../histories/2026-05/20260517-1610-bootstrap-audit-migration-packaging.md) |
| 2026-05-17 | bootstrap-runtime-assembly | `bootstrap::RuntimeAssembly` bundles the per-process permission infrastructure the upcoming agent loop will inherit — a fresh `permission::ApprovalBroker` (per-process HMAC key per `0008-permissions.md` criterion 5) plus the active `permission::AuditSink` (`StorageAuditSink` over an internal `Pool` + `AuditRepository` when `audit_enabled=true`, `NullAuditSink` otherwise). `bootstrap::run` builds the assembly on every startup (currently with `audit_enabled=false` so `orangutan` stays runnable from any CWD until the audit migration assets are packaged); the `--audit-init` operator command keeps the storage-backed path exercised. `xmake run orangutan` now reports slice 14. | New `include/oran/bootstrap/runtime_assembly.hpp` + `src/oran-bootstrap/runtime_assembly.cpp`; `bootstrap::run` builds the assembly using the loaded config's worker count for the `async::Runtime`; tests/bootstrap grows to 20 cases / 75 assertions (+9 cases for null-sink default, storage-sink default-path provisioning, explicit-path override, idempotent re-build, end-to-end record through the assembly's sink for both storage and null backends, broker round-trip, and broker cross-tool rejection); bench/bootstrap grows by one A-vs-B (`assembly_build_with_audit` ~212 µs vs. `assembly_build_without_audit` ~400 ns). The in-process half of criterion 1 is now wired end-to-end. | [history](../histories/2026-05/20260517-1430-bootstrap-runtime-assembly.md) |
| 2026-05-17 | bootstrap-audit-init | `orangutan --audit-init [<path>]` provisions the audit database before the agent loop is wired. Without a path argument it writes to `<workspace>/.orangutan/audit.db`; with one, the operator chooses the location. The command runs the full storage stack the future agent loop will use — one-shot `asio::io_context`, `storage::Pool`, `storage::AuditRepository::migrate()` — and is idempotent on re-run (a second invocation reports `0 migrations applied`). `0008-permissions.md` criterion 1 ("a tool call whose input matches a deny rule returns Error::permission_denied and is recorded in audit") is now closed at the infrastructure layer; the per-call record-on-decision wiring lands with the first tool built-ins or the agent loop scaffolding. `xmake run orangutan` now reports slice 13. | New bootstrap flag in `src/oran-bootstrap/bootstrap.cpp` with `--audit-init`, `--audit-init=<path>`, and `--audit-init <path>` shapes; `oran-bootstrap` now depends on `oran-async` + `oran-storage` directly (was transitive through `oran-config`/`oran-permission`). The flag creates any missing parent directories and surfaces storage / IO errors as `core::Result`. `tests/bootstrap` grows to 11 cases / 47 assertions (+3 cases for default-path, explicit-path-in-two-flag-shapes, and rejected-empty-path; +13 assertions). `--help` text and `--explain-rules` flow unchanged. The slice's three commits land in order: storage repository, permission sink, and bootstrap wiring + slice tag bump. | [history](../histories/2026-05/20260517-1100-audit-pipeline-and-bootstrap.md) |
| 2026-05-17 | audit-permission-sink | `oran-permission` now ships the audit-sink half of `0008-permissions.md` criterion 1. `permission::AuditEvent` captures a single decision (scope/agent/tool/identity/verdict/outcome/reason + optional 32-byte `input_hash` + free-form metadata) and `permission::AuditOutcome` (`allow`/`deny`/`ask`/`approved`/`rejected`) records the rendered post-broker state. The abstract `AuditSink::record(AuditEvent) -> Awaitable<Result<void>>` interface has three concrete implementations: `NullAuditSink` (the no-op default), `RecordingAuditSink` (in-memory capture for tests), and `StorageAuditSink` (writes column-for-column into `storage::AuditRepository::append_event`). `permission::verdict_to_outcome` / `permission::make_audit_event_from_decision` / `permission::to_hex` keep callsites from duplicating boilerplate. Bootstrap wiring + slice tag bump 12 → 13 land in the slice's third commit. | New public surfaces in `include/oran/permission/audit.hpp` (`AuditOutcome`, `AuditEvent`, `AuditSink`, `NullAuditSink`, `RecordingAuditSink`, plus the three free helpers and the `std::formatter<AuditOutcome>` specialization) and `include/oran/permission/storage_audit_sink.hpp` (`StorageAuditSink`). `oran-permission` now depends on `oran-storage` + `oran-async` directly (previously transitive via `oran-config` — making it explicit keeps the dep graph honest). `tests/permission` grows to 83 cases / 379 assertions (+10 cases: AuditOutcome wire-spelling round-trip, `verdict_to_outcome` mapping, decision-to-event helper, RFC-6234 SHA-256 hex vector pinning, Null/Recording/Storage sink behavior, NULL-hash adapter path, per-outcome storage round-trip, repository-error propagation). New `bench-permission/audit` bucket (~260 ns null sink, ~360 ns recording sink, ~18.1 µs storage sink end-to-end through SQLite, ~62 ns `to_hex` on 32 bytes). | [history](../histories/2026-05/20260517-1100-audit-pipeline-and-bootstrap.md) |
| 2026-05-17 | audit-db-storage | `oran-storage` now ships the `audit.db` foundation that the upcoming `permission::AuditSink` will write into — `audit_events` rows carry `scope_key`/`agent_key`/`tool_name`/`identity`/`verdict`/`outcome`/`reason` plus an optional `input_hash_hex` and a free-form `metadata_json`, indexed for scope-by-time, agent-by-time, and outcome-by-time queries. `AuditRepository` exposes typed `append_event`/`list_events`/`count_events` over the same async writer/reader `Pool` the sessions repository already uses, with a default `migrate()` that loads the SQL file from the source tree. This is the storage-layer half of `0008-permissions.md` criterion 1; the permission-side sink + bootstrap wiring land in the rest of this slice. `xmake run orangutan` still reports slice 12. | New public surface in `include/oran/storage/audit_repository.hpp`: `AppendAuditEventRequest`, `AuditEventRecord`, `ListAuditEventsOptions`, `AuditRepositoryOptions`, `AuditRepository`. New migration `src/oran-storage/migrations/audit/0001-audit-initial.sql`. `tests/storage` grows to 60 cases / 702 assertions (migrate, explicit-directory, round-trip, null-hash preserved, filter+order, validation, defensive read of NULL required field, empty scope). New `bench-storage/audit_repository` A/B (raw pool + cache append/count vs. repository append/list over a 64-event batch — ~992 µs raw vs. ~1219 µs repository, ~3.5 µs per event overhead). `oran-permission` interface + storage adapter + bootstrap wiring are upcoming commits in the same slice. | [history](../histories/2026-05/20260517-1100-audit-pipeline-and-bootstrap.md) |
| 2026-05-17 | permission-approval-broker | `oran-permission` now ships the stateful replay-window layer on top of the HMAC-signed approval tokens that landed earlier in the day. `permission::ApprovalBroker` wraps `ApprovalAuthority` and tracks `(tool, identity, input_hash)` triples with their remaining replay count + expiry, so after the operator says "yes" once, the same approval can ride identical follow-up calls up to the configured `replay_max` times before requiring a fresh approval. `0008-permissions.md` criterion 2 ("on approval, replay works within TTL for identical input") is now closed. Operators get `reason=no_grant` (broker missing the entry) and `reason=replay_exhausted` (counter at zero) on top of the authority's existing `expired`/`tool_mismatch`/`identity_mismatch`/`input_mismatch`/`mac_mismatch` reasons; the agent loop can wire per-rule `replay_max` / `approval_ttl_seconds` (with the design-doc 8 / 1h defaults) through config end-to-end. `xmake run orangutan` now reports slice 12. | New public surfaces: `permission::ApprovalBroker` (move-only, owns an `ApprovalAuthority`, exposes `approve`, `check`, `reap_expired`, `outstanding_grants`, `authority`) and `permission::ApprovalGrant`. `permission::Rule` grew `replay_max=8` + `approval_ttl=3600s` defaults; `permission::Decision` mirrors them so the agent loop has the matched-rule policy in hand. `config::PermissionRuleConfig` grew optional `replay_max` (uint32) + `approval_ttl_seconds` (int64); the loader rejects negative or non-integer values with the offending JSON path attached. `permission::materialize` forwards the optionals into the runtime `Rule` while keeping the defaults when the operator omitted them. `tests/permission` grows to 73 cases / 296 assertions (broker replay/exhaust/no_grant/re-approve-resets/reap_expired/replay_max=0-quarantine/distinct-triple-independence/authority-error-propagates-without-spending-honest-counter/TTL-expiry-cross-checks plus Rule + Decision policy-field defaults plus materialize forwarding). `tests/config` grows to 19 cases / 148 assertions (replay_max + approval_ttl_seconds parse-success, defaults-unset, reject-negative-replay_max, reject-negative-approval_ttl_seconds, reject-non-integer-replay_max). New bench bucket `bench-permission/approval_broker` (broker_approve ~9.9 µs, broker_check_ok ~10.7 µs, broker_check_no_grant ~10.9 µs, broker_check_exhausted ~11.0 µs — broker overhead over raw authority verify is ~875 ns). Audit log writes and bootstrap wiring remain future slices. | [history](../histories/2026-05/20260517-0420-permission-approval-broker.md) |
| 2026-05-17 | permission-approval-signing | `oran-permission` now ships the HMAC-SHA-256 approval-signing primitives the design doc and `0008-permissions.md` have been promising. Every runtime generates a fresh 32-byte per-process key from libsodium's CSPRNG; approval tokens carry a SHA-256 input hash + 16-byte nonce + `core::Time` expiry + 32-byte MAC over a domain-separated length-prefixed canonical bytes layout. Tokens signed under a prior process fail MAC verification under the new authority — `0008-permissions.md` criterion 5 ("Approval signing key is rotated when the runtime restarts; prior approvals are invalidated") is now closed. Operators get a verifier that explains *why* a token was rejected via a `reason` context entry on the returned `Error::permission_denied` (`expired`/`tool_mismatch`/`identity_mismatch`/`input_mismatch`/`mac_mismatch`). `xmake run orangutan` now reports slice 11. | New public surfaces: `permission::ApprovalSecret` (libsodium-backed HMAC key wrapper, constant-time `macs_equal`, `sodium_memzero` on move/destruction); `permission::ApprovalRequest`, `permission::ApprovalToken`, and `permission::ApprovalAuthority` (issue/verify façade). Public headers stay sodium-free (rule C6); libsodium 1.0.21 lands in `xmake/packages.lua` as a private dep of `oran-permission`, with the boundary in `docs/rules/libraries.md` extended from "oran-config only" to "oran-config and oran-permission". `tests/permission` grows to 59 cases / 233 assertions (RFC-style HMAC vector pinning vs. OpenSSL reference outputs, cross-tool/cross-input/cross-identity replay rejection, MAC + nonce tamper, expiry boundary semantics, the criterion-5 cross-authority rejection). New bench buckets `bench-permission/approval_secret` (HMAC short-vs-long-message ~6.4 µs / ~28.8 µs, constant-time `macs_equal` ok-vs-no ~60 ns / ~64 ns) and `bench-permission/approval` (issue ~9.9 µs, verify_ok ~9.3 µs, verify_expired ~36 ns — early-reject ~250× faster). Replay tracking (criterion 2's `replay_max`/`approval_ttl` per-rule plumbing) and audit log writes remain future slices. | [history](../histories/2026-05/20260517-0235-permission-approval-signing.md) |
| 2026-05-17 | permission-explain-rules | `orangutan --explain-rules` prints the materialized permission rule set for the loaded config (defaults + global + empty per-agent overlay), one rule per line with verdict, tool pattern, capability scope, and input-pattern source. Operators can now sanity-check a config's runtime effect without writing a script — the legacy "rule explain CLI subcommand" risk in `docs/product-specs/0008-permissions.md` is mitigated. `xmake run orangutan` now reports slice 10. | New `--explain-rules` flag handled by `oran-bootstrap`; `oran-bootstrap` gains a private dep on `oran-permission`. New `permission::RuleSet::rules() -> std::span<const Rule>` accessor exposes the underlying rules in insertion order for diagnostics. `tests/permission` grows to 42 cases / 172 assertions; the existing `--explain-rules` smoke is verified end-to-end on the checked-in `config.example.json` (17 rules: 9 defaults + 8 from config including two `input_pattern`-backed deny rules). | [history](../histories/2026-05/20260517-0155-bootstrap-explain-rules.md) |
| 2026-05-17 | permission-input-pattern-config | Config now accepts runtime regex on permission rules. `permissions.{allow,deny,ask}[*].input_pattern` (and the per-agent overlay equivalent) compiles via re2 once at load and surfaces invalid patterns as `Error::config` with the offending JSON path (e.g. `$.permissions.deny[0].input_pattern`) and the re2 error message under the `regex_error` context key. `permission::materialize` recompiles the validated source into a runtime `InputPattern` when assembling each `Rule`, returning `core::Result<RuleSet>` so a (theoretical) compile failure surfaces instead of silently dropping a rule. Acceptance criterion 4 of `docs/product-specs/0008-permissions.md` is now closed. `xmake run orangutan` reports slice 9. | `config::PermissionRuleConfig` grew `std::optional<std::string> input_pattern`. `oran-config` gains a private dep on `re2`. `xmake/targets.lua`'s `oran_lib` helper switched from `add_packages(table.unpack(pkgs), opts)` to a `for/ipairs` loop after observing that the unpack form silently dropped the second package. `config.example.json` ships two real `input_pattern`-backed `deny` rules. `tests/config` 14 cases / 132 assertions; `tests/permission` 41 cases / 165 assertions; `bench/config` new `config.parse_permissions_with_input_patterns` scenario (~300 ns per re2 compile at load). | [history](../histories/2026-05/20260517-0145-oran-config-input-pattern.md) |
| 2026-05-17 | permission-input-pattern | `oran-permission` now matches runtime regex against the call's input string. `Rule::input_pattern` carries an optional `permission::InputPattern` (re2 partial match, hidden behind a forward-declared `re2::RE2` in the public header — rule C6 stays honest), and `RuleSet::evaluate(tool, input, capabilities, mode)` pays re2 only when a rule is scoped to it. Tests cover compile success / failure paths, partial-match vs anchored semantics, source-string equality, capability + input composition. `xmake run orangutan` reports slice 8. | New `include/oran/permission/input_pattern.hpp` exposing `InputPattern::compile -> Result<InputPattern>` and `InputPattern::matches`. Reason format now prints `input=~"<pattern>"` next to the existing `capability=<name>`. `Rule` is move-only as a result; the bench fixtures switched from copy-into-vector to move-into-`RuleSet`. `tests/permission` 39 cases / 149 assertions; new `bench/permission/scenarios/input_pattern.cpp` (match ~190 ns / miss ~63 ns / no-pattern ~100 ns). | [history](../histories/2026-05/20260517-0136-oran-permission-input-pattern.md) |
| 2026-05-16 | permission-materialize | `oran-permission` now ships `permission::materialize(Mode, const config::PermissionsConfig& global, const config::PermissionsConfig& per_agent) -> RuleSet`, the three-layer merge that combines built-in defaults with global `config.permissions` and per-agent `agents.<name>.permissions` overlays. The design-doc three-layer rule pipeline is end-to-end live: future bootstrap code can hand any agent's runtime evaluator a fully-merged `RuleSet`. `xmake run orangutan` still reports slice 7. | New `include/oran/permission/materialize.hpp` and `permission::materialize`. Layer order is defaults → global → per-agent; the runtime evaluator's deny → allow → ask precedence is unchanged, so an explicit `deny` in any layer outranks an `allow` in any other layer (matching the design-doc guarantee). A two-argument overload uses an empty per-agent overlay for call sites that haven't selected an agent. `oran-permission` now depends on `oran-config` (legal — config sits below permission in the module-boundary layering); the umbrella `<oran/permission.hpp>` re-exports the new header. `tests/permission` grows to 30 cases / 114 assertions (empty merge == defaults, layer order preserved, verdict mapping one-to-one, capability scope preserved, deny in any layer outranks allow in any other layer, intra-layer order preserved, two-argument overload behavior); `bench/permission` adds a three-scenario block — `materialize_defaults_only` (~122 ns), `materialize_with_global` (~230 ns over 8 global rules), `materialize_with_global_and_agent` (~257 ns over 8 global + 2 overlay). re2 input regex, HMAC approvals, and audit logging remain downstream. | [history](../histories/2026-05/20260516-2305-oran-permission-materialize.md) |
| 2026-05-16 | config-permissions | `oran-config` now exposes typed permission data — the global `permissions` root and each `agents.<name>.permissions` overlay parse into `PermissionsConfig` collections of `PermissionRuleConfig {verdict, tool_pattern, capability}`, with capability spellings resolved against `core::Capability` at load time. This is layer-2 and the data side of layer-3 of the design-doc three-layer rule merge; the runtime merge that concatenates them with `Defaults::for_mode` belongs on the upcoming `oran-permission::materialize` slice. `xmake run orangutan` still reports slice 7. | New public surface in `include/oran/config/config.hpp`: `PermissionVerdict {allow,deny,ask}` enum (wire spelling and parse via the generic `core::enum_name` / `core::parse_enum<PermissionVerdict>` helpers in `oran-core`), `PermissionRuleConfig`, `PermissionsConfig`, `AgentConfig`, and `Config::permissions()` / `Config::agents()` accessors. `config.example.json` carries a representative `permissions` block + one agent overlay so the schema is documented. Parser now uses `nlohmann::ordered_json` so JSON authoring order survives across verdict keys and agent names. Validation: missing `tool_pattern` errors; empty `tool_pattern` errors; unknown capability spelling errors; non-object rule entry / wrong array type errors; unknown verdict key + unknown rule field + unknown agent field warn (loose) or fail (strict). `tests/config` grows to 12 cases / 120 assertions; `bench/config` adds an empty-vs-typed permissions parse A/B (~879 ns vs. ~18.4 µs over 16 rules + one agent overlay). | [history](../histories/2026-05/20260516-2210-oran-config-permissions.md) |
| 2026-05-16 | permission-defaults | `oran-permission` now ships a `Defaults::for_mode(Mode)` baseline factory — layer-1 of the design-doc three-layer rule merge — so future config wiring has a documented safe-baseline starting point instead of the empty `RuleSet`. `xmake run orangutan` still reports slice 7. | New `include/oran/permission/defaults.hpp` and `Defaults::for_mode`: `Mode::strict` returns empty (strict mode denies by default); `Mode::default_` returns 9 capability-scoped rules (allow `read_file` / `read_memory`; ask `write_file` / `edit_file` / `write_memory` / `spawn_subprocess` / `egress_http`; deny `runtime_loader` / `delete_path`); `Mode::permissive` returns 2 denies (`runtime_loader`, `delete_path`); `Mode::sandboxed` returns 2 allows (`read_file`, `read_memory`). `tests/permission` grows to 22 cases / 91 assertions (per-mode rule count + verdict per capability, capability-less call falls back to mode default, reason annotation flows through the factory builds, referential transparency); `bench/permission` adds a `Defaults::for_mode(default_)` factory build vs. inline construction A/B (~113 ns vs. ~112 ns — the factory is essentially free at startup). The runtime merge that combines defaults + config + per-agent overlay belongs on the upcoming `oran-config::permissions` slice. | [history](../histories/2026-05/20260516-2120-oran-permission-defaults.md) |
| 2026-05-16 | permission-capability | `oran-permission` now enforces capability scope: a rule may carry an optional `core::Capability`, and the new `RuleSet::evaluate(tool, required_capabilities, mode)` overload only fires capability-bound rules when the call's required set contains that capability. The existing `evaluate(tool, mode)` overload stays as a wrapper, so capability-bound rules silently miss when no capabilities are passed — matching the design-doc "a tool that didn't declare X cannot use it even if a rule otherwise allowed" guarantee. `xmake run orangutan` still reports slice 7. | `permission::Rule` gains `std::optional<core::Capability> capability`; reason strings annotate `capability=<name>` only when the firing rule had a scope (unscoped rules keep the short reason). `tests/permission` grows to 15 cases / 59 assertions (capability match, miss, fall-through to next precedence pass, fall-through to mode default, deny outranking allow at same scope, legacy overload skipping capability-bound rules, reason annotation). `bench/permission` grows to a 4-scenario file: precedence-walk vs. find_if (~139 ns vs. ~75 ns) and capability match vs. miss (~163 ns vs. ~118 ns) over separate 16-rule fixtures. Acceptance criterion #3 of product-specs/0008-permissions.md is now closed for the foundation slice; re2 input regex, HMAC approvals, audit logging, and config wiring remain downstream. | [history](../histories/2026-05/20260516-2030-oran-permission-capability.md) |
| 2026-05-16 | core-capability | The runtime now has a single `core::Capability` vocabulary — the v2 mechanism that ties tool declarations (`ToolDef::requires`, future) to permission rules (`Rule::capability`, next slice). `xmake run orangutan` still reports slice 7. | `oran-core` adds `Capability` (18-entry enum, `std::uint8_t` underlying) and a `std::formatter` specialization. Wire spelling and inverse parse come from the generic `core::enum_name(c)` / `core::parse_enum<Capability>(text)` / `core::enum_values<Capability>()` helpers in `enum_names.hpp` — no per-enum forwarding shims. Public header stays stdlib-only. `tests/core` grows to 54 cases / 366 assertions (round-trip, formatter, out-of-range fallback, refused unknown spellings, unique declaration order); `bench/core` adds a linear-scan parse vs. `std::unordered_map` lookup A/B (~272 ns vs. ~234 ns per 24-input batch). | [history](../histories/2026-05/20260516-2006-oran-core-capability.md) |
| 2026-05-16 | permission-foundation | `oran-permission` is now a real library that any future caller can reach for, instead of design-only text. It owns the verdict vocabulary, mode-driven defaults, and the deny → allow → ask precedence the design doc has been promising. `xmake run orangutan` still reports slice 7. | Adds `oran-permission` with `Verdict { allow, deny, ask }`, `Mode { strict, default_, permissive, sandboxed }`, `Rule`, `Decision`, `RuleSet`, a textbook `*`-glob `glob_match`, and `std::formatter` specializations for `Verdict` and `Mode`. Wire spelling and inverse parse come from the generic `core::enum_name` / `core::parse_enum` helpers — no per-enum forwarding shims. Public header stays stdlib-only. `tests/permission` (new bucket) ships 8 cases / 41 assertions covering literal + wildcard match, deny-wins precedence, ask fallback, mode defaults, and the indexed-reason output; `bench/permission` ships a precedence-walk vs. single-pass `std::ranges::find_if` A/B (~156 ns vs. ~83 ns over a 16-rule fixture). Capability gating, re2 input regex, HMAC approvals, audit logging, and config wiring remain downstream. | [history](../histories/2026-05/20260516-1900-oran-permission-foundation.md) |
| 2026-05-16 | storage-session-role | `SessionRepository` now takes and exposes a typed `core::Role` on the API boundary instead of free-form `std::string`, so callers above storage stop hand-typing role strings and rows with unknown role text surface a clear error on read. `xmake run orangutan` still reports slice 7. | `AppendSessionMessageRequest::role` and `SessionMessageRecord::role` switch from `std::string` to `core::Role`. The write path binds `core::enum_name(role)`; the read path parses the column text back into the enum via `core::parse_enum<Role>` and returns a storage-kind error if it does not match a known role. `tests/core` grows to 48 cases / 276 assertions; `tests/storage` grows to 52 cases / 606 assertions (adds an enumerator-round-trip case and a bad-role-row rejection case). `bench/storage` keeps the raw-pool vs. repository A/B (raw ~796 µs vs. repository ~967 µs on this run). | [history](../histories/2026-05/20260516-1810-oran-storage-session-role.md) |
| 2026-05-16 | core-str-utf8 | Every higher layer (config values, storage payloads, CLI echo, future provider request bodies) can now reach for the same RFC-3629 UTF-8 primitives in `core::str::*` instead of re-implementing them. `xmake run orangutan` still reports slice 7. | `oran-core` adds `core::str::is_valid_utf8` (strict RFC 3629; rejects overlong encodings, lone continuations, surrogate code points U+D800..U+DFFF, code points beyond U+10FFFF), `core::str::count_code_points` (returns `std::optional<std::size_t>`; `nullopt` for invalid input), and `core::str::truncate_to_code_point` (byte budget, never splits a multi-byte sequence). Public header keeps the stdlib-only contract. `tests/core` grows to 46 cases / 266 assertions, and `bench/core` adds the strict validator walk on a 1024-byte mixed fixture (~419 ns) vs. a `std::ranges::all_of` ASCII-only short-circuit (~1.8 ns). | [history](../histories/2026-05/20260516-1740-oran-core-str-utf8.md) |
| 2026-05-16 | core-tooldef | The agent loop, provider adapters, and the future tool registry can now name the canonical tool declaration with `core::ToolDef` (name + description + opaque JSON Schema string) instead of hand-rolling per-layer shapes. `xmake run orangutan` still reports slice 7. | `oran-core` adds `ToolDef` with member-wise equality and a `ToolDef::with_no_input(name, description)` fixture helper that fills a minimal `{"type":"object","properties":{},"additionalProperties":false}` schema. Public header keeps the nlohmann-free contract (rule C6). `tests/core` grows to 37 cases / 223 assertions, and `bench/core` adds `ToolDef` aggregate-init vs. helper construction (~12.9 ns vs. ~18.2 ns per build). | [history](../histories/2026-05/20260516-1715-oran-core-tooldef.md) |
| 2026-05-16 | core-message | Conversation flow between providers, the agent loop, storage, memory, and channels can now move a single canonical message type — `core::Message` (role + ordered content blocks + optional timestamp) carrying `core::Content` (`Text`/`Thinking`/`ToolUse`/`ToolResult`) — instead of every layer plumbing strings or JSON. `xmake run orangutan` still reports slice 7. | `oran-core` adds `Role`, `StopReason`, `TextContent`, `ThinkingContent`, `ToolUseContent`, `ToolResultContent`, the `Content` variant alias with `holds_*` / `text_view` helpers, and `Message` with `user_text`/`assistant_text` factories. Public headers stay nlohmann-free; tool input/output stays opaque so `oran-provider` adapters can own JSON. `tests/core` grows to 33 cases / 209 assertions, and `bench/core` adds `Content` visit-vs-get_if (~28 ns vs. ~26 ns) plus a 32-block message walk (~31 ns). | [history](../histories/2026-05/20260516-1605-oran-core-message.md) |
| 2026-05-16 | core-time | Storage rows, audit logs, and future memory records can now use a single canonical UTC `Time` type with strict ISO-8601 format/parse, instead of plumbing raw `std::chrono::system_clock::time_point` or hand-shaped timestamp strings across library boundaries. `xmake run orangutan` still reports slice 7. | `oran-core` adds `Time`, `core::time::now_utc`, `core::time::format_iso8601_utc` (deterministic `YYYY-MM-DDTHH:MM:SS.fffZ`), and `core::time::parse_iso8601_utc` (strict; rejects unknown shapes, non-UTC offsets, more than three fractional digits, lowercase separators, leading/trailing whitespace, and out-of-range fields). `tests/core` grows to 17 cases / 150 assertions, and `bench/core` adds explicit-format vs. chrono-format scenarios plus a parse cost scenario (~339ns vs. ~174ns format, ~46ns parse). | [history](../histories/2026-05/20260516-1450-oran-core-time.md) |
| 2026-05-16 | storage-sql-file-migrations | Storage migrations can now be authored as checked-in `.sql` files instead of embedded C++ strings. `SessionRepository` loads its sessions schema from the source-tree migration directory by default, with an override for future packaged layouts. `xmake run orangutan` still reports slice 7. | `oran-storage` adds `load_migrations_from_directory` and `run_migrations_from_directory`; migration files use the strict `0001-name.sql` convention and are validated before any database write. `tests/storage` grows to 50 cases / 582 assertions, and `bench/storage` adds compiled-span vs. SQL-file migration load+run scenarios (~229.8μs vs. ~276.8μs cold apply, ~3.7μs vs. ~45.6μs no-op check). | [history](../histories/2026-05/20260516-1345-oran-storage-sql-file-migrations.md) |
| 2026-05-16 | storage-session-repository | Adds the first storage domain repository: sessions can now be migrated, appended, loaded, fetched, and listed over the cached writer/reader pool surface. This is a storage-layer foundation; CLI/provider session persistence is not wired yet. `xmake run orangutan` still reports slice 7. | `oran-storage` now ships `SessionRepository`, `SessionKey`, `AppendSessionMessageRequest`, `SessionMessageRecord`, `SessionRecord`, and `ListSessionsOptions`; messages store opaque `content_json` / `metadata_json` strings until `core::Message` serialization lands. `tests/storage` grows to 45 cases / 474 assertions, and `bench/storage` adds raw pool SQL vs. repository append/load (~698.4μs vs. ~859.0μs per 64-message batch). | [history](../histories/2026-05/20260516-1313-oran-storage-session-repository.md) |
| 2026-05-16 | storage-pool-cache | Pool leases now expose the statement cache for their writer or reader slot, so future repositories can reuse prepared statements without maintaining a side map from connection slot to cache. `xmake run orangutan` still reports slice 7. | `PoolOptions` adds `statement_cache_capacity`, `WriterLease` and `ReaderLease` expose `statement_cache()`, and `Pool::open` creates one cache per writer/reader slot. `tests/storage` grows to 40 cases / 403 assertions, and `bench/storage` adds a pool fresh-prepare vs. pool cached-prepare insert comparison (~326.8μs vs. ~285.4μs per 64-row batch). | [history](../histories/2026-05/20260516-1218-oran-storage-pool-cache.md) |
| 2026-05-16 | storage-statement-cache | Adds per-connection prepared-statement caching on top of the SQLite core, ready for sessions/memory/audit hot paths to amortize `sqlite3_prepare_v2` cost across calls. `xmake run orangutan` still reports slice 7. | `oran-storage` now ships `StatementCache`, `CachedStatement`, and `StatementCacheOptions`; the cache exposes LRU eviction, hit/miss/eviction counters, transient overflow when every entry is leased, RAII leases that reset+clear-bindings on release, and idempotent `clear`. `tests/storage` grows to 37 cases / 335 assertions, and `bench/storage` adds a fresh-prepare vs. cached-prepare insert comparison (~118.8μs vs. ~62.7μs per 64-row batch). | [history](../histories/2026-05/20260516-1134-oran-storage-statement-cache.md) |
| 2026-05-16 | storage-pool | Adds an async writer/reader connection pool on top of the SQLite core, ready for sessions, memory, automation, and audit work to share connections without re-opening per query. `xmake run orangutan` still reports slice 7. | `oran-storage` now ships `Pool`, `WriterLease`, and `ReaderLease`; the pool keeps one writer + N readers driven by `oran-async` executors, hands out RAII leases, and exposes async `acquire_writer` / `acquire_reader`. `tests/storage` grows to 25 cases / 229 assertions (writer exclusion, FIFO reader hand-off, cancellation, readers reject writes) and `bench/storage` adds a direct-connection vs. pool acquire comparison. | [history](../histories/2026-05/20260516-0227-oran-storage-pool.md) |
| 2026-05-16 | cli-handoff | `orangutan --prompt <text>` now reaches a deterministic CLI single-shot shell instead of failing as an unknown bootstrap argument; no-arg runs select the pre-agent-loop REPL shell. `xmake run orangutan` now reports slice 7. | Added stdlib-only `oran-cli` with `CliOptions` / `CliResult`, wired bootstrap config loading before CLI handoff, added `test-cli` (5 cases, 30 assertions), `bench-cli` prompt-vs-REPL dispatch coverage, and updated bootstrap tests for CLI handoff. | [history](../histories/2026-05/20260516-0020-oran-cli-handoff-slice.md) |
| 2026-05-15 | bootstrap-config | `orangutan` now has a real bootstrap config entry: `--config <path>` loads config, missing explicit config fails, and a fresh checkout still runs with built-in defaults. `xmake run orangutan` now reports slice 6. | Added `oran-bootstrap` with `load_config` / `run`, config source reporting, `test-bootstrap` (5 cases, 29 assertions), `bench-bootstrap` default-vs-explicit config startup coverage, and `xmake run` repo-root run directory for the binary. | [history](../histories/2026-05/20260515-2315-oran-bootstrap-config-integration.md) |
| 2026-05-15 | config-foundation | Adds the first loadable v2 config surface for future bootstrap/provider routing. `xmake run orangutan` now reports slice 5. | `oran-config` now ships `Config::parse`, `Config::load_file`, typed runtime/profile/route/session/web fields, recursive `${VAR}` substitutions, unknown-root warnings/strict failures, a checked `config.example.json`, `tests/config` (5 cases, 49 assertions), and `bench/config` parse-vs-file-load coverage. | [history](../histories/2026-05/20260515-2245-oran-config-foundation.md) |
| 2026-05-15 | storage-migrations | Adds the schema migration foundation future config, sessions, memory, automation, and audit databases need at startup. `xmake run orangutan` now reports slice 4. | `oran-storage` now ships `Migration`, `MigrationReport`, and `run_migrations`; migrations are contiguous, transactional, idempotent, recorded in `schema_versions`, covered by `tests/storage` (16 cases, 150 assertions), and benchmarked with cold apply vs. no-op check. | [history](../histories/2026-05/20260515-2230-oran-storage-migrations.md) |
| 2026-05-15 | storage-runtime | Adds the expected-only SQLite foundation for future config, sessions, memory, automation, and audit data. `xmake run orangutan` now reports slice 3. | SQLite core: `oran-storage` with `Connection`, `Statement`, verified WAL/foreign-key setup, `tests/storage` (9 cases, 95 assertions), and `bench/storage` (literal execute inserts vs. prepared statement binding). | [history](../histories/2026-05/20260515-2152-oran-storage-sqlite-core.md) |
| 2026-05-15 | io-runtime | Adds the first reusable local I/O layer for future tools, skills, hooks, bootstrap, and channels. `xmake run orangutan` now reports slice 2. | Slice 2: `oran-io` with cancel-aware text-file read/write helpers, deterministic directory listing, `tests/io` (8 cases, 33 assertions), and `bench/io` (direct `std::ifstream` read vs. coroutine wrapper). | [history](../histories/2026-05/20260515-2123-oran-io-slice-2.md) |
| 2026-05-14 | async-runtime | Adds the first reusable async runtime layer for future IO, storage, agent, and channel work. `xmake run orangutan` now reports slice 1. | Slice 1: `oran-async` with asio `Runtime`, `Awaitable<T>`, bounded `Channel<T>`, cancel-aware `sleep_for`, `tests/async` (8 cases, 38 assertions), and `bench/async` (direct coroutine post loop vs. channel handoff). | [history](../histories/2026-05/20260514-2349-oran-async-slice-1.md) |
| 2026-05-14 | build-skeleton | First working `orangutan` binary lands; project compiles end-to-end as C++26 on GCC 16.1 with `xmake`. `xmake run orangutan` prints a greeting via `std::print`. | Slice 0: language ratchet to C++26 (rule C17), GCC `-fanalyzer` wiring + rule (C18 / `static-analysis.md`), `oran-core` lib (`Error` / `Result<T>` / `all_ok`), `tests/core` (8 cases, 47 assertions, all pass), `bench/core` (A-vs-B scenario). Clean build 4.37 s. | [history](../histories/2026-05/20260514-2214-mvp-build-skeleton-slice-0.md) |
| 2026-05-14 | scaffold | Introduced the harness-engineering scaffold for the Orangutan v2 rewrite. | Added the agent entry docs, design docs, rules, product specs, exec-plan + history templates, scripts, and CI scaffolding. | (initial scaffold) |
