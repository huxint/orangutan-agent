# Quality Score

Track quality by area so agents can prioritize the weakest parts of the system. The
table represents the *current* state of the rewrite. Update in the same PR that
materially changes an area.

## Scale

- `A` — strong coverage, stable behavior, clear docs, low operational risk.
- `B` — acceptable but has known gaps.
- `C` — works but needs targeted hardening.
- `D` — fragile, under-specified, or not yet implemented.
- `—` — not applicable (e.g., shaped only).

## Current State (Pre-v1)

| Area                     | Score | Why | Next Step |
| ------------------------ | ----- | --- | --------- |
| Architecture docs        | B     | Top-level map + design docs drafted. Some sections await concrete code. | Land MVP code; back-fill diagrams. |
| Build system             | B     | xmake skeleton lands in slice 0; GCC 16.1 toolchain detected, C++26 enforced, `compile_commands.json` autoupdates. | Land per-library `check-compile-budget.sh` numbers + Clang secondary toolchain. |
| Compile-time discipline  | C     | Budgets captured; `oran-core`, `oran-async`, `oran-io`, and `oran-storage` build under the current target budget, but no per-TU baseline file exists yet. | Land per-library `check-compile-budget.sh` numbers. |
| Test framework           | C     | Catch2 v3 buckets green for `oran-core` (54 cases / 366 assertions), `oran-async` (8 cases / 38 assertions), `oran-io` (8 cases / 33 assertions), `oran-storage` (60 cases / 702 assertions), `oran-config` (19 cases / 148 assertions), `oran-permission` (83 cases / 379 assertions), `oran-cli` (5 cases / 30 assertions), and `oran-bootstrap` (21 cases / 78 assertions). | Add first tool / hook tests when those libraries land. |
| Bench harness            | C     | nanobench buckets green for `oran-core`, `oran-async`, `oran-io`, `oran-storage`, `oran-config`, `oran-permission`, `oran-cli`, and `oran-bootstrap`; core covers builder-vs-prefilled `Error` construction, time format-explicit-vs-chrono-format plus a parse cost scenario, Content visit-vs-get_if traversal plus a 32-block message walk, `ToolDef` aggregate-init-vs-`with_no_input` helper, the strict RFC-3629 `is_valid_utf8` walk vs. an ASCII-only short-circuit, and the `Capability` linear-scan parse vs. `std::unordered_map` lookup batch; storage covers inserts, compiled-span migrations, SQL-file migration load+run, direct-vs-pool acquire, fresh-vs-cached prepare, pool fresh-vs-cached prepare, raw-pool-vs-session-repository append/load, and raw-pool-vs-audit-repository append/list (~992 µs vs. ~1219 µs over a 64-event batch — ~23% typed-repository overhead, ~3.5 µs per event over the raw ~15.5 µs); permission covers `RuleSet::evaluate` precedence walk vs. a single-pass `std::ranges::find_if`, a capability-aware match-vs-miss A/B, the `Defaults::for_mode(default_)` factory build vs. the equivalent inline construction, a three-layer materialize A/B (defaults-only vs. defaults+global vs. defaults+global+per-agent), an `input_pattern` match vs. miss vs. no-pattern A/B (re2 cost on success / failure / removed), an HMAC-SHA-256 short-vs-long-message A/B plus a constant-time `macs_equal` ok-vs-no A/B (~6.4 us / 32 B, ~28.8 us / 1 KiB, ~60 ns / ~64 ns compare), an `ApprovalAuthority` issue-vs-verify-vs-early-reject-expired A/B (~9.9 us / ~9.3 us / ~36 ns — early reject ~250x faster), an `ApprovalBroker` approve-vs-check_ok-vs-check_no_grant-vs-check_exhausted A/B (~9.9 us / ~10.7 us / ~10.9 us / ~11.0 us — broker overhead over the raw authority verify is ~875 ns, the map find + counter decrement + reason build), and an audit-sink A/B/C plus `to_hex` baseline (~260 ns NullAuditSink / ~360 ns RecordingAuditSink / ~18.1 us StorageAuditSink / ~62 ns `to_hex`-of-32-bytes); config covers parse-vs-file-load plus an empty-vs-typed `permissions` parse A/B and a typed-vs-input-pattern parse A/B (parsing 16 mixed-scope rules then a 14-rule block with 4 re2 input_patterns); CLI covers prompt-vs-REPL dispatch; and bootstrap covers default-vs-explicit config startup plus the new `RuntimeAssembly::build` audit-on-vs-audit-off A/B (~202 µs / ~400 ns post-`#embed` — provisioning the audit pipeline costs ~201 µs over the broker-only baseline, dominated by SQLite open + migration + Pool::open; slice 15's `#embed`-backed migrations shave a few µs off the slice-14 CWD-scan path). No baseline JSON or regression gate yet. | Wire `scripts/bench-compare.sh` to a real baseline file. |
| Async model              | B     | `Runtime`, `Awaitable<T>`, cancel-aware `sleep_for`, and bounded `Channel<T>` are implemented with tests and a bench; `oran-storage::Pool` now consumes the executor for async writer/reader acquisition. Runtime signal integration and mailbox policy are still downstream. | Add cancellation-latency bench coverage for the first orchestration workload. |
| IO runtime               | C     | `oran-io` file/directory MVP is implemented with tests and a bench; subprocess, glob, pipe, signal, and watcher APIs are still downstream. | Add subprocess/signal helpers after permission and hook surfaces are ready to wrap them. |
| Storage / DBs            | C     | Expected-only SQLite `Connection` / `Statement` core, migration runner with SQL-file directory loading and compile-time-embedded built-in migrations (`built_in_audit_migrations()` / `built_in_session_migrations()` reach the SQL via C++26 `#embed`), async writer/reader `Pool`, per-slot pool `StatementCache`, standalone per-connection `StatementCache` (LRU, hit/miss/eviction counters, transient overflow), `SessionRepository` (typed `core::Role` at the API boundary), and `AuditRepository` (`audit_events` table backed by `migrations/audit/0001-audit-initial.sql`, append/list/count with scope-key partitioning + secondary `agent_key`/`tool_name`/`outcome` filters + scope-keyed indexes) are implemented with WAL/foreign-key setup, schema versioning, RAII leases, FIFO waiter resumption, cancellation, tests, and benches. Memory and automation repositories are still downstream. | Add the memory repository on top of the cached pool surface; reuse `built_in_*_migrations()` for its boot path. |
| Config                   | C     | Expected-only JSON loader is implemented with typed runtime/profile/route/session/web fields, recursive env substitution, strict unknown-root handling, `config.example.json`, tests, and a bench. The typed surface now also covers the `permissions` root block and `agents.<name>.permissions` overlays (rules carry pre-resolved `core::Capability`, verdict keys validate against `allow`/`deny`/`ask`, unknown verdict / rule / agent fields warn or fail per `strict_config`), and each rule may now carry an `input_pattern` re2 source string that the loader validates by compiling once via re2 (invalid patterns surface as `Error::config` with the JSON path attached). Each rule also carries optional `replay_max` (uint32) and `approval_ttl_seconds` (int64) so operators can override the design-doc approval-window defaults per rule (negative or non-integer values reject at parse time with the JSON path attached). Generated schema, secret crypto, and the remaining typed agent fields (provider/model override, prompts, hook bindings) are still downstream. | Generate JSON Schema and add secret-field support. |
| Bootstrap                | C     | Process entry now routes through `oran-bootstrap`, loads explicit or default config, falls back to built-in defaults when the default file is absent, hands CLI args to `oran-cli`, exposes `--explain-rules` that calls `permission::materialize(Mode::default_, config.permissions(), {})` and prints the merged rule set (defaults + global + per-agent overlay), exposes `--audit-init [<path>]` that opens the audit DB (defaults to `<workspace>/.orangutan/audit.db`) on a one-shot `asio::io_context` and applies the `storage::AuditRepository::migrate()` flow (idempotent on re-run), and now also assembles the per-process permission infrastructure via `bootstrap::RuntimeAssembly::build(workspace, executor, options)` — a value type bundling a fresh `permission::ApprovalBroker` (per-process HMAC key per `0008-permissions.md` criterion 5) and an active `permission::AuditSink` (`StorageAuditSink` over an internal `storage::Pool` + `storage::AuditRepository` when `options.audit_enabled=true`, `NullAuditSink` otherwise). Slice 15 packages the audit/sessions migrations into the binary via C++26 `#embed`, so `bootstrap::run` defaults `audit_enabled=true` and the binary provisions `<workspace>/.orangutan/audit.db` regardless of CWD; smoke-tested from `/tmp` and covered by a `CwdGuard` test case under `tests/bootstrap/test_runtime_assembly.cpp`. `xmake run orangutan` reports slice 15. Per-agent overlay selection in `--explain-rules`, signal handling, and provider startup are still downstream. | Pass mode + per-agent selection to `--explain-rules`; first tool built-ins consume the assembly's `AuditSink`; first provider adapter. |
| Provider system          | C     | Layered design captured. | First adapter (Anthropic Messages). |
| Tool registry            | C     | Design captured. | First built-ins (file + shell + memory). |
| Memory tiers             | C     | Tier design captured. | Long-term FTS5 v1. |
| Permissions              | C     | Foundation `oran-permission` library now covers eight axes: (1) `Verdict { allow, deny, ask }`, `Mode { strict, default_, permissive, sandboxed }`, `Rule`, `Decision`, and `RuleSet` with the deny → allow → ask precedence walk and a `*`-glob tool-name matcher; (2) capability-aware gating via the optional `Rule::capability` (of `core::Capability`) and the capability-aware `RuleSet::evaluate(tool, required_capabilities, mode)` overload (legacy `evaluate(tool, mode)` retained as a wrapper that passes `{}`); (3) the `Defaults::for_mode(Mode)` baseline factory (layer-1 of the three-layer rule merge); (4) the three-layer `materialize(Mode, global, per_agent) -> Result<RuleSet>` that concatenates defaults + global `config::PermissionsConfig` + per-agent overlay, now forwarding the optional `replay_max` / `approval_ttl_seconds` operator-config fields into each runtime `Rule` while keeping the design-doc defaults when the operator omitted them; (5) runtime input regex via `permission::InputPattern` (re2 partial match) — rules carry an optional `Rule::input_pattern`, and the four-argument `RuleSet::evaluate(tool, input, required_capabilities, mode)` overload pays re2's match cost only when the rule is scoped to it; (6) HMAC-signed approval tokens via `permission::ApprovalSecret` (libsodium-backed 32-byte HMAC-SHA-256 key with `randombytes_buf` generation, constant-time `sodium_memcmp` comparison, and `sodium_memzero` on move/destruction) plus `permission::ApprovalAuthority` / `ApprovalToken` (SHA-256 input hash + 16-byte nonce + `core::Time` expiry + domain-separated length-prefixed canonical bytes, with expiry → tool → identity → input → MAC verify precedence carrying a `reason` context entry on first failure) — closes `0008-permissions.md` criterion 5; (7) `permission::ApprovalBroker`, the stateful replay-window layer on top of `ApprovalAuthority` that registers `(tool, identity, input_hash) -> { expires_at, remaining_uses }` entries on `approve` and honors up to `replay_max` successful `check`s per triple before returning `Error::permission_denied` with `reason=no_grant` (entry missing) or `reason=replay_exhausted` (counter at zero) — closes `0008-permissions.md` criterion 2's replay half. `Rule::replay_max` / `Rule::approval_ttl` default to the design-doc values (8 / 1h); `Decision` copies the matched rule's policy fields so the agent loop can pass them straight to `ApprovalBroker::approve` via an `ApprovalGrant`. (8) `permission::AuditEvent` + `permission::AuditOutcome` value types and the `permission::AuditSink` abstract interface, plus three concrete implementations: `NullAuditSink` (no-op default), `RecordingAuditSink` (in-memory capture for tests), and `StorageAuditSink` (translates each event into a `storage::AuditRepository::append_event` call). The sink interface is async (`Awaitable<Result<void>> record(AuditEvent)`) so the storage backend can write to SQLite without blocking the agent loop. The free helpers `verdict_to_outcome`, `make_audit_event_from_decision`, and `to_hex(span<const std::byte, 32>)` let callsites build events without duplicating their internals. Bootstrap wiring landed 2026-05-17 — `bootstrap::RuntimeAssembly::build` bundles a fresh `ApprovalBroker` and the active `AuditSink` (storage-backed or null) behind one type so the upcoming agent loop receives both as a single reference. Slice 15 packages the migrations into the binary (`storage::built_in_audit_migrations()`), so the assembly now defaults to `audit_enabled=true`. Tests cover literal + wildcard match, deny-wins precedence, capability-bound match / miss / fallback, indexed-reason output (annotated with `capability=<name>` and `input=~"<pattern>"` when the rule was scoped), the per-mode baseline shapes, the three-layer concatenation semantics, re2 compile success/failure paths, partial-match semantics, capability + input_pattern composition, RFC-style HMAC vector pinning against OpenSSL reference outputs, cross-tool/cross-input/cross-identity replay rejection, MAC and nonce tamper, expiry boundary semantics (`now == expires_at` already expired), the cross-authority criterion-5 invariant (token from one secret rejected under another with `reason=mac_mismatch`), the broker's replay-until-exhausted + re-approve-resets + reap_expired + replay_max=0 quarantine + distinct-triple independence + authority-error-propagation-without-spending-the-honest-counter properties, the Rule / Decision policy-field defaults, the config-side `replay_max` / `approval_ttl_seconds` parse + reject-negative paths, the audit pipeline (AuditOutcome wire spelling round-trip, `verdict_to_outcome` mapping, decision-to-event helper, RFC-6234 SHA-256 hex vector pinning, Null/Recording/Storage sink behavior, storage adapter NULL-hash handling, per-outcome storage round-trip, repository-error propagation), the bootstrap assembly (null-sink default, storage-sink default-path provisioning, explicit-path override, idempotent re-build, end-to-end record through the assembly's sink, approval-broker round-trip + cross-tool rejection), and the slice-15 CWD-independent migration smoke (`RuntimeAssembly::build` from a non-source CWD). Bench grew to `bench-permission/approval_secret` (HMAC short-vs-long-message plus constant-time `macs_equal` ok-vs-no), `bench-permission/approval` (issue-vs-verify-vs-early-reject-expired), `bench-permission/approval_broker` (broker_approve-vs-broker_check_ok-vs-broker_check_no_grant-vs-broker_check_exhausted; broker overhead over the raw authority verify is ~875 ns), `bench-permission/audit` (null sink ~260 ns vs. recording sink ~360 ns vs. storage sink ~18.1 µs A/B, plus `to_hex` 32-byte ~62 ns), and `bench-bootstrap/assembly_build_with_audit-vs-without_audit` (~202 µs vs. ~400 ns post-`#embed` — the `#embed`-backed migration is slightly faster than the previous CWD-scan path because no directory iteration is needed). The bootstrap assembly closes the in-process half of `0008-permissions.md` criterion 1 — the agent loop now has a single typed object that owns both the broker and the sink. | First tool-registry built-ins (`file.read`, `file.write`, `file.edit`, `file.search`) consume the assembly's `AuditSink`. |
| Hooks                    | C     | Enumerated lifecycle. | Shell + in-proc sinks. |
| Channels                 | C     | Trait + capability matrix designed. | QQ port + webhook adapter. |
| Orchestration / teams    | C     | Coordination strategies designed. | LeaderWorker + Pipeline. |
| Automation               | C     | Job categories designed. | Cron + periodic + triggered. |
| Web UI                   | C     | Routes + token-auth designed. | Single-page chat. |
| CLI                      | C     | `oran-cli` implements deterministic pre-agent-loop REPL and `--prompt` single-shot shells with tests and a bench. Provider-backed streaming is still downstream. | Connect CLI prompts to `oran-agent` once provider/tool foundations land. |
| Skills                   | D     | Spec drafted; no implementation. | Loader + watcher + `skill.invoke`. |
| Observability            | D     | Logging shim designed. | Metrics endpoint. |
| Security defaults        | B     | Captured. | Implement secret rotation. |
| Supply chain             | B     | Workflows pinned, lockfile present. | Add OSV scan once xmake lock is real. |
| Static analysis          | C     | `-fanalyzer` wiring shipped (rule + xmake option); focused `oran-io` and `oran-storage` analyzer builds pass locally, but analyzer CI remains a planned nightly gate. | Run/record analyzer coverage again for later descriptor-heavy code. |

## Cadence

Reviewed at every v1.x milestone. PRs that move a row's score change it (up or down)
in the same change.

## How To Move A Row Up

- D → C: implementation exists + smoke test passes.
- C → B: full test bucket green, design doc current.
- B → A: bench coverage in place, ≥ 30 days of stable operation, no known issues
  in the tech-debt tracker for that area.
