# Quality Score

Track quality by area so agents can prioritize the weakest parts of the system. The
table represents the *current* state of the rewrite. Update in the same PR that
materially changes an area.

## Scale

- `A` — strong coverage, stable behavior, clear docs, low operational risk.
- `B` — acceptable but has known gaps.
- `C` — works but needs targeted hardening.
- `D` — fragile, under-specified, or not yet implemented.
- `—` — not applicable (e.g., shaped only).

## Current State (Pre-v1)

| Area                     | Score | Why | Next Step |
| ------------------------ | ----- | --- | --------- |
| Architecture docs        | B     | Top-level map + design docs drafted. Some sections await concrete code. | Land MVP code; back-fill diagrams. |
| Build system             | B     | xmake skeleton lands in slice 0; GCC 16.1 toolchain detected, C++26 enforced, `compile_commands.json` autoupdates. Slice 26 (2026-05-20) shipped `scripts/check-deps.sh`, a real CI gate that walks `xmake/targets.lua` and rejects upward / undocumented-sideways library deps against the layering in [`design-docs/module-boundaries.md`](design-docs/module-boundaries.md); the script is wired into `scripts/ci.sh`. | Land per-library `check-compile-budget.sh` numbers + Clang secondary toolchain. |
| Compile-time discipline  | C     | Budgets captured; `oran-core`, `oran-async`, `oran-io`, and `oran-storage` build under the current target budget. Slice 27 (2026-05-20) shipped `scripts/measure-tu.sh` (per-TU compile-time emitter, table + `--json` output). Slice 28 (2026-05-20) shipped the versioned baseline at `compile_budget.json` (20 categories + 9 targets mirrored verbatim from [`docs/rules/compile-budget.md`](rules/compile-budget.md)) and the real `scripts/check-compile-budget.sh` regression gate that joins each measured TU against its library's `{median_sec, p95_sec, hard_cap_sec}` triple, classifies into ok/warn/fail/undef, and exits non-zero on hard-cap (or `undef`, treating a missing budget row as an env error rather than a regression). `--no-strict` demotes fail to warning during calibration; `--show-all` prints greens. Not wired into `scripts/ci.sh` yet (CI does not provision xmake; even when it does, gating needs to land on the documented reference hardware to avoid environmental drift firing as regressions). First local run on WSL2: 38 TUs (21 ok / 1 warn / 16 fail / 0 undef) — the 16 fail rows are hardware-driven; tracked in the new CI-wiring tech-debt row. | Wire `check-compile-budget.sh` into `scripts/ci.sh` once CI provisions xmake on the reference hardware. |
| Test framework           | C     | Catch2 v3 buckets green for `oran-core` (69 cases / 450 assertions), `oran-async` (9 cases / 43 assertions), `oran-io` (49 cases / 286 assertions), `oran-storage` (61 cases / 718 assertions), `oran-config` (28 cases / 207 assertions), `oran-permission` (88 cases / 403 assertions), `oran-hook` (17 cases / 109 assertions), `oran-tool` (166 cases / 1588 assertions), `oran-prompt` (10 cases / 98 assertions), `oran-cli` (5 cases / 30 assertions), and `oran-bootstrap` (48 cases / 153 assertions). | Add first agent-loop tests when that library lands. |
| Bench harness            | C     | nanobench buckets green for `oran-core`, `oran-async`, `oran-io`, `oran-storage`, `oran-config`, `oran-permission`, `oran-hook`, `oran-tool`, `oran-prompt`, `oran-cli`, and `oran-bootstrap`; core covers builder-vs-prefilled `Error` construction, time format-explicit-vs-chrono-format plus a parse cost scenario, Content visit-vs-get_if traversal plus a 32-block message walk, `ToolDef` aggregate-init-vs-`with_no_input` helper, the strict RFC-3629 `is_valid_utf8` walk vs. an ASCII-only short-circuit, the `Capability` linear-scan parse vs. `std::unordered_map` lookup batch, and the slice-44 `BoundedCache` insert+lookup A/B (`unordered_map` baseline ~7.7 µs / 1024 ops vs. `BoundedCache` ~16.5 µs — ~2× LRU bookkeeping overhead) plus an `overflow_lru_eviction` scenario (~30.8 µs) that pins the eviction-on-every-put path; storage covers inserts, compiled-span migrations, SQL-file migration load+run, direct-vs-pool acquire, fresh-vs-cached prepare, pool fresh-vs-cached prepare, raw-pool-vs-session-repository append/load, and raw-pool-vs-audit-repository append/list (~992 µs vs. ~1219 µs over a 64-event batch — ~23% typed-repository overhead, ~3.5 µs per event over the raw ~15.5 µs); permission covers `RuleSet::evaluate` precedence walk vs. a single-pass `std::ranges::find_if`, a capability-aware match-vs-miss A/B, the `Defaults::for_mode(default_)` factory build vs. the equivalent inline construction, a three-layer materialize A/B (defaults-only vs. defaults+global vs. defaults+global+per-agent), an `input_pattern` match vs. miss vs. no-pattern A/B (re2 cost on success / failure / removed), an HMAC-SHA-256 short-vs-long-message A/B plus a constant-time `macs_equal` ok-vs-no A/B (~6.4 us / 32 B, ~28.8 us / 1 KiB, ~60 ns / ~64 ns compare), an `ApprovalAuthority` issue-vs-verify-vs-early-reject-expired A/B (~9.9 us / ~9.3 us / ~36 ns — early reject ~250x faster), an `ApprovalBroker` approve-vs-check_ok-vs-check_no_grant-vs-check_exhausted A/B (~9.9 us / ~10.7 us / ~10.9 us / ~11.0 us — broker overhead over the raw authority verify is ~875 ns, the map find + counter decrement + reason build), and an audit-sink A/B/C plus `to_hex` baseline (~260 ns NullAuditSink / ~360 ns RecordingAuditSink / ~18.1 us StorageAuditSink / ~62 ns `to_hex`-of-32-bytes); hook covers `Bus::publish_advisory` at 0/1/3 subscribed sinks (~242 ns / ~446 ns / ~698 ns — ~204 ns per first sink, ~126 ns per additional sink); config covers parse-vs-file-load (now exercising `runtime.prompt.active_tools`) plus an empty-vs-typed `permissions` parse A/B and a typed-vs-input-pattern parse A/B (parsing 16 mixed-scope rules then a 14-rule block with 4 re2 input_patterns); CLI covers prompt-vs-REPL dispatch; tool covers registry.lookup-vs-registry.dispatch_allow (~8 ns vs. ~2,650 ns — dispatch overhead is dominated by the libsodium SHA-256 input hash + the coroutine spawn + the audit record), `file_write.dispatch_truncate` (~12.2 µs) vs. `file_write.dispatch_append` (~12.2 µs) for the file.write built-in (mode choice is within noise at small-payload sizes), `file_edit.dispatch_unique_replace` (~16.1 µs) vs. `file_edit.dispatch_replace_all_many` (~16.7 µs) for the file.edit built-in on a 1 KiB seed (the ~0.6 µs A/B delta confirms 63 extra substitutions cost less than ~10 ns each over the shared read+write floor), `file_search.single_file_one_match` (~8.2 µs) vs. `file_search.recursive_dir_many_matches` (~27.6 µs) for the file.search built-in (the ~19 µs delta is the `recursive_directory_iterator` walk + per-file open/read overhead on a 4-file / 14-line tree — the baseline a future memory-mapped scan or parallel walker would have to beat), the `file_search.literal_match_1kib` (~9.2 µs) vs. `file_search.regex_match_1kib` (~18.6 µs on the slice-24 cold-compile baseline; after slice 51 the steady-state bench path may hit the bounded compiled-regex cache, so cold compile cost should be measured with a unique-pattern scenario if needed), the slice-21 `dispatch_ask_short_circuit` (~2.4 µs) vs. `dispatch_ask_approved` (~13.4 µs) vs. `dispatch_ask_rejected` (~13.6 µs) trio that pins the approval-broker overhead inside `Registry::dispatch` (~11 µs over the short-circuit baseline, ~875 ns of which is the raw broker `check` from `bench-permission/approval_broker`; the remainder is the HMAC verify + audit-row reason swap on the rejected branch and the handler co_await on the approved branch), and the slice-22 `dispatch_allow_no_hooks` (~2.1 µs) vs. `dispatch_allow_with_empty_bus` (~2.4 µs) vs. `dispatch_allow_with_two_sinks` (~3.0 µs) trio that pins the hook-bus dispatch tap (~346 ns "bus attached, nothing listens" tax, ~914 ns "bus attached with two observers" tax — both small relative to the audit-record cost); prompt covers `prompt.build_default_active_set` (~6.0 µs), `prompt.build_explicit_subset` (~4.2 µs), `prompt.build_promoted_subset` (~7.1 µs), and `prompt.promotion_state_promote_snapshot` (~4.0 µs per batched operation) for prompt assembly/promotion-state sanity checks; and bootstrap covers default-vs-explicit config startup plus the `RuntimeAssembly::build` audit-on-vs-audit-off A/B (~202 µs / ~400 ns post-`#embed` — provisioning the audit pipeline costs ~201 µs over the broker-only baseline, dominated by SQLite open + migration + Pool::open; slice 15's `#embed`-backed migrations shave a few µs off the slice-14 CWD-scan path) and the slice-23 `signal_drain_with_scope` (~11.2 µs) vs. `signal_drain_bare` (~443 ns) A/B that pins the per-drain `asio::signal_set` install + `release()` cancel cost (~10.7 µs, small relative to the ~205 µs assembly build). No baseline JSON or regression gate yet. | Wire `scripts/bench-compare.sh` to a real baseline file. |
| Async model              | B     | `Runtime`, `Awaitable<T>`, cancel-aware `sleep_for`, and bounded `Channel<T>` are implemented with tests and a bench; `oran-storage::Pool` now consumes the executor for async writer/reader acquisition. Slice 23 added `bootstrap::SignalScope` — the first concrete consumer of the cancel-aware-drain contract — so SIGINT/SIGTERM interrupt the `bootstrap::run_audit_init` one-shot `io_context`. Mailbox policy and a fine-grained `asio::cancellation_signal` end-to-end refinement (replacing the slice-23 blunt `io.stop()`) remain downstream. | Add cancellation-latency bench coverage for the first orchestration workload. |
| IO runtime               | C     | `oran-io` file/directory MVP is implemented with tests and a bench; slice 42 adds `FileFingerprint` + `compute_file_fingerprint` as the first piece of spec 0011's file-view system (size + ns-resolution mtime, reserved optional SHA-256 hook); slice 43 layers the second piece — `io::read_text_file_ranged` returning `ReadTextResult { text, fingerprint, start_line, end_line, returned_bytes, truncated }`, `FileRange { LineSpan | ByteSpan }` input validation, mid-read fingerprint capture (size/mtime drift -> retry once for whole-file reads under 64 KiB, `Error::conflict` for larger or ranged reads), and dual-end UTF-8 code-point boundary alignment for byte ranges (the legacy `read_text_file` becomes a thin wrapper that errors when `truncated=true`); slice 50 adds the first bounded cache consumer in `oran-io`, a `core::BoundedCache`-backed line-offset index for line ranges in files larger than 256 KiB; slice 52 adds the bounded file-view cache for successful `ReadTextResult` payloads, revalidated with `stat` before every hit; slice 53 adds a bounded 64-entry singleflight table for concurrent cold `read_text_file_ranged` calls, with `ReadTextSingleflightStats` exposing leader/follower/completion/error counters plus current table size; slice 54 adds `ReadTextFileCacheStats` so the line-offset index and file-view cache expose bounded-state health without leaking keys or paths; slice 57 adds `invalidate_read_text_file_ranged_cache(path)` so in-process writes/deletes and watcher events can invalidate only one canonical path; slice 58 adds the Linux/inotify-backed `watch_read_text_file_ranged_cache(executor, root, options)` source with aggregate `ReadTextFileWatchStats` and cancellation-aware long-running semantics. Subprocess, glob, pipe, signal, bootstrap/config watcher startup wiring, and content hashing are still downstream. | Add subprocess/signal helpers after permission and hook surfaces are ready to wrap them. |
| Storage / DBs            | C     | Expected-only SQLite `Connection` / `Statement` core, migration runner with SQL-file directory loading and compile-time-embedded built-in migrations (`built_in_audit_migrations()` / `built_in_session_migrations()` reach the SQL via C++26 `#embed`), async writer/reader `Pool`, per-slot pool `StatementCache`, standalone per-connection `StatementCache` (LRU, hit/miss/eviction counters, transient overflow), `SessionRepository` (typed `core::Role` at the API boundary), and `AuditRepository` (`audit_events` table backed by `migrations/audit/0001-audit-initial.sql`, append/update-metadata/list/count with scope-key partitioning + secondary `agent_key`/`tool_name`/`outcome` filters + scope-keyed indexes) are implemented with WAL/foreign-key setup, schema versioning, RAII leases, FIFO waiter resumption, cancellation, tests, and benches. Slice 67 adds same-row metadata replacement for post-result tool usage; `test-storage` now reports 61 cases / 718 assertions. Memory and automation repositories are still downstream. | Add the memory repository on top of the cached pool surface; reuse `built_in_*_migrations()` for its boot path. |
| Config                   | C     | Expected-only JSON loader is implemented with typed runtime/profile/route/session/web fields, including `runtime.tool_output.max_text_bytes` / `max_data_bytes` defaults for structured tool-output caps and `runtime.prompt.active_tools` for the prompt-builder active set (`"defaults"` or an explicit allowlist, preserving author order and rejecting empty tool names), recursive env substitution, strict unknown-root handling, `config.example.json`, tests, and a bench. The typed surface now also covers the `permissions` root block and `agents.<name>.permissions` overlays (rules carry pre-resolved `core::Capability`, verdict keys validate against `allow`/`deny`/`ask`, unknown verdict / rule / agent fields warn or fail per `strict_config`), and each rule may now carry an `input_pattern` re2 source string that the loader validates by compiling once via re2 (invalid patterns surface as `Error::config` with the JSON path attached). Each rule also carries optional `replay_max` (uint32) and `approval_ttl_seconds` (int64) so operators can override the design-doc approval-window defaults per rule (negative or non-integer values reject at parse time with the JSON path attached). Generated schema, secret crypto, and the remaining typed agent fields (provider/model override, prompts, hook bindings) are still downstream. | Generate JSON Schema, add secret-field support, and type the remaining agent fields. |
| Bootstrap                | C     | Process entry now routes through `oran-bootstrap`, loads explicit or default config, falls back to built-in defaults when the default file is absent, hands CLI args to `oran-cli`, exposes `--explain-rules` that calls `permission::materialize(<selected mode>, config.permissions(), <selected per-agent overlay>)` and prints the merged rule set, exposes `--audit-init [<path>]` that opens the audit DB (defaults to `<workspace>/.orangutan/audit.db`) on a one-shot `asio::io_context` and applies the `storage::AuditRepository::migrate()` flow (idempotent on re-run), and now also assembles the per-process permission infrastructure via `bootstrap::RuntimeAssembly::build(workspace, executor, options)` — a value type bundling a fresh `permission::ApprovalBroker` (per-process HMAC key per `0008-permissions.md` criterion 5) and an active `permission::AuditSink` (`StorageAuditSink` over an internal `storage::Pool` + `storage::AuditRepository` when `options.audit_enabled=true`, `NullAuditSink` otherwise). Slice 15 packages the audit/sessions migrations into the binary via C++26 `#embed`, so `bootstrap::run` defaults `audit_enabled=true` and the binary provisions `<workspace>/.orangutan/audit.db` regardless of CWD; smoke-tested from `/tmp` and covered by a `CwdGuard` test case under `tests/bootstrap/test_runtime_assembly.cpp`. Slice 16 extends `--explain-rules` with `--mode <strict\|default\|permissive\|sandboxed>` and `--agent <name>` selectors via the new public helpers `bootstrap::parse_explain_rules_selector` and `bootstrap::materialize_rules`; unknown spellings reject as `Error::invalid_argument`, unknown agent names reject as `Error::not_found` with the offending value attached. **Slice 23 (2026-05-19) added signal-aware shutdown**: `bootstrap::SignalScope` (in the new `<oran/bootstrap/signal_drain.hpp>`) is an RAII trap that installs `asio::signal_set` handlers for SIGINT + SIGTERM on a target `io_context`, captures the signum atomically, calls `io.stop()` on first delivery, and exposes an idempotent `release()` so the caller can let `io.run()` drain naturally on success. `run_audit_init` adopts the scope around its one-shot drain; the migration coroutine calls `release()` at its tail, and `bootstrap::run` translates the resulting `Error::cancelled` (with `signal=<name>` + `signum=<int>` context entries) into `Result<int>(128 + signum)` — SIGINT yields exit code 130, SIGTERM yields 143 — printing `orangutan: interrupted by <name> (<num>)` to stderr. Two new free helpers (`signal_name(int)`, `signum_from_error(const Error&)`) round out the public surface. `xmake run orangutan` reports slice 23. Provider startup is still downstream. | First tool built-ins consume the assembly's `AuditSink`; first provider adapter. |
| Provider system          | C     | Layered design captured. | First adapter (Anthropic Messages). |
| Tool registry            | C     | The current `oran-tool` surface is implemented: `tool::Registry` (add / remove / find / catalog / dispatch) with transparent string lookup, schema validation before insertion, permission evaluation, audit recording, approval-broker mediation, workspace pre-resolution, the full advisory hook lifecycle, and spec-0014 output handling. `tool::CatalogRenderer` provides deterministic prompt-facing catalog bytes with a bounded 256-entry rendered-block cache and is now consumed by `oran-prompt`. `tool::Output` / `ToolUsage` / `Attachment` plus `OutputCapOptions` / `OutputCapReport` / `apply_output_caps` cover the text fallback, optional serialized `data_json`, usage counters, and text/data cap flags. Slice 66 applies `DispatchContext::output_caps` after successful handler returns and before `tool_after`; slice 67 then best-effort enriches the same pre-handler audit row with `metadata_json.usage` after caps are applied, preserving the one decision row per dispatch. Built-ins `file.read`, `file.write`, `file.edit`, `file.search`, `directory.list`, and `file.delete` all consume workspace pre-resolution when supplied, retain their text fallbacks, and fill measured usage counters; read-side/list/search tools also fill structured `data_json`. Slice 68 adds non-deferred `tool.search` as a capability-free registry metadata lookup returning text plus structured matched tool definitions and `usage.match_count`; slice 71 adds prompt-side promotion state in `oran-prompt`, while the agent-owned side effect that promotes `tool.search` matches remains future. Tests cover add / remove / find / catalog / dispatch, output envelope helpers, output-cap behavior, audit usage metadata enrichment, structured built-in payloads, usage/data propagation into hooks with default-vs-trusted redaction, mutation counters, catalog cache stats, `tool.search` metadata lookup, approval-broker paths, hook publishing, built-in schemas and behavior, workspace resolution, pre-permission audit metadata, override-root metadata, path-policy failure before ask approval, and malformed write options staying on the handler validation path. `test-tool` reports 166 cases / 1588 assertions. Bench `bench-tool` ships registry dispatch, file write/edit/search, approval, hook-bus, catalog-renderer, output-envelope A/B, and `output.apply_caps` scenarios. | Land provider adapter mapping per spec 0014; wire `tool.search` promotion side effects in `oran-agent`; land scheduler ownership of output-cap options and batched-call audit correlation for agent calls; land `tool::Runtime` and its capability-gated `workspace()` accessor when the runtime handle exists. |
| Prompt builder           | C     | `oran-prompt` now ships `prompt::Builder`, `BuilderInputs`, `BuilderOptions`, `CacheSection`, `RenderedPrompt`, `SectionVersions`, and `PromotionState`. The builder consumes `config::PromptActiveToolsConfig`, delegates active/deferred catalog bytes to `tool::CatalogRenderer`, consumes promoted-tool snapshots for next-turn full-schema rendering, emits the seven prompt-design sections, computes stable content hashes, a cache-versioned `prefix_hash` over sections 1-6, `prefix_bytes`, and one breakpoint before the conversation tail. `PromotionState` is explicit-time, caps promotions at 16 names with a 24-hour TTL, reports aggregate stats, and returns sorted snapshots for deterministic prompt bytes. Tests cover default active selection, explicit allowlists, explicit promotion of a deferred tool, builder consumption of promotion snapshots, LRU / TTL / invalid promotion names, missing explicit tool names, tail-independent prefix hashes, and cache-version invalidation; `test-prompt` reports 10 cases / 98 assertions. `bench-prompt` compares default active-set assembly (~6.0 µs), an explicit two-tool subset (~4.2 µs), promoted snapshots (~7.1 µs), and promotion-state promote+snapshot (~4.0 µs per batched operation). | Wire `tool.search` promotion side effects in `oran-agent`, then add real skill/memory renderers, provider cache mapping, and the `bench/oran-agent/prompt_cache_hit_rate.cpp` fixture. |
| Memory tiers             | C     | Tier design captured. | Long-term FTS5 v1. |
| Permissions              | C     | Foundation `oran-permission` covers verdict/mode/rule evaluation, capability-aware gating, defaults + config materialization, re2 input patterns, HMAC approval tokens, the bounded `ApprovalBroker`, and the audit pipeline. The audit surface now includes `AuditEvent`, `AuditOutcome`, `AuditMetadataUpdate`, and the async `AuditSink` interface with `record(AuditEvent)` plus slice-67 `update_metadata(AuditMetadataUpdate)`; `NullAuditSink` discards events, `RecordingAuditSink` captures and can update matching in-memory events, and `StorageAuditSink` persists through `AuditRepository::append_event` / `update_event_metadata`. `Registry::dispatch` still records the decision before handler side effects and uses the update hook only for best-effort post-result metadata enrichment. Tests cover rule precedence, capability/input-pattern composition, approval signing and replay, bounded grant eviction, config policy fields, audit record/update behavior, storage adapter NULL-hash handling, per-outcome storage round trips, repository-error propagation, bootstrap assembly wiring, and the registry broker handoff; `test-permission` now reports 88 cases / 403 assertions. Bench coverage remains the approval-secret / approval / approval-broker / audit-sink / bootstrap assembly buckets. | Render-side ask flow in `oran-agent` (operator prompt + token capture), and the hook bus that will publish `permission_ask_rendered` / `permission_ask_resolved` around the broker handoff. |
| Hooks                    | C     | Foundation `oran-hook` library now ships the cross-cutting observability surface: `hook::Event` enum (41 enumerators across agent/provider/tool/memory/channel/orchestration/automation/session/permission lifecycles), `hook::Mode { advisory, blocking }` annotation + `default_mode(Event)` returning `blocking` for the design-doc gate events (`tool_before`, `memory_*_before`, `permission_ask_rendered`) and advisory otherwise, abstract `hook::Sink` interface (async `record`-style `receive(Event, Payload)`) with `SinkKind::default_` / `SinkKind::trusted_local` trust labels, the `hook::InProcessSink` `std::function`-backed implementation with optional sink kind, and `hook::Bus` with `bind` / `unbind` / `publish_advisory` (the bus iterates subscribed sinks in subscription order, makes per-sink payload copies, redacts `ToolAfterPayload::data_json` for non-trusted-local sinks, captures per-sink `Result<void>` in the returned `PublishOutcome` with `sink_id` + `error`, and never aborts the publish on a sink error — advisory contract; slice 31 hardened this to also catch sink exceptions and stamp them as `Error::internal` with `sink=<id>`, so a throwing extension can no longer crash tool dispatch). Payload variant covers `std::monostate` (placeholder for events whose typed shape lands with the producing subsystem) plus the typed `ToolBeforePayload` / `ToolDispatchedPayload` / `ToolAfterPayload` / `ToolErrorPayload` the `Registry::dispatch` wiring publishes across slices 22 + 25; slice 60 adds hook-local `ToolUsage` copied from successful `tool::Output::usage` without making `oran-hook` depend on `oran-tool`, and slice 65 adds optional `ToolAfterPayload::data_json` for raw structured bytes that only trusted-local sinks receive. `Registry::dispatch` consults `DispatchContext::bus` after the registry resolves a tool def: when non-null, it publishes `tool_before` with the call's `(name, input_json, identity, started_at)`, `tool_dispatched` (slice 25) between audit success and the handler `co_await` on the paths where the handler actually runs (`allow` or ask-approved — payload carries the rule verdict wire spelling), `tool_error` (slice 25) on every error exit as a failure-only narrow channel, and `tool_after` at every exit with `(succeeded, output_text, usage, data_json for trusted-local sinks only, error_kind, error_message, started_at, finished_at, duration)`. Sinks subscribed to other events (provider, memory, …) see nothing yet — their producers do not exist. Tests cover bus bind/unbind idempotence, advisory publish across 0/1/3 sinks, sink-error swallowing, subscription order, per-sink raw-data redaction vs. trusted-local delivery, default/trusted `InProcessSink::kind`, the tool-dispatch publish on allow/deny/handler-error/ask-short-circuit/broker-reject paths, the slice-25 `tool_dispatched` publish on allow/ask-approved and its suppression on deny/ask-short-circuit/broker-reject, the slice-25 `tool_error` publish on handler-failure/deny/ask-short-circuit/broker-reject and its suppression on the allow happy path, the four-event ordering (before → dispatched → error → after) under a failing handler, the unknown-tool silent path, and the null-bus backwards-compat path. Bench: `bench-hook/publish_no_sinks` ~242 ns vs. `publish_one_sink` ~446 ns vs. `publish_three_sinks` ~698 ns (~204 ns per first sink, ~126 ns per additional sink); `bench-tool/dispatch_allow_no_hooks` ~2,071 ns vs. `dispatch_allow_with_empty_bus` ~2,417 ns vs. `dispatch_allow_with_two_sinks` ~2,986 ns (~346 ns "bus attached, nothing listens" tax, ~914 ns "bus attached with two observers" tax — the bus overhead is small relative to the audit-record cost at ~18 µs StorageAuditSink). | Blocking semantics with veto (currently advisory-only); first non-trivial external sink (`ShellSink`) for the design-doc bindings JSON; the typed payload shapes for provider/memory/channel events as those subsystems land. |
| Channels                 | C     | Trait + capability matrix designed. | QQ port + webhook adapter. |
| Orchestration / teams    | C     | Coordination strategies designed. | LeaderWorker + Pipeline. |
| Automation               | C     | Job categories designed. | Cron + periodic + triggered. |
| Web UI                   | C     | Routes + token-auth designed. | Single-page chat. |
| CLI                      | C     | `oran-cli` implements deterministic pre-agent-loop REPL and `--prompt` single-shot shells with tests and a bench. Provider-backed streaming is still downstream. | Connect CLI prompts to `oran-agent` once provider/tool foundations land. |
| Skills                   | D     | Spec drafted; no implementation. | Loader + watcher + `skill.invoke`. |
| Observability            | D     | Logging shim designed. | Metrics endpoint. |
| Security defaults        | B     | Captured. | Implement secret rotation. |
| Supply chain             | B     | Workflows pinned, lockfile present. | Add OSV scan once xmake lock is real. |
| Static analysis          | C     | `-fanalyzer` wiring shipped (rule + xmake option); focused `oran-io` and `oran-storage` analyzer builds pass locally, but analyzer CI remains a planned nightly gate. | Run/record analyzer coverage again for later descriptor-heavy code. |

Current bench note: slice 66 adds `bench-tool` coverage for
`output.apply_caps`, alongside the existing `output.text_only` and
`output.with_data_16kib` structured-output scenarios.

Current tool-registry note: slice 67 adds same-row audit usage metadata
enrichment after successful direct-dispatch output caps. The remaining
spec-0014 work is provider adapter mapping; spec-0012 scheduler work is
ownership of cap options and audit correlation for batched agent calls, not a
second copy of cap or usage-enrichment logic. Slice 68 adds the
registry-local `tool.search` lookup primitive for spec 0016; slice 69 adds
the typed active-tool config selector; slice 70 adds the prompt-builder
consumer; slice 71 adds prompt-side promotion state and builder snapshot
consumption. The `tool.search` side effect that mutates session promotion
state remains future `oran-agent` work.

## Cadence

Reviewed at every v1.x milestone. PRs that move a row's score change it (up or down)
in the same change.

## How To Move A Row Up

- D → C: implementation exists + smoke test passes.
- C → B: full test bucket green, design doc current.
- B → A: bench coverage in place, ≥ 30 days of stable operation, no known issues
  in the tech-debt tracker for that area.
