# Quality Score

Track quality by area so agents can prioritize the weakest parts of the system. The
table represents the *current* state of the rewrite. Update in the same PR that
materially changes an area.

## Scale

- `A` — strong coverage, stable behavior, clear docs, low operational risk.
- `B` — acceptable but has known gaps.
- `C` — works but needs targeted hardening.
- `D` — fragile, under-specified, or not yet implemented.
- `—` — not applicable (e.g., shaped only).

## Current State (Pre-v1)

| Area                     | Score | Why | Next Step |
| ------------------------ | ----- | --- | --------- |
| Architecture docs        | B     | Top-level map + design docs drafted. Some sections await concrete code. | Land MVP code; back-fill diagrams. |
| Build system             | B     | xmake skeleton lands in slice 0; GCC 16.1 toolchain detected, C++26 enforced, `compile_commands.json` autoupdates. Slice 26 (2026-05-20) shipped `scripts/check-deps.sh`, a real CI gate that walks `xmake/targets.lua` and rejects upward / undocumented-sideways library deps against the layering in [`design-docs/module-boundaries.md`](design-docs/module-boundaries.md); the script is wired into `scripts/ci.sh`. | Land per-library `check-compile-budget.sh` numbers + Clang secondary toolchain. |
| Compile-time discipline  | C     | Budgets captured; `oran-core`, `oran-async`, `oran-io`, and `oran-storage` build under the current target budget. Slice 27 (2026-05-20) shipped `scripts/measure-tu.sh` (per-TU compile-time emitter, table + `--json` output). Slice 28 (2026-05-20) shipped the versioned baseline at `compile_budget.json` (20 categories + 9 targets mirrored verbatim from [`docs/rules/compile-budget.md`](rules/compile-budget.md)) and the real `scripts/check-compile-budget.sh` regression gate that joins each measured TU against its library's `{median_sec, p95_sec, hard_cap_sec}` triple, classifies into ok/warn/fail/undef, and exits non-zero on hard-cap (or `undef`, treating a missing budget row as an env error rather than a regression). `--no-strict` demotes fail to warning during calibration; `--show-all` prints greens. Not wired into `scripts/ci.sh` yet (CI does not provision xmake; even when it does, gating needs to land on the documented reference hardware to avoid environmental drift firing as regressions). First local run on WSL2: 38 TUs (21 ok / 1 warn / 16 fail / 0 undef) — the 16 fail rows are hardware-driven; tracked in the new CI-wiring tech-debt row. | Wire `check-compile-budget.sh` into `scripts/ci.sh` once CI provisions xmake on the reference hardware. |
| Test framework           | C     | Catch2 v3 buckets green for `oran-core` (71 cases / 459 assertions), `oran-async` (14 cases / 76 assertions), `oran-http` (3 cases / 21 assertions), `oran-io` (54 cases / 311 assertions), `oran-storage` (77 cases / 988 assertions), `oran-config` (51 cases / 468 assertions), `oran-permission` (89 cases / 426 assertions), `oran-hook` (38 cases / 313 assertions), `oran-memory` (38 cases / 841 assertions; `--vector_memory=y` reports 36 / 791), `oran-automation` (106 cases / 1849 assertions), `oran-skill` (27 cases / 168 assertions), `oran-tool` (208 cases / 2181 assertions), `oran-prompt` (10 cases / 98 assertions), `oran-provider` (86 cases / 652 assertions), `oran-agent` (56 cases / 10 744 assertions), `oran-cli` (26 cases / 205 assertions), and `oran-bootstrap` (134 cases / 1160 assertions). | Loader snapshot, watcher hot-reload, one-shot `skill.invoke`, per-agent skill allowlist, runtime selector, per-agent prompt-overlay, transcript-derived active skill markers, explicit activation-policy resolver, explicit deactivation-policy input, explicit expiration-policy input, the slice-146 per-agent config source for deactivation/expiration, the slice-147 transcript-event `skill.deactivate` source, slice-148 session-store-backed activation records, slice-149 shared transcript activation-event extraction, slice-150 trace retention enforcement, slice-151 nested config strictness, slice-152 hook mutation input redaction, slice-153 atomic-write durability, slice-154 public `io::run_blocking`, slice-155 `DispatchContext::for_now`, slice-156 advisory hook fan-out, slice-158 shared hook payload snapshots, slice-159 runtime run-state coverage, slice-161 long-term FTS5 backend coverage, slice-162 long-term runtime recall coverage, slice-163 bootstrap long-term memory assembly coverage, slice-164 prompt-boundary long-term recall coverage, slice-165 configured-route recall policy coverage, slice-166 recall kind-filter coverage, slice-167 recall query-strategy coverage, slice-168 memory recall tool coverage, slice-169 memory remember tool coverage, slice-170 memory forget tool coverage, slice-172 hybrid long-term runtime coverage, and slice-174 hybrid-search config coverage, slice-175 bootstrap hybrid-search guard coverage, and slice-176 gated sqlite-vec vector backend coverage, slice-178 gated hybrid bootstrap/runtime coverage, and slice-179 memory write/forget lifecycle hook coverage, slice-180 memory read hook coverage, slice-181 memory recall-touch coverage, slice-182 memory decay-shadow coverage, and slice-183 retention config coverage, and slice-184 startup retention decay coverage, slice-185 startup decay diagnostics coverage, slice-186 startup decay hook coverage, slice-187 automation retention cadence coverage, slice-188 bootstrap retention job mapping coverage, slice-189 automation retention state repository coverage, slice-190 automation retention service tick coverage, slice-191 automation retention hook producer coverage, slice-192 automation runtime state-handle coverage, slice-193 automation retention loop-step coverage, slice-194 automation retention job-lifecycle hook coverage, and slice-195 automation retention lease coverage, and slice-196 automation retention loop-policy coverage, slice-197 automation cron evaluator coverage, slice-198 automation cron repository coverage, slice-199 automation cron runtime scan/wait coverage, slice-200 automation cron due-execution coverage, slice-201 automation cron loop-policy coverage, slice-202 automation cron lifecycle hook coverage, and slice-203 automation cron config seed coverage, slice-204 automation cron seed-apply coverage, and slice-205 automation cron service-cycle coverage, and slice-206 automation cron run-history coverage, slice-207 automation cron loop stop-policy coverage, slice-208 automation cron run-outcome coverage, slice-209 automation cron execution-lease coverage, slice-210 automation cron agent-lease coverage, slice-211 automation triggered-intake coverage, slice-212 automation triggered-execution coverage, slice-213 automation triggered-lifecycle-hook coverage, slice-214 automation triggered agent-lease coverage, slice-215 automation triggered queue/backpressure coverage, slice-216 automation triggered queue drain-one coverage, slice-217 automation triggered queue lease-conflict drop coverage, slice-218 async try-receive plus automation triggered queue drain-available coverage, slice-219 automation job `agent_prompt` config/repository/bootstrap coverage, slice-220 automation prompt-runner handler coverage, slice-221 bootstrap automation prompt-bridge coverage, slice-222 automation notifier callback coverage, slice-223 composed automation service-owner coverage, slice-224 automation service hold/retry plus delayed-attempt timing coverage, and slice-225 finite automation service loop-policy coverage are complete for `oran-config`, `oran-skill`, `oran-tool`, `oran-core`, `oran-storage`, `oran-memory`, `oran-automation`, `oran-hook`, `oran-io`, `oran-async`, and `oran-bootstrap`; broader non-bootstrap skill policy ownership remains downstream. |
| Bench harness            | C     | nanobench buckets green for `oran-core`, `oran-async`, `oran-http`, `oran-io`, `oran-storage`, `oran-config`, `oran-permission`, `oran-hook`, `oran-memory`, `oran-automation`, `oran-skill`, `oran-tool`, `oran-prompt`, `oran-provider`, `oran-agent`, `oran-cli`, and `oran-bootstrap`; core covers builder-vs-prefilled `Error` construction, time format-explicit-vs-chrono-format plus a parse cost scenario, Content visit-vs-get_if traversal plus a 32-block message walk, `ToolDef` aggregate-init-vs-`with_no_input` helper, the strict RFC-3629 `is_valid_utf8` walk vs. an ASCII-only short-circuit, the `Capability` linear-scan parse vs. `std::unordered_map` lookup batch, and the slice-44 `BoundedCache` insert+lookup A/B (`unordered_map` baseline ~7.7 µs / 1024 ops vs. `BoundedCache` ~16.5 µs — ~2× LRU bookkeeping overhead) plus an `overflow_lru_eviction` scenario (~30.8 µs) that pins the eviction-on-every-put path; http covers client construction vs. invalid-request validation on the public body-client boundary without network I/O; storage covers inserts, compiled-span migrations, SQL-file migration load+run, direct-vs-pool acquire, slice-157 reader-pool acquire contention (`storage.pool_reader_uncontended_acquire_batch` ~6.71 us vs. `storage.pool_reader_contended_fifo_acquire_batch` ~18.96 us per 32-acquire batch on a noisy local run), fresh-vs-cached prepare, pool fresh-vs-cached prepare, raw-pool-vs-session-repository append/load, and raw-pool-vs-audit-repository append/list (~992 µs vs. ~1219 µs over a 64-event batch — ~23% typed-repository overhead, ~3.5 µs per event over the raw ~15.5 µs); memory covers raw `SessionRepository` append/load (~849.8 us / 64-message batch) vs. typed `memory::session::Store` append/load (~1022.4 us / batch), plus `longterm::Runtime::search` through FTS5 over 10k rows (~15.08 ms / limit-10 query), the shared FTS5-vs-vector-vs-hybrid search comparison, and gated sqlite-vec vector-only (~3.03 ms) plus FTS5+sqlite-vec hybrid (~18.96 ms) rows; automation covers periodic schedule evaluation (~4.49 us / 1024-job batch) vs. memory-retention request planning (~15.10 us / 1024-job batch); permission covers `RuleSet::evaluate` precedence walk vs. a single-pass `std::ranges::find_if`, a capability-aware match-vs-miss A/B, the `Defaults::for_mode(default_)` factory build vs. the equivalent inline construction, a three-layer materialize A/B (defaults-only vs. defaults+global vs. defaults+global+per-agent), an `input_pattern` match vs. miss vs. no-pattern A/B (re2 cost on success / failure / removed), an HMAC-SHA-256 short-vs-long-message A/B plus a constant-time `macs_equal` ok-vs-no A/B (~6.4 us / 32 B, ~28.8 us / 1 KiB, ~60 ns / ~64 ns compare), an `ApprovalAuthority` issue-vs-verify-vs-early-reject-expired A/B (~9.9 us / ~9.3 us / ~36 ns — early reject ~250x faster), an `ApprovalBroker` approve-vs-check_ok-vs-check_no_grant-vs-check_exhausted A/B (~9.9 us / ~10.7 us / ~10.9 us / ~11.0 us — broker overhead over the raw authority verify is ~875 ns, the map find + counter decrement + reason build), and an audit-sink A/B/C plus `to_hex` baseline (~260 ns NullAuditSink / ~360 ns RecordingAuditSink / ~18.1 us StorageAuditSink / ~62 ns `to_hex`-of-32-bytes); hook covers concurrent `Bus::publish_advisory` fan-out/gather at 0/1/3 subscribed sinks (~309 ns / ~1.57 µs / ~3.77 µs), shared-redacted large-payload delivery at 1/3 default sinks (~4.05 µs / ~6.58 µs), plus blocking publish scenarios (`publish_blocking_no_sinks` ~390 ns, one sink ~2.80 µs, three sinks all-proceed ~6.91 µs, short-circuit-second ~4.80 µs); config covers parse-vs-file-load (now exercising `runtime.prompt.active_tools`) plus an empty-vs-typed `permissions` parse A/B and a typed-vs-input-pattern parse A/B (parsing 16 mixed-scope rules then a 14-rule block with 4 re2 input_patterns); CLI covers prompt-vs-REPL dispatch; tool covers registry.lookup-vs-registry.dispatch_allow (~8 ns vs. ~2,650 ns — dispatch overhead is dominated by the libsodium SHA-256 input hash + the coroutine spawn + the audit record), `file_write.dispatch_truncate` (~12.2 µs) vs. `file_write.dispatch_append` (~12.2 µs) for the file.write built-in (mode choice is within noise at small-payload sizes), `file_edit.dispatch_unique_replace` (~16.1 µs) vs. `file_edit.dispatch_replace_all_many` (~16.7 µs) for the file.edit built-in on a 1 KiB seed (the ~0.6 µs A/B delta confirms 63 extra substitutions cost less than ~10 ns each over the shared read+write floor), `file_search.single_file_one_match` (~8.2 µs) vs. `file_search.recursive_dir_many_matches` (~27.6 µs) for the file.search built-in (the ~19 µs delta is the `recursive_directory_iterator` walk + per-file open/read overhead on a 4-file / 14-line tree — the baseline a future memory-mapped scan or parallel walker would have to beat), the `file_search.literal_match_1kib` (~9.2 µs) vs. `file_search.regex_match_1kib` (~18.6 µs on the slice-24 cold-compile baseline; after slice 51 the steady-state bench path may hit the bounded compiled-regex cache, so cold compile cost should be measured with a unique-pattern scenario if needed), the slice-21 `dispatch_ask_short_circuit` (~2.4 µs) vs. `dispatch_ask_approved` (~13.4 µs) vs. `dispatch_ask_rejected` (~13.6 µs) trio that pins the approval-broker overhead inside `Registry::dispatch` (~11 µs over the short-circuit baseline, ~875 ns of which is the raw broker `check` from `bench-permission/approval_broker`; the remainder is the HMAC verify + audit-row reason swap on the rejected branch and the handler co_await on the approved branch), and the slice-22 `dispatch_allow_no_hooks` (~2.1 µs) vs. `dispatch_allow_with_empty_bus` ~2.4 µs vs. `dispatch_allow_with_two_sinks` ~3.0 µs trio that pins the hook-bus dispatch tap; prompt covers `prompt.build_default_active_set` (~6.0 µs), `prompt.build_explicit_subset` (~4.2 µs), `prompt.build_promoted_subset` (~7.1 µs), and `prompt.promotion_state_promote_snapshot` (~4.0 µs per batched operation) for prompt assembly/promotion-state sanity checks; bootstrap covers default-vs-explicit config startup plus the `RuntimeAssembly::build` audit-on-vs-audit-off A/B (~202 µs / ~400 ns post-`#embed` — provisioning the audit pipeline costs ~201 µs over the broker-only baseline, dominated by SQLite open + migration + Pool::open; slice 15's `#embed`-backed migrations shave a few µs off the slice-14 CWD-scan path) and the slice-23 `signal_drain_with_scope` (~11.2 µs) vs. `signal_drain_bare` (~443 ns) A/B that pins the per-drain `asio::signal_set` install + `release()` cancel cost (~10.7 µs, small relative to the ~205 µs assembly build); provider covers `provider.cache_hints_enabled` (~394 ns/mapping) vs. `provider.cache_hints_disabled` (~317 ns/mapping) for adapter-side prompt-cache mapping; and agent covers `agent.prompt_cache_no_promotions` (~56.6 us / fixture) vs. `agent.prompt_cache_after_promotion` (~63.0 us / fixture), aborting on `RenderedPrompt::prefix_hash` drift across changing conversation tails. `skill` covers order-trusting metadata concat vs. deterministic catalog render, and `bench-skill` is the current local comparison. No baseline JSON or regression gate yet. | Wire `scripts/bench-compare.sh` to a real baseline file. |
| Async model              | B     | `Runtime`, `Awaitable<T>`, cancel-aware `sleep_for`, and bounded `Channel<T>` with non-blocking `try_send` / `try_receive` are implemented with tests and a bench; `oran-storage::Pool` now consumes the executor for async writer/reader acquisition. Slice 23 added `bootstrap::SignalScope` — the first concrete consumer of the cancel-aware-drain contract — so SIGINT/SIGTERM interrupt the `bootstrap::run_audit_init` one-shot `io_context`. Slice 159 clarifies `Runtime::run()` as a one-shot lifecycle and translates escaping `io_context.run()` handler exceptions into `ErrorKind::internal` instead of letting them leave an IO worker. Mailbox policy and a fine-grained `asio::cancellation_signal` end-to-end refinement (replacing the slice-23 blunt `io.stop()`) remain downstream. | Add cancellation-latency bench coverage for the first orchestration workload. |
| IO runtime               | C     | `oran-io` file/directory MVP is implemented with tests and a bench; slice 42 adds `FileFingerprint` + `compute_file_fingerprint` as the first piece of spec 0011's file-view system (size + ns-resolution mtime, reserved optional SHA-256 hook); slice 43 layers the second piece — `io::read_text_file_ranged` returning `ReadTextResult { text, fingerprint, start_line, end_line, returned_bytes, truncated }`, `FileRange { LineSpan | ByteSpan }` input validation, mid-read fingerprint capture (size/mtime drift -> retry once for whole-file reads under 64 KiB, `Error::conflict` for larger or ranged reads), and dual-end UTF-8 code-point boundary alignment for byte ranges (the legacy `read_text_file` becomes a thin wrapper that errors when `truncated=true`); slice 50 adds the first bounded cache consumer in `oran-io`, a `core::BoundedCache`-backed line-offset index for line ranges in files larger than 256 KiB; slice 52 adds the bounded file-view cache for successful `ReadTextResult` payloads, revalidated with `stat` before every hit; slice 53 adds a bounded 64-entry singleflight table for concurrent cold `read_text_file_ranged` calls, with `ReadTextSingleflightStats` exposing leader/follower/completion/error counters plus current table size; slice 54 adds `ReadTextFileCacheStats` so the line-offset index and file-view cache expose bounded-state health without leaking keys or paths; slice 57 adds `invalidate_read_text_file_ranged_cache(path)` so in-process writes/deletes and watcher events can invalidate only one canonical path; slice 58 adds the Linux/inotify-backed `watch_read_text_file_ranged_cache(executor, root, options)` source with aggregate `ReadTextFileWatchStats` and cancellation-aware long-running semantics; slice 153 adds opt-in `WriteTextDurability` for atomic writes (`rename_only`, `fsync_file`, `fsync_file_and_parent`) plus PID+random exclusively-created temp leaves; slice 154 exports `io::run_blocking(executor, fn)` as the public cancel-aware boundary for short blocking callables returning `core::Result<T>`. `test-io` reports 54 cases / 311 assertions. Subprocess, glob, pipe, signal, bootstrap/config watcher startup wiring, and content hashing are still downstream. | Add subprocess/signal helpers after permission and hook surfaces are ready to wrap them. |
| Storage / DBs            | C     | Expected-only SQLite `Connection` / `Statement` core (now including BLOB bind/read and optional per-open SQLite auto-extension registration), migration runner with SQL-file directory loading and compile-time-embedded built-in migrations (`built_in_audit_migrations()` / `built_in_session_migrations()` / `built_in_trace_migrations()` reach the SQL via C++26 `#embed`), async writer/reader `Pool`, per-slot pool `StatementCache`, standalone per-connection `StatementCache` (LRU, hit/miss/eviction counters, transient overflow), `SessionRepository` (typed `core::Role` at the message API boundary plus slice-148 `session_skill_activations` upsert/load rows for latest per-session skill active/inactive state), `AuditRepository` (`audit_events` table backed by `migrations/audit/0001-audit-initial.sql`, append/update-metadata/list/count with scope-key partitioning + secondary `agent_key`/`tool_name`/`event_kind`/`outcome` filters + scope-keyed indexes, plus nullable `parent_turn_id` BLOB correlation, event-kind discrimination, and parent/event-scoped metadata updates), and `TraceRepository` (`trace_turns` table backed by audit DB migration `migrations/audit/0002-trace-turns-initial.sql`, append/get/list/count plus provider usage rollups and explicit-cutoff purge over 16-byte BLOB turn/session ids, prompt/cache hashes, token/cost fields, optional cancellation phase, and redacted context bytes) are implemented with WAL/foreign-key setup, schema versioning, RAII leases, FIFO waiter resumption, cancellation, tests, and benches. Slice 67 adds same-row metadata replacement for post-result tool usage; slice 78 adds the storage trace primitive for spec 0018, including audit-v1-to-trace-v2 upgrade coverage; slice 79 adds `audit_events.parent_turn_id` as migration version 3 and validates/scopes that join key through storage audit APIs; slice 80 adds the first `oran-agent` terminal-success writer that consumes `TraceRepository`; slice 81 adds the typed trace policy config surface; slice 82 adds the loop-side explicit trace-disabled policy that skips the writer and keeps direct-dispatch audit parent ids NULL; slice 83 adds provider/tool cancellation trace rows; slice 84 adds provider/loop-boundary error trace rows; slice 85 generates missing turn ids for configured trace writers; slice 88 adds the operator-level `list_events_for_turn` read consumed by the new `--trace` inspector; slice 93 adds `audit_events.event_kind` as migration version 4 so direct `hook_publish` rows can share the trace/audit join without metadata clobbering; slice 127 adds `TraceRepository::list_provider_usage_rollups` for UTC-day/agent/profile/model aggregation over the recorded trace usage columns; slice 150 adds `TraceRepository::purge_turns_started_before` for trace-row retention while leaving audit rows untouched; `test-storage` reports 77 cases / 988 assertions and `test-agent` reports 25 cases / 401 assertions for the loop join / disabled-policy / cancellation/error/generated-id/iteration-cap / approval-clock / structured-output preservation paths. Memory-tier schemas above the pool now live in `oran-memory`; slice 189 adds the automation retention repository above the same generic pool in `oran-automation`, slice 190 adds the caller-driven service tick above that repository, slice 191 adds optional advisory hook publishing above the same automation service, slice 192 adds a caller-owned automation runtime handle above storage, and slice 193 adds a caller-started retention loop step above that same service/runtime state, slice 194 adds job lifecycle hook publishing above the same service/runtime state, slice 195 adds automation-owned retention lease rows above the same generic pool, slice 196 adds finite caller-owned loop policy above the same automation repository/service stack, and slice 198 adds automation-owned cron job rows, slice 206 adds automation-owned cron run rows, slice 209 adds automation-owned cron execution lease rows, slice 210 adds automation-owned cron agent lease rows, slice 211 adds automation-owned triggered job descriptor rows, slice 212 adds automation-owned triggered run rows, and slice 214 adds automation-owned triggered agent lease rows above the same generic pool, so `oran-storage` remains schema-generic for automation. | Keep storage generic while automation and memory runtimes own domain schemas above the same pool/migration primitives. |
| Config                   | C     | Expected-only JSON loader is implemented with typed runtime/profile/route/session/web/trace/hooks/memory/automation fields, including `runtime.tool_output.max_text_bytes` / `max_data_bytes` defaults for structured tool-output caps, `runtime.prompt.active_tools` for the prompt-builder active set (`"defaults"` or an explicit allowlist, preserving author order and rejecting empty tool names), optional `profiles.<name>.protocol` strings for provider route resolution, spec-0018 `trace.enabled` / `trace.store_raw_bodies` / `trace.retention_days` policy defaults, `hooks.timeout_ms` for blocking hook deadlines, and slice-167 `memory.longterm.recall.enabled` / positive `limit` / optional non-empty unique `kinds` / `query_strategy` policy for configured-route prompt-boundary recall, plus slice-174 `memory.longterm.hybrid_search` typed policy (`enabled`, positive lexical/vector/result limits, non-negative weights, and non-zero combined weight) and slice-183 `memory.longterm.retention` typed policy (`forget_after_unused_days`, `importance_floor`, `max_records_per_scope`, `decay_check_interval_hours`, explicit-unit defaults, and bounds validation), plus slice-203 `automation.cron.jobs[]` typed cron seed parsing (`job_key`, optional `agent_key` defaulting to `automation`, required non-empty `agent_prompt`, POSIX cron `expression`, UTC `first_fire_at`, optional UTC `last_fired_at`, and unique keys), recursive env substitution, strict unknown-field handling at the root plus typed nested provider profile/pricing, route, hook, memory, permission, workspace, and agent sections, `config.example.json`, tests, and a bench. The typed surface now also covers the `permissions` root block and `agents.<name>.permissions` overlays (rules carry pre-resolved `core::Capability`, verdict keys validate against `allow`/`deny`/`ask`, unknown verdict / rule / agent fields warn or fail per `strict_config`) plus optional `agents.<name>.prompt_overlay` stable section-6 prompt text and optional `agents.<name>.skills_enabled` arrays for bootstrap prompt-runner selection plus slice-146 optional `agents.<name>.skills_deactivated` names and `agents.<name>.skills_expirations` `{name, expires_at}` rows feeding the bootstrap-built `skill::ActivationPolicy`, and each rule may now carry an `input_pattern` re2 source string that the loader validates by compiling once via re2 (invalid patterns surface as `Error::config` with the JSON path attached). Each rule also carries optional `replay_max` (uint32) and `approval_ttl_seconds` (int64) so operators can override the design-doc approval-window defaults per rule (negative or non-integer values reject at parse time with the JSON path attached). Slice 87 wires `config.trace().enabled` through `bootstrap::run` into `RuntimeAssemblyOptions::trace_enabled`, slice 92 wires `config.hooks().timeout_ms` into `RuntimeAssemblyOptions::hook_blocking_timeout`, slice 102 lets route resolution distinguish the vendor/operator label from the wire protocol, slice 150 has `bootstrap::run` consume `trace.retention_days` by deriving the trace-row purge cutoff passed to `RuntimeAssembly`, slice 151 closes the deep-review config strictness sweep for typed nested provider/route/hook sections while preserving reserved hook `sinks` / `bindings` placeholders, slice 165 maps the typed memory recall policy into bootstrap's long-term recall runner option, slice 166 maps recall kind filters into the prompt-boundary query, slice 167 maps recall query strategy into the runner, slice 174 validates the parser-level hybrid-search policy, slice 183 validates the parser-level retention policy before execution owners consume it, slice 178 maps enabled hybrid search into configured-route bootstrap under `--vector_memory=y` while default builds reject `enabled=true` with `reason=build_option_disabled` / `option=vector_memory`, and slice 176 provides the optional library-level sqlite-vec vector backend, and slice 203 maps typed automation cron seeds into bootstrap `RuntimeAssembly` descriptors without opening `automation.db`, slice 210 carries cron `agent_key` through those descriptors, and slice 219 carries required cron `agent_prompt` through the same descriptor path. Generated schema, secret crypto, end-to-end loop consumption of `store_raw_bodies`, typed hook sink/binding models, and the remaining typed agent fields (provider/model override and hook bindings) are still downstream. | Generate JSON Schema, add secret-field support, thread `store_raw_bodies` into the future trace runtime, and type hook sink/binding plus remaining agent fields. |
| Bootstrap                | C     | Process entry routes through `oran-bootstrap`, loads config, preflights the configured default provider route, exposes `--explain-rules`, `--audit-init`, and `--trace <turn-id>`, and assembles runtime services for audit, trace, hooks, workspace policy, provider HTTP transport, session memory, long-term memory, prompt rendering, and configured-route `AgentPromptRunner` execution. Recent prompt-runtime slices added session persistence, once-per-turn memory framing, the stable system preamble, workspace skill loading/hot-reload, `skill.invoke`, per-agent `skills_enabled` filtering, runtime `--mode` / `--agent` selectors, selected-agent `prompt_overlay` handoff into stable section 6, and transcript-derived active skill markers resolved through `skill::ActivationPolicy` in section 4 while exact caller-supplied overlay bytes remain authoritative. The public policy now accepts explicit deactivation names, expiration rows, and durable session activation rows: slice 146 sources config deactivation/expiration inputs, slice 147 adds the runner-installed `skill.deactivate` callback, slice 148 loads/persists latest `skill.invoke` / `skill.deactivate` active-state rows through `memory::session::Store` so transcript compaction cannot lose the latest decision, and slice 149 has the runner consume `skill::skill_activation_events_from_transcript(...)` instead of owning a second skill-result parser. Slice 150 has bootstrap derive an explicit trace retention cutoff from `config.trace().retention_days` and apply it before exposing the assembly-owned trace repository; slice 163 adds assembly ownership for `<workspace>/.orangutan/memory.db`, `memory::longterm::Fts5Backend`, and `memory::longterm::Runtime`, enabled only on configured provider routes from `bootstrap::run`; slice 164 adds opt-in `AgentPromptRunnerOptions::longterm_recall`, which queries that runtime once at the prompt boundary; slice 165 maps `memory.longterm.recall.enabled` / `limit` from config into that runner option while keeping recall disabled by default; slice 166 maps optional `memory.longterm.recall.kinds` into `memory::longterm::Query::kinds`; slice 167 maps `memory.longterm.recall.query_strategy` into current-prompt or last-user-message recall query text; slice 168 binds `memory.recall` through `DispatchContext::memory_recall` to the assembly-owned long-term runtime; slice 169 binds `memory.remember` through `DispatchContext::memory_remember` to the assembly-owned long-term backend; and slice 170 binds `memory.forget` through `DispatchContext::memory_forget` to that backend's idempotent scoped delete path. Slice 178 consumes configured-route `memory.longterm.hybrid_search.enabled=true` under `--vector_memory=y`: runtime assembly owns `.orangutan/memory-vectors.db`, `SqliteVecBackend`, and `HybridRuntime`; enabled prompt-boundary recall plus `memory.recall` use hybrid search; and `memory.remember` / `memory.forget` mirror vector writes/deletes. Default builds still reject enabled hybrid search before runtime assembly or provider construction with `reason=build_option_disabled`, `option=vector_memory`. Slice 179 adds blocking `memory_write_before` for `memory.remember`, advisory `memory_write_after` / `memory_forget`, and veto/no-persist coverage; slice 180 adds advisory `memory_read_after` publishing for prompt-boundary recall and `memory.recall` with trusted-local raw query/hit capture and default-sink redaction; slice 186 adds build-only startup hook bindings plus advisory `memory_decay` publishing after the configured-route startup retention pass; and slice 188 maps configured-route retention policy into the automation-owned `MemoryRetentionJob` descriptor stored on `RuntimeAssembly`, and slice 203 maps typed `automation.cron.jobs[]` seeds into `RuntimeAssembly::cron_jobs()` descriptors without opening `automation.db`, slice 204 proves caller-owned `AutomationRuntime` can explicitly apply those descriptors, slice 219 carries each cron seed's required `agent_prompt` through the same bootstrap descriptor path, and slice 221 adds the bootstrap-owned `make_automation_agent_prompt_runner(...)` bridge plus the fail-closed `AgentPromptRunnerOptions::bind_operator_prompt_sink` knob for noninteractive automation callers. `test-bootstrap` reports 134 cases / 1160 assertions by default and 120 cases / 958 assertions with `--vector_memory=y`. | Semantic/external embedding provider, blocking `memory_read_before`, automation service-loop ownership, and optional `MEMORY.md` mirror remain downstream. |
| Provider system          | C     | `oran-provider` now exists as a provider-domain + fake-provider + route-resolver + execution-runtime + offline protocol request/response library plus an injected protocol transport seam. `<oran/provider.hpp>` exports the slice-73 cache-mapping shapes (`Request`, `Response`, `Usage`, `RetryPolicy`, `PromptCacheHints`, `PromptCacheOptions`, `make_prompt_cache_hints`) plus the slice-74 system surface (`ProtocolKind`, `ModelTarget`, `ProviderPricing`, `Route`, abstract `EventSink` with default no-op delta callbacks, abstract `System::send(Request, Route, EventSink*) const`, and `FakeProvider` — the first concrete `System`) plus slice-97 `provider::execution::Runtime`, a `System` decorator that consumes `Request::retry`, retries retryable errors per target, tries route fallbacks, observes cancellation during backoff, suppresses retry/fallback after visible stream output, and fills missing `Response::model_used` with the selected target model, plus slice-98/102/103 route resolution, slice-104 adapter planning, slice-105 credential resolution, slice-106 adapter factory dispatch, and slices 107-109 protocol request/response/transport mapping. Slice 129 carries optional `profiles.<name>.pricing` through route resolution to `ModelTarget::pricing` so `agent::Loop` can calculate missing provider usage costs without making protocol adapters own pricing. `provider::ProtocolTransportAdapterFactory` builds Anthropic/OpenAI Responses `System` backends over an injected `ProtocolTransport`, streams either shipped protocol when `request.stream` is true and the transport supports streaming, injects provider API-key headers, maps HTTP status classes into provider error categories, and rejects route profile/model/protocol mismatches before sending. Slice 111 adds bootstrap's `HttpProviderBackend` as the first `oran-http::Client`-backed `ProtocolTransport` adapter outside `oran-provider`, and slice 112 consumes that backend from configured-route `bootstrap::run`. Slices 122 and 124 add the Anthropic Messages and OpenAI Responses streaming decode paths: `ProtocolTransport::send_streaming(ProtocolHttpRequest, ProtocolSseCallback)` plus a `supports_streaming()` capability gate (default false), the stateful `AnthropicSseDecoder` and `OpenAiResponsesSseDecoder` siblings of `decode_protocol_response`, and `ProtocolTransportSystem` honoring `request.stream` for both protocols; the slice-97 `AttemptSink` already covers stream retry/fallback suppression with no new code. Slice 126 deliberately keeps provider lifecycle hooks outside `oran-provider`: `agent::Loop` publishes metadata-only request/response/error/fallback events around calls into any `provider::System`, while this library remains a hook-free provider domain. `test-provider` reports 86 cases / 652 assertions; `bench-provider` still reports `provider.cache_hints_enabled` about 394 ns / mapping vs. disabled about 317 ns / mapping. | Add remaining protocol families. |
| Agent runtime            | C     | `oran-agent` now owns the slice-72 session-state promotion surface plus the slice-80 sequential direct-dispatch `agent::Loop` foundation. `agent::SessionState` owns `prompt::PromotionState`, observes successful `tool.search` structured output, promotes only deferred matches into the next prompt snapshot, ignores non-search / failed-search outputs, rejects malformed successful payloads without partial mutation, and keeps JSON parsing private to the `.cpp`. `agent::Loop` builds the rendered prompt from stable section inputs, maps cache hints, mirrors active/promoted tools into name-sorted `provider::Request::tools`, sends requests through `provider::System`, dispatches `tool_use` blocks through a caller-supplied `tool::Registry` / `DispatchContext`, refreshes `DispatchContext::now` for every direct tool call while preserving reusable context fields, appends ordered `Role::tool` result messages, preserves successful `tool::Output::data_json` on provider-facing `ToolResultContent`, re-enters the provider, aggregates provider usage, computes missing provider cost estimates from selected route-profile pricing while preserving provider-supplied costs, returns the complete transcript tail, converts model-repairable tool errors into tool-result error blocks, annotates parent-cancelled provider/tool awaits with `reason=parent_cancelled` plus `cancellation_phase=provider|tools`, writes cancelled trace rows for provider/tool parent cancellations when trace is enabled, writes `stop_reason=error` trace rows for non-cancelled provider failures and response-backed loop-boundary failures, generates a non-zero turn id when an enabled trace writer is configured and callers leave `RunTurnInputs::turn_id` unset, stamps direct-dispatch audit rows with that id when trace is enabled, clears/restores reusable context ids when tracing is explicitly disabled, writes one body-free `trace_turns` row for enabled terminal-success turns when `RunTurnInputs::trace` supplies a `TraceRepository`, propagates storage/internal dispatch failures, and enforces `LoopOptions::max_iterations`. Slice 101 proves the loop can run under bootstrap's `AgentPromptRunner` with the execution-runtime wrapper and runtime-assembly services supplied, and slice 112 uses that runner from configured-route ordinary `bootstrap::run`. Slices 116-120 land `agent::ToolScheduler` (bounded parallelism, per-canonical-path read/write locks, per-call timeout, a 100 ms parent-cancellation guarantee, and the `cancellation_lag` audit row) and wire `agent::Loop` to dispatch every tool batch (including N=1) through `scheduler.run_batch`; `AgentPromptRunner` owns a persistent scheduler built from the `runtime.tool_scheduler.*` config block. Slice 126 adds optional `RunTurnInputs::bus` plus scope/agent identity metadata; the loop publishes advisory provider request/response/error/fallback hook payloads around provider awaits, and resets coroutine cancellation before post-provider advisory cleanup so parent-cancelled provider turns still report the original cancellation error. Slice 131 makes bootstrap expose an assembly-owned `memory::session::Store`, slice 132 has `AgentPromptRunner` persist/reload transcript history through it, slice 133 keeps prompt memory framing as a runner-rendered once-per-turn input, slice 134 adds `agent::SystemPreamble` / `SystemPreambleOwner` plus the loop-owned default preamble fallback for empty `RunTurnInputs::system_preamble`, slice 135 adds `skill::CatalogOwner` as the stable section-4 owner, slice 136 feeds that owner from a bootstrap-loaded `skill::Loader` snapshot, slice 137 includes `skill.invoke` in the default active tool set while preserving the stable prompt prefix by returning skill bodies as tool results, and slice 138 lets bootstrap refresh that skill snapshot between prompts without changing mid-turn prompt bytes. `test-agent` reports 56 cases / 10 744 assertions, including default-preamble stability, provider hook request/response, error, fallback attribution, and profile-priced cost coverage. `bench-agent` runs the prompt-cache stability fixture (`agent.prompt_cache_no_promotions` about 56.6 us / fixture, `agent.prompt_cache_after_promotion` about 63.0 us / fixture) plus the slice-120 `scheduler_overhead` (AC12) and `scheduler_audit_fanout` scenarios. | Skill activation policy remains outside `oran-agent`; cross-runtime policy can build on the bootstrap/memory source without moving section rendering into the loop. |
| Tool registry            | C     | The current `oran-tool` surface is implemented: `tool::Registry` with transparent lookup, schema validation, workspace pre-resolution, permission/audit/approval mediation, advisory hook fan-out, blocking `tool_before` consumption, blocking `permission_ask_rendered` broker bridging, spec-0014 output handling, and slice-155 `DispatchContext::for_now(...)` factories for current-clock base contexts plus prototype clone/refresh before scheduler dispatch. Slice 91 consumes `veto` / rewrite / `require_approval` decisions before workspace and permission evaluation; slice 92 carries timed-out blocking sinks through the same `blocked_by_hook` path and serializes `metadata_json.hook_decisions[].elapsed_ms`; slice 93 writes joinable `event_kind=hook_publish` rows for traced blocking `tool_before` publishes before recording the ordinary permission-decision row; slice 94 publishes typed ask prompts for direct dispatch, issues/checks broker grants on `proceed`, returns `operator_denied` on `veto`, and records `metadata_json.permission_ask_decisions[]` while preserving no-sink `approval_required`; slice 95 adds the terminal CLI sink that can answer those prompts; slice 152 fills hash-and-byte-count `redacted_input_json` summaries for `file.write` / `file.edit` hook payloads. Built-ins cover the current filesystem set plus `tool.search`, `skill.invoke`, slice-147 `skill.deactivate`, slice-168 `memory.recall`, slice-169 `memory.remember`, and slice-170 `memory.forget`, and tests cover dispatch, output caps, audit usage metadata, parent-turn stamping, hook output/input redaction, blocking hook decisions, timeout handling, hook-publish rows, ask-prompt approval/veto/no-sink paths, workspace policy, built-in schemas, structured payloads, skill invocation/deactivation plus memory recall/remember/forget callback boundaries, and the dispatch-context factory. Slice 107 consumes structured tool-result bytes at the provider request-mapping boundary for Anthropic/OpenAI tool results. `test-tool` reports 208 cases / 2181 assertions. Bench `bench-tool` ships registry dispatch, file write/edit/search, approval, hook-bus, catalog-renderer, output-envelope A/B, and `output.apply_caps` scenarios. | Land scheduler ownership of output-cap options and batched-call audit correlation for agent calls; bind the CLI operator-prompt sink into the real binary agent-loop runtime; land `tool::Runtime` and its capability-gated `workspace()` accessor when the runtime handle exists. |
| Prompt builder           | C     | `oran-prompt` now ships `prompt::Builder`, `BuilderInputs`, `BuilderOptions`, `CacheSection`, `RenderedPrompt`, `SectionVersions`, and `PromotionState`. The builder consumes `config::PromptActiveToolsConfig`, delegates active/deferred catalog bytes to `tool::CatalogRenderer`, consumes promoted-tool snapshots for next-turn full-schema rendering, emits the seven prompt-design sections, computes stable content hashes, a cache-versioned `prefix_hash` over sections 1-6, `prefix_bytes`, and one breakpoint before the conversation tail. `PromotionState` is explicit-time, caps promotions at 16 names with a 24-hour TTL, reports aggregate stats, and returns sorted snapshots for deterministic prompt bytes; `agent::SessionState` is now the first owner that mutates it after `tool.search`, and `provider::make_prompt_cache_hints` is now the first adapter-side consumer of the rendered cache boundary. Tests cover default active selection, explicit allowlists, explicit promotion of a deferred tool, builder consumption of promotion snapshots, LRU / TTL / invalid promotion names, missing explicit tool names, tail-independent prefix hashes, and cache-version invalidation; `test-prompt` reports 10 cases / 98 assertions. `bench-prompt` compares default active-set assembly (~6.0 µs), an explicit two-tool subset (~4.2 µs), promoted snapshots (~7.1 µs), and promotion-state promote+snapshot (~4.0 µs per batched operation); `bench-agent` now pins the session-owned prompt-cache fixture. Slice 133 adds the first memory framing owner outside `oran-prompt`, slice 134 adds the first system-preamble owner outside `oran-prompt`, and slices 135-149 add the skill-catalog owner, loader snapshot, `skill.invoke` result path, prompt-boundary skill hot-reload, per-agent filtering, runtime selector wiring, prompt overlays, transcript-derived active markers, explicit activation policy, explicit deactivation policy, explicit expiration policy, the per-agent config source for deactivation/expiration, the transcript-event `skill.deactivate` source, durable session activation rows, and shared activation-event extraction outside `oran-prompt`; `prompt::Builder` still consumes stable section-1, section-4, and section-5 bytes as inputs while invoked skill bodies enter only through the conversation tail. | Cross-runtime skill policy can decide future section-4 shifts without changing the prompt builder boundary. |
| Memory tiers             | C     | Tier design captured, and slice 130 ships the first implementation target: `oran-memory::session::Store` wraps `storage::SessionRepository` with typed `SessionId` / `AgentKey` / `SessionSummary` values and private `core::Message` JSON serialization. `test-memory` covers ordered round-trips, agent scoping, malformed-row rejection, id validation, the 500-message session criterion, and slice-148 skill activation update/record validation; `bench-memory` compares raw repository append/load (~849.8 us / 64-message batch) to typed memory wrapper append/load (~1022.4 us / batch), records the 10k-row FTS5 `longterm::Runtime::search` baseline (~15.08 ms / limit-10 query), and now reports gated sqlite-vec vector-only (~3.03 ms) plus FTS5+sqlite-vec hybrid (~18.96 ms) rows on the shared corpus. Slice 131 wires runtime assembly ownership for a separate `<workspace>/.orangutan/sessions.db`, including migration, a dedicated pool/repository/store, public `session_store()` / `sessions_path()` accessors, and configured-route enablement while no-route startup stays disabled. Slice 132 closes the runner wiring by loading persisted history from `session_store()` at prompt start and appending the successful transcript suffix after each turn. Slice 133 adds `memory::FramingOwner` for prompt section 5 and proves the runner renders it once per prompt before loop iterations; slice 148 records/loads latest skill activation state through the same session owner; slice 160 adds the public `memory::longterm` record/query/write/touch contracts, lexical `Backend`, vector `VectorBackend`, and validation helpers; slice 161 adds the default `memory::longterm::Fts5Backend` over SQLite FTS5 with embedded migrations, scoped CRUD/search/touch, kind/shadow filters, lexical scores, `not_found` get misses, and idempotent deletes; slice 162 adds `memory::longterm::Runtime` plus deterministic recall framing from returned hits; slice 163 wires configured-route bootstrap assembly ownership for `memory.db`, the default backend, and runtime accessors; slice 164 lets prompt-runner callers opt into one prompt-boundary recall from that runtime while preserving record-only section-5 bytes; slice 165 lets ordinary configured-route startup enable that recall through `memory.longterm.recall.enabled` / `limit` while leaving the default off; slice 166 adds optional configured kind filters for that prompt-boundary query; slice 167 adds the first configured query-derivation selector (`prompt_text` or `last_user_message`); slice 168 adds read-only `memory.recall` plus structured recall `data_json` rendering; slice 169 adds structured saved-record `data_json` rendering for `memory.remember`; slice 170 adds structured scoped removed-key `data_json` rendering for `memory.forget`; slice 172 adds `memory::longterm::HybridRuntime`, which composes lexical and vector backends, hydrates vector-only records, skips stale vector rows, and returns deterministic weighted hits plus recall framing; and slice 174 adds the parser-level `memory.longterm.hybrid_search` policy for positive lexical/vector/result limits plus non-negative weights; slice 175 first added the default-build guard for enabled hybrid search; slice 176 adds `SqliteVecBackend`, the optional `--vector_memory=y` sqlite-vec `VectorBackend` with scoped upsert/search/remove coverage plus dimension-drift validation; and slice 178 adds deterministic local text/record embedding helpers plus gated configured-route hybrid recall wiring through assembly-owned vector memory. Slice 181 adds recall-side read-touch metadata, slice 182 adds the library-level `DecayRequest` / `Backend::decay` shadow transition with FTS5 coverage for scoped stale-record hiding, and slice 183 adds parsed `memory.longterm.retention` policy, slice 184 applies it during configured-route startup, slice 185 exposes the startup decay shadow count through `RuntimeAssembly` diagnostics plus the startup banner, slice 186 adds startup `memory_decay` hook metadata plus build-only startup hook binding coverage, slice 188 maps the configured retention policy into a stored automation-owned periodic job descriptor without moving periodic execution into memory, and slice 189 persists that descriptor's automation-owned job/run/last-fired state without making memory own periodic execution, and slice 190 adds a caller-driven `MemoryRetentionService::tick(...)` execution boundary that runs due decay through a supplied backend and records outcomes, slice 191 publishes advisory periodic `memory_decay` from that explicit tick owner when callers supply a hook bus, and slice 192 adds the caller-owned `AutomationRuntime` state handle for automation DB open/migrate ownership, and slice 193 adds the caller-started `MemoryRetentionLoop::run_once(...)` step without moving periodic execution into memory, and slice 194 adds advisory job lifecycle metadata from due retention ticks without moving lifecycle ownership into memory, and slice 195 adds retained job leases plus loop-side due-run ownership without moving periodic execution into memory, and slice 196 adds finite caller-owned loop policy above that leased step. Default `test-memory` reports 38 cases / 841 assertions, `--vector_memory=y` `test-memory` remains last reported at 36 cases / 791 assertions, `test-config` reports 51 cases / 468 assertions, `test-hook` reports 37 cases / 299 assertions for retention-policy config plus memory hook payload/redaction coverage, and `test-bootstrap` reports 134 cases / 1160 assertions by default and 120 cases / 958 assertions with `--vector_memory=y` for assembly, prompt-runner, configured-route startup, hybrid recall when recall is enabled, memory-tool vector mirroring, slice-179 memory lifecycle hook publishing/veto behavior, default-build slice-180 memory read-after publishing coverage, slice-181 recall touch coverage, slice-185 startup decay diagnostics coverage, slice-186 startup decay hook coverage, and slice-188 retention job descriptor mapping coverage, plus slice-189 repository coverage, slice-190 service tick coverage, and slice-191 periodic `memory_decay` hook coverage plus slice-192 runtime state-handle coverage and slice-193 retention loop-step coverage and slice-194 job lifecycle hook coverage plus slice-195 retention lease coverage and slice-196 finite loop-policy coverage plus slice-197 cron schedule coverage, slice-198 cron repository coverage, slice-199 cron runtime scan/wait coverage, slice-200 cron due-execution coverage, slice-201 cron loop-policy coverage, and slice-202 cron lifecycle hook coverage plus slice-203 cron config seed coverage, slice-204 cron seed-apply coverage, and slice-205 cron service-cycle coverage plus slice-206 cron run-history coverage plus slice-207 cron loop stop-policy coverage plus slice-208 cron run-outcome coverage plus slice-209 cron execution-lease coverage plus slice-210 cron agent-lease coverage plus slice-211 triggered-intake coverage, slice-212 triggered-execution coverage, slice-213 triggered-lifecycle-hook coverage, and slice-214 triggered agent-lease coverage through `test-automation` at 84 cases / 1302 assertions, and slice-221 bootstrap automation prompt-bridge coverage for stable per-job session reuse and fail-closed noninteractive approvals. | Semantic/external embedding provider, blocking `memory_read_before`, automation service-loop ownership, and optional `MEMORY.md` mirror remain downstream. |
| Permissions              | C     | Foundation `oran-permission` covers verdict/mode/rule evaluation, capability-aware gating, defaults + config materialization, re2 input patterns, HMAC approval tokens, the bounded `ApprovalBroker`, and the audit pipeline. The audit surface now includes `AuditEvent`, `AuditOutcome` (including the slice-91 hook outcomes `blocked_by_hook` and `rewritten`), `AuditMetadataUpdate`, and the async `AuditSink` interface with `record(AuditEvent)` plus slice-67 `update_metadata(AuditMetadataUpdate)`; `NullAuditSink` discards events, `RecordingAuditSink` captures and can update matching in-memory events, and `StorageAuditSink` persists through `AuditRepository::append_event` / `update_event_metadata`. Slice 79 adds typed `parent_turn_id` to audit records and metadata updates, and slice 93 adds `event_kind`; both in-memory and storage sinks match on those fields during enrichment. `Registry::dispatch` still records the decision before handler side effects and uses the update hook only for best-effort post-result metadata enrichment; blocking `tool_before` can now set the hook-specific outcomes before that row is recorded, and slice 94 resolves direct `ask` decisions through blocking `permission_ask_rendered` by issuing/checking broker grants on `proceed` or returning `operator_denied` on `veto`; slice 95 adds the CLI sink that supplies those operator approval/denial decisions for terminal callers. Tests cover rule precedence, capability/input-pattern composition, approval signing and replay, bounded grant eviction, config policy fields, audit record/update behavior, parent-turn-scoped metadata updates, storage adapter NULL-hash handling, per-outcome storage round trips, repository-error propagation, bootstrap assembly wiring, the registry broker handoff, the direct ask bridge, and the new hook audit outcomes; `test-permission` reports 89 cases / 426 assertions, `test-tool` reports 208 cases / 2181 assertions for the dispatch consumer, and `test-cli` reports 26 cases / 205 assertions across shell, prompt-runner, prompt-sink, streaming-sink, interactive-REPL, and REPL slash-command coverage. Bench coverage remains the approval-secret / approval / approval-broker / audit-sink / bootstrap assembly buckets. | Publish `permission_ask_resolved` around the broker handoff once the binary uses the prompt path. |
| Hooks                    | C     | Foundation `oran-hook` now ships `hook::Event`, advisory/blocking mode annotations, shared immutable `PayloadPtr` sink delivery, `Sink` trust labels, `InProcessSink`, advisory `Bus::publish_advisory`, constrained `Bus::publish_blocking<E>`, `BusOptions { blocking_timeout }`, and typed payloads for tool lifecycle plus slice-94 `PermissionAskRenderedPayload`, slice-126 provider lifecycle metadata (`ProviderRequestPayload`, `ProviderResponsePayload`, `ProviderErrorPayload`, `ProviderFallbackPayload`), slice-179 memory write/delete lifecycle metadata (`MemoryWritePayload`, `MemoryForgetPayload`), and slice-180 memory read metadata (`MemoryReadPayload`, `MemoryReadHitPayload`), and slice-186 memory decay metadata (`MemoryDecayPayload`) and slice-194 job lifecycle metadata (`JobLifecyclePayload`), and slice-215 job drop metadata (`JobDroppedPayload`). Advisory publishes now start subscribed sinks as sibling child coroutines, gather through a bounded `async::Channel`, preserve subscription-ordered `PublishOutcome` rows, share at most one raw payload snapshot and one default/redacted snapshot per publish, and keep sink errors non-vetoing. Blocking publishes walk sinks in subscription order, use the same shared raw/default-redacted snapshots, record `HookDecisionTrace`, short-circuit at the first non-`proceed`, classify sink errors/throws as `hook_error` vetoes, and race each sink against the configured timeout, synthesizing `reason=hook_timeout` with elapsed metadata on expiry. Slice 152 extends per-sink redaction beyond structured `ToolAfterPayload::data_json`: tool and approval payloads with `redacted_input_json` deliver that sanitized `input_json` to default sinks while `trusted_local` sinks keep the original mutation input. `RuntimeAssembly` owns the process bus with `hooks.timeout_ms` applied, traced direct `tool_before` publishes now get joinable `hook_publish` audit rows via `oran-tool` / `oran-storage`, direct ask dispatches consume `permission_ask_rendered` through the same blocking path, `oran-cli` now ships the first terminal sink for that event, and `agent::Loop` publishes provider lifecycle hooks through the same process bus. Provider payloads are metadata-only by construction and carry no prompt text, messages, headers, credentials, or raw provider bodies. Memory write payloads are redacted for default sinks, preserving id/scope/kind plus byte/count metadata while clearing title/body/tags/linked ids; trusted-local sinks receive raw records. Memory read payloads redact raw recall queries and hit title/body/tags/linked ids for default sinks while preserving source, scope, kind, limit, match, score, timing, and byte/count metadata; trusted-local sinks receive raw queries and hit records. `AgentPromptRunner` now consumes `memory_write_before` for `memory.remember`, publishes advisory `memory_write_after` / `memory_forget`, and publishes advisory `memory_read_after` after successful prompt-boundary recall and `memory.recall` reads, and publishes startup advisory `memory_decay` metadata after configured-route startup retention decay succeeds; `MemoryRetentionService` can publish periodic advisory `memory_decay` metadata after a successful due tick and advisory job lifecycle metadata around due retention ticks when callers supply a bus, and `TriggeredService` can publish advisory job lifecycle metadata around explicit triggered handler attempts when callers supply a bus. `TriggeredQueue` can publish advisory `job_dropped` metadata on drop-newest overflow when callers supply a bus. `test-hook` reports 38 cases / 313 assertions, `test-agent` reports 56 cases / 10 744 assertions for the provider lifecycle producer, `test-tool` reports 208 cases / 2181 assertions for the direct-dispatch consumers, and `test-cli` reports 26 cases / 205 assertions across shell, prompt-runner, prompt-sink, streaming-sink, interactive-REPL, and REPL slash-command coverage. Bench coverage includes advisory fan-out/gather, large redacted payload fan-out, plus slice-91 blocking publish scenarios. | Land the first non-trivial external sink (`ShellSink`) for the design-doc bindings JSON, plus channel payloads and full scheduler/service-loop producers as those producers land. |
| Channels                 | C     | Trait + capability matrix designed. | QQ port + webhook adapter. |
| Orchestration / teams    | C     | Coordination strategies designed. | LeaderWorker + Pipeline. |
| Automation               | C     | Slice 187 adds deterministic `PeriodicSchedule` evaluation plus `MemoryRetentionJob` planning that validates long-term retention policy fields and produces `memory::longterm::DecayRequest` only when the cadence is due. Slice 188 has bootstrap map configured-route `memory.longterm.retention` into that automation-owned descriptor and expose the stored scheduler seed from `RuntimeAssembly` without starting a background loop. Slice 189 adds `AutomationRepository` over `storage::Pool`, with an embedded `automation.db` retention migration, job upsert/load, `last_fired_at`, run recording, recent-run listing, and validation coverage. Slice 190 adds `MemoryRetentionService::tick(...)`, which consumes stored jobs, invokes a supplied memory backend only when due, records success/failure runs, and advances `last_fired_at` only after success. Slice 191 adds optional advisory `memory_decay` publication from that explicit tick owner after successful due retention; advisory sink failures stay non-fatal and are summarized in the tick result. Slice 192 adds `AutomationRuntime::open(...)` as the caller-owned state handle that creates parent directories, opens/migrates `automation.db`, owns the pool/repository lifetime, exposes the migration report, and constructs retention services over stable state. Slice 193 adds `MemoryRetentionLoop::run_once(...)`, a caller-started single awaitable that ticks immediately, waits only within the caller's `max_wait` budget for the next fire, delegates due work back to the retention service, propagates cancellation while waiting, and rejects negative wait budgets. Slice 194 adds advisory `job_started`, `job_failed`, and `job_finished` metadata around due retention ticks while keeping sink failures non-fatal. Slice 195 adds repository-backed retention job leases plus loop-side due-run lease ownership; the loop plans and waits without holding a lease, acquires immediately before due tick execution, releases after the tick, rejects invalid lease budgets, and reports active-lease conflicts. Slice 196 adds finite caller-owned `MemoryRetentionLoop::run(...)` policy over that leased step, stopping at `max_iterations` or when no due work remains within the wait budget and reporting summary counters plus the last step. Slice 197 adds `CronSchedule` plus POSIX 5-field UTC `evaluate_cron_schedule(...)`, covering `*`, lists, ranges, steps, Sunday `0`/`7`, DOM/DOW OR semantics, stored-state advancement, and malformed input without persistence or scheduler startup. Slice 198 adds `automation_cron_jobs` plus repository APIs to upsert/load/list cron schedules and mark cron jobs fired without reading config or starting timers. Slice 199 adds `CronService::tick(...)`, `CronLoop::run_once(...)`, and `AutomationRuntime` cron factories so runtime owners can scan stored cron jobs for due work and wait once within a caller budget without mutating `last_fired_at`. Slice 200 adds `CronService::execute_due(...)`, which invokes a caller-supplied handler for each due cron job and advances `last_fired_at` only after the handler succeeds. Slice 201 adds finite caller-owned `CronLoop::run(...)` policy over due execution, stopping on no due work, iteration limit, or handler failure. Slice 202 adds advisory cron lifecycle hook metadata around handler execution through `CronServiceOptions::hooks`. Slice 203 adds typed `automation.cron.jobs[]` parsing and `bootstrap::cron_jobs_from(...)` mapping into `UpsertCronJobRequest` descriptors stored on `RuntimeAssembly` without opening `automation.db`. Slice 204 adds `AutomationRuntime::apply_cron_job_seeds(...)` as the explicit caller-owned persistence handoff for those descriptors. Slice 205 adds `AutomationRuntime::run_cron_service_cycle(...)`, which validates finite service policy before seed writes, applies supplied seeds, and awaits the existing cron loop in one caller-owned cycle. Slice 206 adds `automation_cron_runs` plus repository record/list APIs, and `CronService::execute_due(...)` records success/failure run rows for due handler attempts while not-due scans stay run-free and failed handlers remain due for retry. Slice 207 adds `CronLoopRunRequest::stop_requested` plus `stop_requested` stop reason so finite cron loops and service cycles can stop cooperatively between execution iterations. Slice 208 adds typed `CronRunOutcome` storage for `success`, `failure`, and `aborted` cron runs; cancelled handler errors are recorded as `aborted`, while failed and aborted attempts keep stored cron state due for retry. Slice 209 adds repository-backed cron execution leases, direct `CronService::execute_due(...)` opt-in lease policy, and default finite-loop/service-cycle lease ownership that reports active conflicts before handlers run. Slice 210 adds stored cron job `agent_key`, repository-backed cron agent leases, same-agent conflict handling before handlers run, and hook payload agent-key attribution. Slice 211 adds `automation_triggered_jobs`, triggered descriptor repository APIs, `TriggeredService::intake(...)`, and the runtime factory for caller-owned trigger-key matching without queueing or agent execution. Slice 212 adds `automation_triggered_runs`, repository record/list APIs, typed `TriggeredRunOutcome`, and `TriggeredService::execute(...)` for caller-supplied handler attempts that record success, failure, and aborted outcomes without queueing or agent execution. Slice 213 adds `TriggeredServiceOptions::hooks` and advisory triggered `job_started`, `job_failed`, and `job_finished` lifecycle metadata around explicit handler attempts. Slice 214 adds repository-backed triggered agent leases plus `TriggeredService::execute(...)` opt-in lease ownership that rejects active same-agent conflicts before handlers and releases after durable outcomes. Slice 215 adds caller-owned `TriggeredQueue` intake buffering over bounded `async::Channel` state, explicit `receive()` draining by callers, `drop_newest` overflow reporting, advisory `job_dropped` metadata, positive-capacity validation, and `AutomationRuntime::triggered_queue(...)` factory coverage. Slice 216 adds `TriggeredService::execute_one(...)` for exact single-descriptor execution and `TriggeredQueue::drain_once(...)` for one-at-a-time queued descriptor draining without re-intaking by trigger key. Slice 217 adds drain-time `drop_on_conflict` handling for active triggered-agent leases, returning `TriggeredDroppedJob(reason=agent_lease_conflict)`, publishing advisory `job_dropped`, and suppressing handler/run-row side effects for the consumed queued descriptor. Slice 218 adds non-blocking triggered queue polling and `TriggeredQueue::drain_available(...)` for finite caller-owned batch draining over the same execution/drop path. Slice 219 adds required non-empty `agent_prompt` fields to cron config seeds plus stored cron/triggered job descriptors, so future agent firing has durable prompt input. Slice 220 adds `AutomationPromptRunRequest`, `AutomationPromptRunner`, and `make_cron_prompt_handler(...)` / `make_triggered_prompt_handler(...)` so callers can adapt those stored prompts into the existing explicit cron/triggered execution paths without moving provider or bootstrap ownership into `oran-automation`. Slice 221 adds bootstrap-owned `make_automation_agent_prompt_runner(...)`, which bridges those prompt runs into configured-route `AgentPromptRunner` execution one durable job at a time, derives stable per-job session identity, applies configured-agent overlays only when present, and keeps noninteractive approvals fail-closed by default. Slice 222 adds caller-owned notifier callbacks plus `AutomationJobHandlerResult` / output-carrying cron and triggered attempt results, preserving prompt-runner text on durable attempts and publishing one post-outcome callback only after the cron run row plus `last_fired_at` advancement or triggered run row has completed, while notifier failures stay advisory on the attempt result. Slice 223 adds `AutomationRuntime::automation_service(...)` plus move-only `AutomationService`, giving callers one composed owner that keeps a bounded triggered queue, validates a full triggered-plus-cron request before side effects, drains buffered triggered work first, then applies cron seeds and awaits the existing finite cron cycle. Slice 224 adds caller-owned blocked-agent hold/retry semantics on top of that owner: `TriggeredQueueBlockedAgentPolicy::requeue_on_conflict` is now consumed by `AutomationService::run_cycle(...)`, which retries previously held triggered descriptors before newer queued ones, preserves public `TriggeredQueue::drain_*` drop-only behavior by rejecting `requeue_on_conflict` there, and records retried triggered `finished_at` / lease timing at the actual attempt time while preserving the original `fired_at`. Slice 225 adds `AutomationService::run(...)`, the finite caller-owned loop policy above that same owner: it repeats explicit cycles over caller-owned iteration and retry-wait budgets, aggregates triggered and cron counters across cycles, sleeps only when held blocked triggered work remains, and stops with explicit `iteration_limit` / `no_due_work` / `handler_failure` / `stop_requested` / `held_jobs_remaining` reasons. `test-automation` reports 106 cases / 1849 assertions, `test-config` reports 51 cases / 468 assertions, `test-bootstrap` reports 134 cases / 1160 assertions for the consuming boundaries, and `bench-automation` reports the 1024-job schedule-evaluation vs. retention-planning comparison. No hidden detached service loop, bootstrap-owned automatic `automation.db` opening, concrete notifier routing, or agent firing exists yet. | Continue the active cron/category plan with long-running startup/shutdown ownership above `AutomationService::run(...)` before detached/background startup or broader agent-facing jobs. |
| Desktop App              | C     | Slint, in-process, local-only; spec drafted. | Chat + session/audit panels. |
| CLI                      | C     | `oran-cli` implements deterministic no-runner REPL and `--prompt` single-shot shells, slice-100's `PromptRunner` / `run_async` seam for caller-owned async prompt execution, and slice-95 `OperatorPromptSink` for terminal `permission_ask_rendered` approvals with scripted-answer tests. Slice 101's bootstrap `AgentPromptRunner` consumes both seams for caller-supplied provider backends, slice 112 reaches that runner from configured-route ordinary `bootstrap::run`, and built-in no-route defaults still use the no-runner shell. Slice 123 adds `cli::StreamingPromptSink` — a `provider::EventSink` that renders answer/thinking deltas to an injectable `std::ostream` (default `std::cout`, flushed per delta) plus a `[tool: <name>]` marker — which `AgentPromptRunner` builds for non-quiet streaming runs so configured-route prompts stream live. Slice 125 adds `CliOptions::interactive_repl`; when `run_async` has a runner, no scripted `repl_lines`, and that flag is true, it reads terminal stdin line-by-line until empty line or EOF and dispatches each prompt through the runner in order. Slice 128 adds CLI-owned REPL slash commands: `/help` prints the command list, `/exit` and `/quit` stop scripted or interactive REPL loops before runner dispatch, and recognized commands do not increment `prompts_processed`. `test-cli` reports 26 cases / 205 assertions. | Add line editor/history; add more slash-command targets only when runtime command surfaces exist. |
| Skills                   | C     | Section-4 catalog renderer + owner land in slice 135, slice 136 adds the first markdown loader snapshot over `<workspace>/.orangutan/skills/*.md` with bounded body/frontmatter reads, deterministic catalog conversion, missing-directory empty snapshots, and bootstrap first-prompt integration, slice 137 wires one-shot `skill.invoke` through the permissioned tool path using that loaded snapshot, slice 138 adds prompt-boundary `WorkspaceSkillSnapshot` refresh with Linux inotify when available plus signature fallback for add/update/remove changes, slice 139 lets selected agent configs filter the catalog/body snapshot through `agents.<name>.skills_enabled`, slice 140 maps the configured-route binary `--agent <name>` selector into that allowlist path, slice 142 derives deterministic active skill markers from successful `skill.invoke` transcript results, slice 143 makes that transcript-derived rule explicit as `skill::ActivationPolicy` / `resolve_active_skills(...)` filtered against the current loaded/allowed catalog, slice 144 lets explicit `deactivated_skill_names` subtract from that active set at prompt boundaries, slice 145 lets explicit `SkillExpiration` rows subtract only when callers provide `evaluation_time`, slice 146 sources those deactivation/expiration inputs from per-agent `agents.<name>.skills_deactivated` / `skills_expirations` config with a runner-supplied evaluation time, slice 147 adds the transcript-event `skill.deactivate` built-in (capability `deactivate_skill`) whose `skill_deactivation` records net against `skill.invoke` activations at the next prompt boundary, slice 148 adds session-store-backed activation records that overlay transcript state so compaction/pruning cannot lose the latest active/inactive decision, and slice 149 exposes shared `SkillActivationEvent` extraction for runtime owners that need to persist the same transcript events. | Broader cross-runtime skill policy ownership remains downstream. |
| Observability            | D     | Logging shim designed. | Metrics endpoint. |
| Security defaults        | B     | Captured. | Implement secret rotation. |
| Supply chain             | B     | Workflows pinned; package versions are declared in `xmake/packages.lua` and documented in `docs/rules/libraries.md`. PR supply-chain checks now keep Dependency Review as an explicit `ENABLE_DEPENDENCY_REVIEW=true` opt-in for repositories where GitHub supports it, while OSV scanner is pinned to a valid immutable upstream commit and runs by default. `xmake-requires.lock` is intentionally ignored until CI owns deterministic refresh/check semantics. | Add a CI-owned xmake resolver check or lockfile workflow before treating resolver output as reproducibility evidence. |
| Static analysis          | C     | `-fanalyzer` wiring shipped (rule + xmake option); focused `oran-io` and `oran-storage` analyzer builds pass locally, but analyzer CI remains a planned nightly gate. | Run/record analyzer coverage again for later descriptor-heavy code. |

Current bench note: slice 66 adds `bench-tool` coverage for
`output.apply_caps`, alongside the existing `output.text_only` and
`output.with_data_16kib` structured-output scenarios.

Current provider note: slice 73 added the first `oran-provider` target and
adapter-facing prompt-cache hint mapping; slice 74 closes the provider
prework for spec 0017 by shipping the abstract `provider::System`, the
streaming `EventSink`, the `ProtocolKind` / `ModelTarget` / `Route` value
shapes, and the first concrete `provider::FakeProvider` with cancel-aware
scripted latency; slice 97 adds `provider::execution::Runtime` for retryable
per-target retry and route fallback; slice 98 adds
`provider::resolve_route` for resolving config profiles/routes into
`provider::Route`; slice 102 lets that resolver prefer explicit
`profiles.<name>.protocol` values over provider-alias inference; slice 103 adds
`provider::resolve_route_profiles` for preserving provider/base-url/API-key-env
metadata beside the loop-facing route; slice 104 adds
`provider::make_adapter_construction_plan` for offline endpoint preflight and
adapter-family classification; slice 105 adds
`provider::resolve_adapter_credentials` for the explicit future-factory
secret-read boundary; slice 106 adds `provider::make_adapter_system` for the
factory dispatch seam that consumes credentials and registered protocol
factories to produce a profile-routed `provider::System`; slice 107 adds
`provider::make_protocol_request` for offline Anthropic/OpenAI Responses request
serialization and structured tool-result mapping; slice 108 adds
`provider::decode_protocol_response` for offline Anthropic/OpenAI Responses
response decoding; slice 109 adds `provider::ProtocolTransportAdapterFactory` for injected `ProtocolTransport`-backed systems; and slice 101 consumes route resolution plus
the execution wrapper from bootstrap's
`AgentPromptRunner` when a caller supplies a backend.
HTTP body transport and SSE transport exist in `oran-http`, bootstrap now has a
configured-route `HttpProviderBackend` handoff through the ordinary binary path,
and provider lifecycle hooks, trace-derived usage rollup reads, and profile-priced
cost calculation have shipped. The remaining protocol families stay downstream.

Current agent note: slice 96 refreshes `DispatchContext::now` around every
direct tool dispatch and pins the broker-backed `permission_ask_rendered`
approval path in the fake-provider loop. Slice 86 adds iteration-cap error
trace rows on top of slice 85's loop-owned turn-id generation, slice 84's provider/loop-boundary error
trace rows, slice 83's provider/tool
cancellation trace rows, slice 82's explicit trace-disabled policy, and slice
80's first loop-owned trace writer for the sequential tool-dispatch
`agent::Loop` over `provider::System`. Tool calls now run through the existing
registry boundary, re-enter the provider, persist parent provider/tool
cancellations as `stop_reason=cancelled` trace rows, stamp non-cancelled
provider and response-backed loop-boundary failures as `stop_reason=error`
trace rows, persist iteration-cap exits as `stop_reason=error` trace rows with
`iteration_count = LoopOptions::max_iterations`, generate a missing turn id for
configured trace writers, stamp direct-dispatch audit rows with that id when
tracing is enabled, append one terminal-success `trace_turns` row through
enabled `RunTurnInputs::trace`, and preserve trace-off audit rows by writing no
trace row and keeping direct-dispatch `parent_turn_id` NULL when
`TraceContext::enabled=false`. The direct approval test now proves the
permission prompt payload's request time / replay policy, audit
`permission_ask_decisions` metadata, replay token, tool result, and provider
re-entry; slice 101 wires the execution-runtime-wrapped supplied backend into
the loop through bootstrap's `AgentPromptRunner`, slice 131 gives bootstrap an
assembly-owned session store, and slice 132 makes the runner persist and reload
conversation history through that store, slice 133 has the runner render
`memory::FramingOwner` once before entering the loop, slice 134 adds the
default `agent::SystemPreambleOwner` path for stable section-1 prompt bytes, and
slice 135 adds the section-4 `skill::CatalogOwner` plus deterministic catalog
rendering before loop entry, slice 136 feeds it from a `skill::Loader`
snapshot before the first configured-route prompt, slice 137 wires `skill.invoke`,
slice 138 refreshes the skill catalog/body snapshot at prompt boundaries, and
slice 142 derives next-prompt active skill markers from successful
`skill.invoke` transcript results that still match the current loaded/allowed
snapshot, slice 143 moves that rule behind `skill::ActivationPolicy` /
`resolve_active_skills(...)`, and slice 144 adds explicit deactivation names
to that policy. Slice 145 adds explicit expiration rows plus caller-provided
evaluation time to the same policy. The remaining long-term memory loader and
runtime-owned deactivation/expiration event work are downstream.

Current storage note: slice 80 consumes the spec-0018 trace repository from the
agent loop. Slice 79 adds the audit join key on top of the
slice-78 trace foundation. `TraceRepository` can migrate and persist redacted
`trace_turns` rows, and permission audit rows can now carry typed
`parent_turn_id`; the agent loop now writes terminal-success turn rows when
configured with an enabled repository, generates a missing turn id for that
writer, and skips rows for explicitly disabled trace contexts. Slice 83 also
writes cancelled rows for provider/tool parent cancellations, and slice 84
writes ordinary provider/loop-boundary error rows. Slice 127 adds the first
trace-derived provider usage rollup read, grouped by UTC day, agent key, route
profile, and route model.

Current tool-registry note: slice 67 adds same-row audit usage metadata
enrichment after successful direct-dispatch output caps. Slice 107 consumes structured tool-result bytes at the provider request-mapping boundary for Anthropic/OpenAI Responses, slice 126 adds provider lifecycle hook metadata from the agent loop, slice 127 adds trace-derived provider usage rollup reads, and slice 129 adds profile-priced cost calculation over provider token usage. The remaining spec-0014 adapter work is other protocol families. Spec-0012 scheduler work is ownership of cap options and audit correlation for batched agent calls, not a second copy of cap or usage-enrichment logic. Slice 68 adds the
registry-local `tool.search` lookup primitive for spec 0016; slice 69 adds
the typed active-tool config selector; slice 70 adds the prompt-builder
consumer; slice 71 adds prompt-side promotion state and builder snapshot
consumption; slice 72 adds `agent::SessionState` as the owner that mutates
session promotion state after successful `tool.search` output; slice 73 adds
the provider cache-hint mapper consumed before the fake-provider loop lands.

## Cadence

Reviewed at every v1.x milestone. PRs that move a row's score change it (up or down)
in the same change.

## How To Move A Row Up

- D → C: implementation exists + smoke test passes.
- C → B: full test bucket green, design doc current.
- B → A: bench coverage in place, ≥ 30 days of stable operation, no known issues
  in the tech-debt tracker for that area.
